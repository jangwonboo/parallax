node_modules/
dist/
release/
*.log

# 비밀
anthropic.key
*.key
.env
.env.*

# 작업 문서와 원본 — 원문이 전량 들어 있고 크다.
# .parallax 는 번역이 진행될 때마다 통째로 바뀌는 SQLite 라 히스토리에 넣으면 급격히 커진다.
*.parallax
*.parallax-shm
*.parallax-wal
*.pdf
# md 스냅샷 백업 쌍 — 파일은 지우지 말 것(CONTEXT.md), 원문 전량이라 히스토리에는 안 넣는다
*.en.md
*.ko.md

# 파이프라인 중간 산출물
work/
out/

# 스킬을 푼 사본 — 정본은 pdf-ko-translate.zip
pdf-ko-translate/
blocks.txt

# Supertonic 자산 383MB. 고정 리비전으로 언제든 다시 받는다 —
# scripts/fetch-tts-assets.mjs, 해시는 tts-assets/VERSION 에 있다.
tts-assets/

# 낭독 참고 구현이 들어온 배달 꾸러미. 내용은 이미 풀려 있다 —
# spec-tts.md(=tts/spec.md)와 vendor/supertonic-nodejs/.
tts.zip
