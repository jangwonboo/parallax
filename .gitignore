node_modules/
dist/
release/
*.log

# 비밀
anthropic.key
*.key
.env
.env.*

# 작업 문서와 원본 — 원문이 전량 들어 있고 크다.
# .parallax 는 번역이 진행될 때마다 통째로 바뀌는 SQLite 라 히스토리에 넣으면 급격히 커진다.
*.parallax
*.parallax-shm
*.parallax-wal
*.pdf
# md 스냅샷 백업 쌍 — 파일은 지우지 말 것(CONTEXT.md), 원문 전량이라 히스토리에는 안 넣는다
*.en.md
*.ko.md

# 파이프라인 중간 산출물
work/
out/

# 스킬을 푼 사본 — 정본은 pdf-ko-translate.zip
pdf-ko-translate/
blocks.txt

# ko-style 은 소스를 추적한다(CONTEXT.md) — 파이썬 부산물만 뺀다
__pycache__/
*.pyc
