# 네이티브 모듈(better-sqlite3)을 Electron ABI 로 내려받습니다.
# 이걸 지정하지 않으면 npm 이 설치한 Node 용 미리 빌드된 바이너리를 찾다가
# 못 찾으면 소스 컴파일로 넘어가고, Windows 에서 Visual Studio 를 요구합니다.
# target 은 devDependencies 의 electron 메이저와 맞춰 두세요.
runtime=electron
target=33.4.11
disturl=https://electronjs.org/headers
