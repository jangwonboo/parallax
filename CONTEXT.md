# CONTEXT — 지금 어디까지 와 있나

새 세션이 이 저장소를 이어받을 때 먼저 읽는 문서. 설계는 `spec.md`, 실행 방법은 `USAGE.md`.

최종 갱신: 2026-08-12

---

## 이 프로젝트는 네 조각이다

| | 무엇 | 어디 |
|---|---|---|
| **Parallax 앱** | 좌우 대역 리더. Electron + better-sqlite3. 읽기와 스크롤 구동 번역. | `src/` |
| **pdf2parallax 스킬** | PDF → 구조 복원 → 배치 번역 → `.parallax` 내보내기. Python. | `pdf2parallax.zip` |
| **ko-style 스킬** | 한국어 톤앤매너 명세(문체 6종 · 축 6개) + 개조식 종결 검사. 파이프라인과 무관하게 단독으로 쓴다. | `ko-style/` · `ko-style.zip` |
| **vlmparse** | PDF → VLM OCR → 구조 있는 블록. 판독 계층만 떼어 낸 **별도 저장소**(2026-08-16). | [jangwonboo/vlmparse](https://github.com/jangwonboo/vlmparse) (비공개) |

**`vlmparse` 는 `pagecheck.py` 에 엉켜 있던 Datalab 판독 경로를 떼어 낸 것이다.** 경계는 「PDF → (블록, 그림)」이고, 그 아래(일치율 계산·기존 book.json 병합·이음매 수리)는 parallax 고유라 남겼다. 떼어 보니 `extract.py` 의존이 0이었다 — 그 아홉 함수는 전부 병합 쪽이 쓰고 있었다. 자체 CLI(`vlmparse read book.pdf --out book.json`)와 시험 22개가 있고, 실측으로 얻은 것들(429 는 대기다·전송 오류도 물러난다·과금 단위는 센트다·asset id 는 내용 sha1 이다)이 주석과 시험으로 옮겨져 있다.

**아직 parallax 에 물리지 않았다.** 스킬이 zip 으로 배포돼 설치 단계가 없으므로 `pip` 의존으로 두면 배포가 깨진다 — **git subtree** 로 사본을 안에 두는 방식을 쓸 계획이다(저장소는 독립, zip 은 자족).

둘은 `.parallax`(SQLite) 파일로만 오간다. 스키마는 `spec.md` §2.2와 스킬의 `scripts/_parallax.py`에 **이중으로** 적혀 있다 — 한쪽을 고치면 다른 쪽도 고쳐야 한다.

**렌더링은 앱만 한다.** 스킬에 있던 `render.py`와 `templates/`(이북 HTML·테마 CSS)는 2026-07-27에 제거했다. 같은 조판을 두 벌 유지하면 반드시 어긋나고, 실제로 쓰이는 쪽은 앱이다. 스킬의 마지막 단계는 `export.py`(`.parallax` 생성)다.

---

## 앱 상태

`spec.md` §10 기준 E1~E5(스케줄러·pagecheck 통합) 구현. `tsc --noEmit` 통과.

> **낭독(TTS)은 2026-08-06 에 통째로 걷어냈다**(사용자 지시). 지운 것: `src/main/tts/`·`vendor/supertonic-nodejs/`·`scripts/fetch-tts-assets.mjs`·`spec-tts.md`, main 의 `tts:*` IPC 전부, preload 의 `tts` 표면과 `tts:state`/`tts:progress` 채널, 렌더러의 낭독 툴바(`sayBar`)·하이라이트·mp3 내보내기, `types.ts` 의 `isSpeakable`, 의존성 `onnxruntime-node`·`@breezystack/lamejs` 와 `asarUnpack`, `.gitignore` 의 `tts-assets/`·`tts.zip` 항목. `.npmrc` 와 `scripts/rebuild-native.mjs` 는 better-sqlite3 용이라 남는다. 사전 열기(더블클릭)와 짝 강조는 낭독과 무관하게 그대로다. 아래 낭독 관련 기록은 전사(前史)로만 남긴 것이다 — johnb PC 의 `tts-assets/` 정션과 `tts.zip` 도 이제 쓸 데가 없다.

2026-08-16 저녁 — **표를 그림으로 오려 낸다**(사용자 지시: 「표 capture 하는 것은 버려줘. 품질이 안 나오니 그림으로 간주」). HTML 재구성 자체는 잘 됐지만(rowspan 병합·셀 안 목록·엔티티까지 렌더 시험 통과) 편집된 표의 조판까지는 살리지 못한다 — 원본을 오려 내면 보이는 것이 원서 그대로다.

**Datalab 은 표 그림을 주지 않는다.** Table 청크의 `images` 는 비어 있고 HTML 만 온다. 대신 **`bbox` 가 오는데, 그것이 우리가 보낸 쪽 이미지와 같은 좌표계다**(실측: 쪽 1485×2200 에 bbox 최대 1384×1240) — 배율 환산 없이 그대로 자르면 된다. `Table`·`TableGroup`·`Form` 을 오려 `figure` 로 낸다(`tables="image"` 기본, `--tables html` 로 옛 동작).

- **글을 버리지 않는다.** HTML 을 `asset.alt` 에 함께 넣어 검색과 나중의 셀 단위 번역 여지를 남긴다(실측 2612·1212·1039자). 보이는 것은 그림, 글은 딸린 데이터다.
- **JPEG 품질 90 + `subsampling=0`(4:4:4).** 무손실 PNG 는 표 셋에 1.5MB 였다. 기본 4:2:0 이면 표는 글자라 획 가장자리에 색번짐이 생긴다 — 4:4:4 로 끄면 눈으로 무손실과 구별되지 않으면서 1,565 → 762KB 다. **90 아래로 내리지 말 것.**
- **오려 낼 여유는 5px.** 10px 에서는 50쪽 표에 아래 각주 한 줄이 딸려 왔다.
- 쪽 이미지가 없거나·bbox 가 없거나·오려낸 것이 너무 작으면 `table_raw` 로 물러난다.

**`asset.wfrac`(원본 쪽 폭 대비 폭, 0~1)을 새로 싣는다.** 상한만 걸면 쪽 귀퉁이의 140px 아이콘도 큰 도판과 같은 폭이 되어 **원서의 크기 관계가 사라진다.** 리더는 `width: min(wfrac%, 원본px)` 로 그린다 — 두 번째 항이 확대를 막는다(비율로는 크게 나와도 제 픽셀 크기를 넘지 않는다). 상한은 60% → **90%** 로 올렸다(사용자 지시). 실측: 표 0.81~0.83 → 화면 444~456px, 아이콘 0.094 → 51px. **`wfrac` 은 1로 자른다** — 판독기가 쪽 밖으로 넘는 bbox 를 줄 때가 있다(실측 1.1342).

스키마가 늘어 **네 곳을 함께** 고쳤다: 앱 `db.ts`(DDL·`Doc.create`·SELECT 둘)·`types.ts` 의 `AssetMeta`·스킬 `_parallax.py` DDL·`export.py` INSERT·`parallax_import.py` 왕복. `wfrac` 없는 옛 파일은 상한만 받는다.

> **파이프라인이 새 경로를 못 쓰고 있었다 — 이것이 핵심 수정이다.** `pagecheck.py` 가 `chunk_items(chunks)` 로만 불러 **쪽 이미지를 안 넘겼고**, 그래서 표가 조용히 `table_raw` 로 떨어져 리더가 HTML 을 글자로 보여줬다. `page_image=page_img(pno)` 를 넘기고 `--tables` 옵션을 붙였다. 함께 고친 것 둘: **`--pages` 가 범위 하나만 받아** `50,97,429` 로 주면 `ValueError` 로 죽었다(`vlmparse.render.parse_pages` 를 쓰게 했다 — 쪽마다 돈이 나가는 도구에서 명령을 여러 번 치게 하면 같은 쪽을 두 번 산다), 그리고 그 자리에 남아 있던 `lo`/`hi` 참조.

2026-08-16 이어서 — **스킬 이름을 `pdf-ko-translate` → `pdf2parallax` 로 바꿨다**(사용자 지시). 판독을 `vlmparse` 로 떼어 내고 나니 옛 이름이 하는 일을 못 말했다. 이제 셋의 이름만으로 경계가 읽힌다:

```
vlmparse       쪽 이미지 → 블록      (VLM 판독, 선택)
pdf2parallax   PDF → .parallax       (추출·번역·조립)
ko-style       한국어 문체           (독립)
```

> 후보 중 `pdf_extract`·`blk_to_parallax` 는 **틀린 이름이다.** 추출은 `vlmparse` 로 간 것이 아니다 — `extract.py`(923줄, PDF 텍스트 레이어)는 **여전히 이 스킬 안**에 있고, `vlmparse` 가 가져간 것은 `pagecheck.py` 안의 Datalab 재판독 경로 **하나**다. 두 판독기는 읽는 대상이 다르다(텍스트 레이어 vs 쪽 이미지). 그래서 입력은 여전히 PDF 이고 `blk_…` 는 성립하지 않는다.

바꾼 곳 41군데 — `.gitignore`·`README.md`·`USAGE.md`·`spec.md`·`ko-style/SKILL.md`·`scripts/sync-vlmparse.mjs`·`src/main/importers/index.ts`(**`findSidecar()` 탐색 경로 셋**)·`src/main/index.ts`·`src/renderer/{index.html,reader.css}`·스킬의 `SKILL.md`(frontmatter `name:` 포함), 그리고 zip 과 폴더 이름. `PARALLAX_SKILL_DIR` 환경변수 이름은 그대로다. `~/.claude/skills/` 에는 설치본이 없어 손댈 것이 없었다.

2026-08-16 이어서 — **vlmparse 를 subtree 로 물리고 파이프라인의 수식 손실을 고쳤다**(사용자 지시). 이제 **새로 변환하는 책도 수식을 지키지 않는다** — 아니, 지운다가 아니라 **지킨다**.

`pagecheck.py` 의 Datalab 덩어리 **188줄이 32줄**이 됐다. 429 백오프·폴링·과금 집계·그림 수확·chunks 대응이 전부 `vlmparse` 로 갔고, 남은 것은 얇은 위임 셋(`datalab_key`·`datalab_read_page`·`chunk_items`)뿐이다. 옮기면서 고쳐진 것이 **수식 보존**이다 — 옛 `html_clean` 이 `<math>` 를 걷어내던 자리가 이제 인라인 `$…$`·별행 `$$…$$` 로 남는다.

- **`equation` 도 번역 대상이 아니다.** `rebuild_page` 의 두 경로(빈 쪽·일반)에서 `figure` 와 같이 `translate=False` 로 둔다. 수식의 `src` 는 LaTeX 라 번역할 것도, 판독 텍스트라 할 것도 아니다.
- **스킬은 zip 하나로 배포돼 설치 단계가 없다** — `findSidecar()` 가 폴더를 찾을 뿐이라 `pip install` 을 시킬 자리가 없다. 그래서 `pip` 의존이 아니라 **사본을 zip 안에** 넣는다: `scripts/vlmparse/`. 스크립트는 자기 폴더가 `sys.path[0]` 이므로 `import vlmparse` 가 그대로 걸린다. 저장소는 독립을 유지하고 zip 은 자족한다 — 이것이 subtree 를 고른 이유다.
- **`node scripts/sync-vlmparse.mjs [--zip]`** 이 `vendor/vlmparse/src/vlmparse` → 스킬 사본 복사와 zip 재포장을 한다. 저장소를 오가는 것은 `git subtree pull/push --prefix=vendor/vlmparse`.
- 개발 중에는 스킬 사본이 없어도 된다 — `ImportError` 면 `vendor/vlmparse/src` 를 직접 본다.

실측: 풀어 놓은 zip(29항목, 전 19)에서 `pagecheck.chunk_items` 가 `Equation` → `[{'type':'equation','text':'$$E=mc^2$$'}]` 를 낸다. `pagecheck.py --help` 정상, 위임 셋과 `Spend` 객체 연결 확인.

2026-08-16 이어서 — **《Beyond Weird》의 망가진 수식을 원본 PDF 로 되살렸다**(사용자 지시). `beyond_weird.parallax` 의 **35블록**에 구분자가 돌아왔고 맨몸 LaTeX 는 **0**이 됐다(수식 구간 254개). 백업은 `beyond_weird.parallax.bak-math-202608152238`.

**블록 글을 통째로 갈아 끼우지 않았다.** 재판독본에서 **수식 구간만** 뽑아 기존 글의 같은 자리를 `$…$` 로 감쌌다 — 그래야 개조식 번역과 그동안의 사후 손질이 그대로 남는다. 21쪽만 다시 읽어 **$0.20**. `vlmparse` 로 읽었다(수식 보존 규칙이 거기 있다).

세 겹으로 감쌌다. ① 그 쪽의 재판독 수식(가장 확실하다) ② 책 전체의 재판독 수식 중 **LaTeX 명령이 든 것만**(판독은 실행마다 조금씩 달라 어떤 쪽은 수식을 아예 안 잡아 준다 — p74·p155 가 그랬다. 숫자만 있는 것은 다른 쪽에 옮기면 엉뚱한 숫자를 잡으므로 제외) ③ 그래도 남은 **맨 LaTeX 토큰**만 딱 감싼다(`dime \rightarrow cat` → `dime $\rightarrow$ cat`).

**예행(dry-run)이 두 번 살렸다.** 처음 돌렸을 때 ① 홑글자 수식(`x`·`y`·`z` — 판독기가 이탤릭 변수를 정당하게 수식으로 준다)이 낱말 속에서 잡혀 `physicist` → `ph$y$sicist`, `Max` → `Ma$x$` 가 됐고, ② 자리표시자를 `\x00{번호}\x00` 로 만든 탓에 짧은 숫자 수식(`0`·`-1`)이 **그 번호 안에서** 매칭돼 `180^\circ` 가 `$0$` 로 뭉개졌다. 양옆 경계 검사와 「자리표시자는 어떤 패턴에도 안 걸리는 글자 하나(U+E000~)」로 고쳤다. **파일을 고치는 일은 예행부터 붙일 것.**

리더 쪽도 하나 고쳤다 — **`$-1$`·`$0$` 이 글자 그대로 보였다.** 수식 판별이 LaTeX 신호(`\ ^ _ { }`)만 봤는데, 판독기는 기울임 값에도 수식 표시를 붙인다. 돈 표기와 가르는 기준을 **「영어 낱말이 들어 있는가」**로 바꿨다(`$5 million and the next one $` 는 사이에 낱말이 있고 진짜 수식 조각에는 없다) + 길이 상한 16자. 실측: `b0614` 가 katex 4개 → **13개**, `$10 million` 가격 블록은 여전히 0개.

> 확인하다 겪은 것 둘. **`vlmparse` 첫 호출이 `CERTIFICATE_VERIFY_FAILED` 로 죽었다** — TLS 검사 장비다. `truststore.inject_into_ssl()` 을 넣어 해결(스킬의 `_llm.py` 가 이미 하던 것을 옮겨 오지 않았다). 그리고 **측정 스크립트의 정규식이 텍스트에서 `s` 를 지워** 데이터가 깨진 것처럼 보였다 — DB 원문을 직접 확인해 무사함을 확인했다. **화면 측정값이 이상하면 원본부터 볼 것.**

2026-08-16 — **수식이 산문으로 뭉개지던 것을 찾아 리더가 그리게 했다**(사용자 질문에서 시작).

**Datalab 이 주는 형식은 이렇다**: markdown 은 `$$…$$`, **html/chunks 는 `<math>` 태그 안의 LaTeX**(MathML 이 아니다). `block_type` 에 수식 전용이 **둘** 있다 — `Equation`(별행)과 `TextInlineMath`(인라인이 든 단락). 인라인은 `balanced` 이상에서만 LaTeX 로 변환된다(`fast` 는 텍스트 레이어를 긁어 놓친다). 우리는 기본이 `accurate` 라 충족한다.

**그런데 파이프라인이 그것을 지우고 있었다.** `DATALAB_TYPE` 에 두 유형이 없어 둘 다 `p` 로 접히고, `html_clean()` 이 `<math>` 를 걷어내 **안쪽 LaTeX 만 맨몸으로** 남겼다. 실측: `beyond_weird` 본문에 `\Delta e \times \Delta d`·`h/2\pi`·`\mathbf{M}` 이 산문처럼 박혀 있고(7블록), 번역기가 글로 보고 제각각 처리했다 — 같은 책에 `∆e × ∆d`(유니코드로 바꿈)와 `3 \times 2`(그대로)가 섞여 있다. 표를 `table_raw` 로 살린 것과 같은 원리인데 수식만 그 예외에서 빠져 있었다. **이미 만든 책은 복구가 안 된다** — 태그가 판독 단계에서 지워졌으므로 해당 쪽을 재판독해야 한다(쪽당 1¢).

**리더는 KaTeX 를 동봉해 그린다**(사용자 지시). CDN 은 CSP `script-src 'self'` 가 막고, 리더는 비행기에서도 열려야 한다. 저장소에 바이너리를 커밋하지 않고 **빌드가 `node_modules` 에서 복사한다**(`copy-assets.mjs`) — 폰트는 **woff2 만**, 600KB. woff·ttf 까지 넣으면 1.2MB 인데 Chromium 은 woff2 를 찾은 뒤 나머지를 요청하지 않는다.

- **`$` 가 보인다고 다 수식이 아니다.** 본문에 `$5 million to $10` 같은 돈 표기가 흔하다(signals 에 10곳). 안쪽에 LaTeX 신호(`\ ^ _ { }`)나 수학 기호가 있을 때만 수식으로 본다 — **애매하면 글자 그대로** 두는 쪽이 안전하다. 수식이 글로 보이는 것은 읽을 수 있지만 문장이 수식으로 깨지면 못 읽는다.
- **크기를 따로 주지 않는다.** `.katex` 는 em 기반이라 본문 크기를 그대로 따라간다 — 조판 슬라이더가 수식에도 그대로 듣는다. 색만 `inherit` 로 물려준다(어두운 테마에서 상속이 끊기면 검은 식이 검은 바탕에 놓인다).
- `equation` 블록은 **그림과 같은 취급** — 좌우 두 칸에 같은 식을 하나씩. 긴 식은 그 칸만 가로로 굴린다(본문 전체가 밀리는 것보다 낫다).
- `schema_version` 은 **올리지 않았다** — 테이블 구조가 그대로고 `type` 은 원래 자유 문자열이라 옛 리더가 열어도 `$$…$$` 가 글자로 보일 뿐이다.

실측(시험용 `.parallax` 를 만들어 CDP 로): 별행 셋(`E=mc^2`·슈뢰딩거·가우스 적분)이 **양쪽 칸에 각각** 렌더(katex 노드 2개씩), 인라인 넷이 든 단락은 8개(4×2칸), **돈 표기 블록은 katex 노드 0개**(글자 그대로 남음), **깨진 LaTeX 는 붉게 표시되고 뒤 문장이 이어진다**(렌더가 멈추지 않는다). 두 테마 모두 색 상속 확인, 폰트는 20개 등록 중 실제 쓰인 4개만 로드. 내보내기도 두 언어 모두 `$$…$$`·`$…$` 를 그대로 통과시킨다.

> **`vlmparse`**(별도 저장소, 아래 항목)에 이 규칙이 이미 들어가 있다. parallax 파이프라인 쪽(`chunk_items`·`html_clean`)은 아직 옛 상태다 — subtree 로 물릴 때 함께 바뀐다.

2026-08-15 밤 마지막 — **설정이 조용히 증발하던 구멍을 막았다**(사용자가 「글꼴 설정이 바뀐 듯」이라고 해서 찾았다). 실제로 `settings.json` 이 이 세션 중에 **260바이트 → 143바이트**로 줄었고 `theme` 이 사라져 있었다.

원인은 두 줄이었다. `readSettings()` 가 **어떤 오류든 `{}`** 를 돌려주고, `settings:set` 이 그 위에 패치를 병합한다 — 파일이 한 번만 못 읽히면 그 다음 저장이 나머지를 통째로 날린다. 로그도 없다. **불러온 것은 이 세션에서 앱을 `taskkill /F` 로 여러 번 강제 종료한 것으로 보인다**(`writeFileSync` 도중에 끊기면 본체가 잘린다). 검증하느라 앱을 죽일 때는 `/F` 를 붙이지 말 것.

두 겹으로 막았다. ① **쓰기를 원자적으로** — 임시 파일에 쓰고 `renameSync`(같은 볼륨이라 원자적). 도중에 죽어도 본체가 잘리지 않는다. ② **못 읽으면 덮어쓰지 말고 격리** — `readSettingsSafe()` 가 값과 함께 `trusted` 를 돌려주고, 파일이 **아직 없는 것**(빈 설정이 정답)과 **깨진 것**(덮어쓰면 안 됨)을 가른다. 깨졌으면 `settings.json.bad-<epoch>` 로 옮기고 경고를 남긴다 — 잃더라도 되돌릴 길은 남는다.

실측(진짜 설정을 건드리지 않게 `--user-data-dir` 로 따로 돌렸다): ① 멀쩡한 설정에 `set` 한 번 → 기존 여섯 키가 **전부 살아남고** 새 키만 붙는다, 부팅 테마 `night`·한글 글꼴 `"Nanum Myeongjo"` 적용 확인. ② 쓰다 만 JSON → `settings.json.bad-1786798090465` 로 격리되고 **원문이 그대로 보존**된다(예전에는 흔적 없이 증발). ③ `.tmp` 잔재 없음.

`theme` 은 `night` 로 되돌렸다(2026-08-06 기록의 「초기 테마 복원(night)」이 근거다). 되돌리기 전 파일은 `settings.json.before-theme-restore` 로 남겼다. 글꼴·조판 값(`faceKo 나눔명조 · sizePt 16 · leadStep 2 · paraStep 2 · width 90`)은 **초기화되지 않았다** — 전부 기본값과 다른 사용자 값 그대로였다.

2026-08-15 밤 이어서 — **짝 강조에 페이드를 넣었다**(사용자 지시, 타이밍만·색은 그대로). `--hl-in: .13s` / `--hl-out: .32s` 를 `--dur` 과 **따로** 뒀다 — `--dur` 은 목차 슬라이드·조판 패널·손잡이·목차 강조가 함께 쓰는 값이라 여기를 늘리려고 건드리면 목차 여닫힘까지 느려진다. 쉬는 상태(`.cell`)의 전이가 곧 **사라지는 쪽**이고, `:hover`/`.mate` 쪽에서 `transition-duration` 만 덮어써 켜질 때를 빠르게 한다. 단축 `background` 를 `background-color` 로 좁혔다. 나가는 쪽을 길게 잡으면 커서를 훑을 때 사라지는 중인 단락이 겹쳐 혜성 꼬리가 되므로 `ease-out` 으로 초반에 알파를 떨군다.

`mouseleave` 도 함께 넣었다(`app.js`). 없으면 커서가 툴바·목차로 간 뒤에도 마지막 단락의 `.mate` 가 남는데, `:hover` 는 저절로 풀리므로 **커서 쪽 칸만 꺼지고 반대쪽 칸만 켜진 채**로 남아 짝이 아닌 것이 짝처럼 보였다. 사라지는 페이드가 가장 잘 보여야 할 순간이 바로 이 자리다.

> **이 PC 는 `prefers-reduced-motion: reduce` 가 켜져 있다 — 앱의 모든 전이가 죽어 있다.** 실측: `matchMedia('(prefers-reduced-motion: reduce)').matches === true`, 계산된 `transition-property: none · duration: 0s`, 전이 이벤트 0건. `reader.css` 의 `@media (prefers-reduced-motion: reduce) { * { transition: none !important; } }` 가 페이드만이 아니라 **목차 슬라이드·쐐기 회전·부드러운 스크롤까지** 전부 끈다. 화면에서 애니메이션이 안 보이면 **코드를 의심하기 전에 이것부터 볼 것.** 원인은 Windows 설정 → 접근성 → 시각 효과 → 애니메이션 효과(끄면 reduce 로 보고된다). CDP 로는 `Emulation.setEmulatedMedia({features:[{name:'prefers-reduced-motion',value:'no-preference'}]})` 로 뒤집어 실측할 수 있다 — 그렇게 재보니 켜질 때 130ms(중간색 9단계) · 꺼질 때 320ms(21단계)로 정확히 의도대로 돌았다. 사용자는 규칙을 좁히는 대신 **Windows 설정을 켜는 쪽**을 골랐으므로 이 전역 규칙은 그대로 둔다.

2026-08-15 밤 이어서 — **도움말 → 「기능 설명」(F1)을 넣었다**(사용자 지시). 도움말에는 sqlite.org 링크 하나뿐이었다. **네이티브 대화상자가 아니라 앱 안 패널이다** — `dialog.showMessageBox` 는 OS 룩이라 테마를 따라오지 못하고(툴바를 웹 버튼으로 바꾼 것과 같은 이유), 글이 길어 스크롤도 안 된다. 내용은 `index.html` 에 붙박이로 둔다(상태가 없으므로 렌더러가 만들 것이 없다). 여덟 절: 읽기 · 목차 · 조판 · 사전 · 번역 · 내보내기 · 단축키 · 문서. 채널은 `help:show` 로 preload 화이트리스트에 추가했다 — **빠뜨리면 `api.on` 이 throw 해서 부팅 중에 리더 전체가 죽는다**(그래서 검증에 채널 확인을 따로 넣었다).

글자는 `--ui-size` 고정이다. 목차는 본문 조판을 따르지만(항목이 본문 제목 그 자체다) 이 글은 앱이 하는 말이라 본문을 따라 커지면 안 된다 — 커지면 설명이 화면을 넘는다.

실측: F1 이 실제로 연다(메뉴 accelerator → IPC → 렌더러 전 구간), 툴바 아래 19px·뷰포트 안·세로 스크롤·가로 넘침 없음, 두 테마 모두 바탕·글자 살아 있음, 닫는 길 셋(Esc · 닫기 · 바깥) 모두 동작. **Esc 는 한 겹씩 닫힌다** — 넷을 다 띄우고 네 번 눌러 `도움말 → 사전 → 조판 → 목차` 순서를 확인했다(설명에 적은 순서와 실제가 같아야 한다).

2026-08-15 밤 — **사전 더블클릭이 다음 낱말까지 삼키던 것을 고쳤다**(사용자 보고). 낱말을 짚으면 사전에 두 낱말이 떴다 — 실측 재현: `Common` 더블클릭 → 선택 `Common Marketing`, 조회어도 그것.

- **원인은 「늘리기」의 출발점이었다.** 크로미엄의 낱말 선택은 **뒤따르는 공백까지** 물고 온다(`Common ` ). 핸들러는 그 상태에서 끝을 낱말 경계까지 바깥으로 늘렸는데, 끝이 이미 공백 뒤 다음 낱말의 첫 글자에 놓여 있어 그 낱말을 통째로 걸어 나갔다. 이제 **안쪽으로 먼저 줄이고(공백·구두점을 떼고) 그 다음에 바깥으로 편다.** 한 텍스트 노드 안이면 줄이기가 서로를 넘지 않도록 `floor`/`ceil` 로 막는다. 늘리기 자체는 그대로 둔다 — 선택이 낱말 가운데를 자른 경우를 살리는 장치다.
- **범위만 고치면 화면의 칠은 그대로다.** `sel.removeAllRanges(); sel.addRange(range)` 로 선택을 다시 얹어 파란 칠이 실제 조회어와 맞게 했다.
- **덤으로 잡은 기존 버그 — 늦게 온 사전 응답이 현재 표제어를 덮어썼다.** 낱말을 잇달아 짚으면 먼저 보낸 조회가 뒤에 도착해 지금 보고 있는 항목을 갈아 치운다(실측: `here` 를 짚었는데 앞서 짚은 `don't` 가 떠 있었다). `showDict` 에 순번(`dictSeq`)을 두고 늦게 온 것은 버린다. 이 수정과 무관한 예전부터의 성질이다.
- 실측(CDP `Input.dispatchMouseEvent` 로 진짜 더블클릭을 쏘았다 — 선택을 손으로 만들면 증상이 안 나온다): 본문 낱말 셋과 경계 사례 다섯(`don't` · `self-aware` · 문장 끝 `here` · 따옴표 안 `Quoted` · `big`) 모두 **선택과 조회어가 한 낱말로 일치**. 하이픈 낱말은 의도대로 통째(`aware` 를 짚어도 `self-aware`)다.

2026-08-15 저녁 — **《SIGNALS》 개조식 판본을 만들었다**(사용자 지시). `D:\ebook\__output__\signals.nominal.parallax`(1,717블록 · 번역 1,715 · 그림 2 · asset 2 · 용어집 84 · page_check 262 · superseded 251), 백업은 `signals.nominal.book.json`. 뉴닉 판본 `signals.parallax` 는 손대지 않았다. **세 번째 개조식 판본이고 절차는 그대로 들어맞았다** — 소스는 `.parallax` 에서 되돌리고, `--force` 대신 `ko`·`ko_raw` 만 비우고, deslop 은 건너뛴다. 지출 합계 **$5.10**(1차 82호출 $3.77 · 2차 31호출 $1.29 · fixend $0.02 · 구간 재번역 $0.02).

- **첫 실행이 4시간 53분 만에 75%에서 죽었다 — `getaddrinfo failed`.** DNS 가 몇 시간 끊긴 것이라 안쪽 재시도(10회/900초)가 예산을 다 쓰고 포기했다. 초반 13청크가 4분이었으니 그 시간의 대부분은 번역이 아니라 단절 대기다. 셸 루프가 30초 쉬는 사이에 작업이 종료돼 **바깥 그물도 못 걸렸다** — 재시도 루프는 프로세스가 살아 있어야 소용이 있다. 손실은 없었다(1,289블록 보존, 재개는 완료분을 공짜로 건너뛴다). 재개 전 `build_system` 과 `translate-system.txt` 를 sha1 로 대조해 일치(`2cb2a6b5…` · 3,377자)를 확인했다.
- **이어받기라 `--fix-endings-only` 를 책 전체에 한 번 더 돌렸다**(3/1,715 누출 · 교정 0). 셋 중 둘(`b1065`·`b1251`)은 직접 인용 안이라 규칙이 지킨 자리인데, **`b1632a` 는 진짜 누출이었다** — 인용이 아닌 서술 문장 하나가 긴 블록 가운데에서 풀렸고 교정자가 두 번 다 손대지 않았다.
- **verify 경고 중 하나는 진짜였다.** `누락 0 · 한글없음 8 · 숫자 20 · 길이이상 4 · 용어집 2` 에서, 길이비율 `b1638` **0.22 는 압축이 아니라 누락**이었다 — 뉴닉 판본(201자)에는 있는 첫 두 문장이 개조식(92자)에는 통째로 없었다. **길이 경고를 「nominal 은 원래 하한에 닿는다」로 뭉뚱그리면 안 된다는 실례다** — 같은 블록을 다른 판본과 대조하면 압축인지 누락인지 바로 갈린다. `--range b1632a-b1638 --force` 로 다섯 블록만 다시 돌려 둘을 함께 고쳤다(199자 · 0.48, 종결 누출 0).
- 나머지 경고는 무해했다. 한글없음 8은 ISBN 한 줄과 영문 참고문헌, 길이이상 3(`b0012`·`b0013`·`b0015`)은 법적 고지 상용구의 개조식 압축, 용어집 2는 `Foundational Marketing Signals` 같은 부 제목이라 「시그널스」로 바꾸면 오히려 틀린다. **숫자 누락 20은 `[^n]` 마커 문제**인데 원문 0 · 뉴닉 77 · 개조식 63 이라 **이번 실행의 회귀가 아니라 기존 성질**이다(여전히 미해결).
- **`ko_raw` 가 0인 것이 정상이다.** deslop 을 안 태우니 「deslop 이전 원본」이 없다 — 기존 개조식 판본 둘도 0이다(파일이 뉴닉보다 작은 까닭이 이것이다). 나머지 표는 뉴닉과 전부 일치했다(블록·asset·용어집·page_check·superseded·asset 바이트).
- **개조식 판본을 처음으로 앱 화면에서 확인했다**(아래 미결 항목 해소). 목차 295항목이 3단계(8·63·224)로 접혀 71줄로 열리고 병합 23, 그림 둘(673×1190 · 85×85) 렌더, 본문은 명사형 종결(`파트너십을 완전히 잃음.` `이 패턴은 반복.`)에 직접 인용은 규칙대로 서술형(`"마진이 맞지 않았습니다."`)이다.

2026-08-15 이어서 — **Markdown 내보내기도 목차와 같은 규칙으로 머리글을 단다**(사용자 지시). 전에는 `MD_HEAD` 로 블록 유형을 그대로 `#`/`##`/`###` 에 대응만 해서, 목차에는 「CHAPTER 1 · The Meaning of Meaning」 한 줄인 것이 md 에서는 `# CHAPTER 1` 다음 `## The Meaning of Meaning` 두 줄로 갈렸다. 규칙을 **`src/shared/headings.ts` 한 곳으로** 뺐다(`MAX_HEADING`·`isNumberedLabel`·`mergesWithNext`·`MERGE_SEP`) — `Doc.outline()` 과 `renderMarkdown()` 이 같은 것을 부른다. 두 벌로 두면 반드시 어긋나고, 이 저장소는 스키마를 이중으로 적어 둔 값을 이미 치르고 있다.

내보내기 쪽만 다른 것 하나: **길이 상한에 걸린 오탐 제목을 목차는 감추지만 내보내기는 문단으로 내린다.** 감추면 글이 사라진다 — 태그만 떼고 글은 남긴다. 인접 판정은 목차가 SQL 의 `rn`, 내보내기가 배열의 이웃(버린 블록은 건너뛴다)으로 서로 다르게 알지만 뜻은 같다. 병합 판정은 **양쪽 모두 원문으로** 한다 — 번역에서는 수사 꼴이 달라진다(`CHAPTER 1` → `제1장`). 합칠 때 잇는 글은 그 언어의 것이라 한글 파일은 `# 제1장 · 의미의 의미` 로 나온다.

실측(책 넷): md 머리글이 목차와 **개수·레벨·글자까지 1:1 일치**(92/68/295/22). 글 손실 0 — 원문·한글 각각 945·1,715블록을 md 안에서 다시 찾아 확인했다. 오탐 제목은 머리글이 아니라 문단으로 들어가 있다.

> 남는 것 둘. ① md 레벨은 **블록 유형 그대로**라 원본이 건너뛴 자리는 md 도 건너뛴다(signals 에 `#` 다음 `###` 가 있다 — 부 밑에 장 없이 절이 바로 오는 자리다). 목차는 스택 깊이로 그려 화면에서는 안 드러난다. 트리 깊이로 다시 매기면 markdown 린트(MD001)는 조용해지지만 같은 종류의 절이 자리에 따라 `##`·`###` 로 갈린다 — 지금은 원본에 충실한 쪽을 골랐다. ② **리더 본문은 아직 그 오탐 블록을 h2 로 그린다**(129자짜리가 굵은 큰 글씨로 문단 한가운데 선다). 목차와 내보내기만 손봤고 본문 조판은 건드리지 않았다.

2026-08-15 이어서 — **목차에 계층을 넣었다**(사용자 지시). 시작은 「목차로 구분된 내용들이 hierarchy 가 없다」였는데, 재 보니 **계층은 이미 데이터에 있었고 화면이 일부러 안 그리고 있었다.** `outline()` 은 진작 `level: 1|2|3` 을 내려보내고 렌더러도 `data-level` 을 찍는데, CSS 가 `.toc a[data-level] { padding-left: .4rem }` 로 전부 왼쪽에 붙였다 — 2026-07-27 의 「들여쓰기·크기 차이 없이 위계는 굵기로」 결정이다. 그 판단은 **제목이 두 단계뿐이던 때**의 것이고, 부 > 장 > 절 세 단계인 책에서는 굵기 둘로 세 단계가 갈리지 않는다.

책 넷을 재서 문제를 둘로 갈랐다(h1/h2/h3): meaning_of_your_life 13/89/0 · mind_is_flat 7/27/36 · signals 11/58/249 · beyond_weird 2/23/0(원래 평평한 에세이 모음이다).

- **수사와 제목이 두 블록으로 갈려 있다** — 「CHAPTER 1」 다음 줄에 「The Meaning of Meaning」. 넷 중 셋에 다 있고, 들여쓰기만 하면 수사가 부모·제목이 자식으로 보여 위계가 한 칸씩 **밀린다.** `Doc.outline()` 이 맞붙은 두 제목을 한 항목으로 합친다(`CHAPTER 1 · The Meaning of Meaning`). 맞붙었는지는 **rn**(버리지 않은 블록 전체에서의 `ROW_NUMBER`)으로 본다 — `ord` 차이로 재면 안 된다, 성기게 매겨져 간격이 일정하지 않다. 클릭 목표와 자리는 앞의 수사 블록을 그대로 써서 착지 위치가 안 변한다. 실측 병합: meaning 9 · mind_is_flat 2 · signals 23 · beyond_weird 0.
  - **번호가 붙은 수사만 합친다**(`LABEL_EN`/`LABEL_KO`, 끝을 `$` 로 물렸다). 「Part I — Foundational Marketing Signals」처럼 이미 한 줄인 것은 걸리면 안 되고, 「INTRODUCTION」·「CONCLUSION」 같은 맨 수사는 **일부러 뺐다** — 뒤따르는 것이 그 부의 제목인지 첫 절인지 데이터로 구분되지 않는다(INTRODUCTION 다음의 「Unheimlich」는 제목이 아니라 첫 절로 보인다). 잘못 합치면 절 하나가 목차에서 사라진다.
- **본문 한 문단이 제목으로 잘못 잡혀 목차에 박혀 있었다.** meaning_of_your_life 의 129자짜리 h2 다. 네 책의 진짜 제목 최장이 95자(signals)라 **120자 상한**으로 거른다. 파일은 그대로 두고 목차에서만 감춘다. 남는 것 하나: 같은 책의 「I first read Zen in the Art of Archery…」(64자)는 길이로 못 거른다 — 진짜 제목과 구분할 안전한 규칙이 없어 그대로 뒀다.

렌더러는 중첩 `ul` 로 그리고 가지마다 접는 손잡이를 둔다. **깊이는 레벨 숫자가 아니라 스택으로 정한다** — h1 다음에 곧바로 h3 가 오는 책에서 레벨을 그대로 들여쓰면 쓰지도 않는 단계만큼 밀린다. 처음에는 깊이 0 만 펼친다(두 단계까지 보인다): signals 는 다 펼치면 318줄인데 **71줄**로 열린다. 스크롤 강조가 접힌 가지 안으로 들어가면 조상을 **펼치기만** 한다 — 지나온 가지를 도로 닫으면 손으로 펼쳐 둔 것까지 쓸려 나가고 스크롤을 되돌릴 때마다 목차가 여닫힌다.

밟은 것 하나: **손잡이 없는 항목에 그 자리를 비우지 않으면 위계가 뒤집힌다.** 「빈 칸만 늘어난다」고 보고 안 비웠더니 손잡이가 있는 부모(43px)가 자식(36px)보다 오른쪽에서 시작했다. `.toc .item > a:first-child` 에 손잡이 폭만큼 margin 을 준다 — 실측 43 / 61 / 80 으로 단조 증가. 착지·강조는 계층 도입 뒤에도 세 책 모두 툴바 아래 8~9px, 강조 일치.

2026-08-15 목차·배치 손질 — 사용자 지시 넷. **③ 은 고칠 것이 없었다: 우하단 진행 막대와 우상단 번역 범위 셀렉트는 2026-08-06 에 이미 지웠고, 사용자가 보고 있던 것은 옛 설치본이었다**(`release/` 의 `app.asar` 가 8/6 21:43, 제거 커밋 `2339615` 가 같은 날 22:02 — 19분 차이다). 같은 날 「우상단 낭독 메뉴」로 한 번 겪은 일이 되풀이됐으니, 화면 이야기가 소스와 어긋나면 **먼저 `release/` 시각과 커밋 시각을 견주어 볼 것.** 그 김에 죽은 `#prog`(스크롤 진행 막대) 를 걷어냈다 — div 와 스크롤마다 width 를 쓰던 한 줄이 남아 있었는데 CSS 규칙이 없어 화면에는 애초에 안 나왔다.

① **목차 클릭이 목표 제목을 툴바 바로 아래에 세운다**(`gotoBlock`). 예전 한 줄(`tops[i] - 80`)이 어긋나던 까닭은 둘이다. 하나는 `.doc` 의 `padding-top: 2.5rem` — `tops[]` 의 0 은 doc 의 위쪽이 아니라 **안여백을 지난** 첫 행의 위쪽인데 `doc.offsetTop` 만 더하고 있었다(`docTop()` 으로 분리). 더 큰 하나는 `tops[]` 가 창 밖 블록에 대해 **추정치**라는 것 — 먼 장으로 뛰면 도착점이 이미 어긋나 있고, 도착한 뒤 `renderWindow → measure → rebuildTops` 가 실측으로 덮어쓰면서 목표가 **스크롤이 끝난 뒤에 또 움직였다.** 이제 뛰고-마운트하고-다시 재기를 자리가 굳을 때까지(최대 5회) 되풀이하고, **목표 행이 DOM 에 올라온 뒤로는 추정치를 아예 안 본다**(`el.getBoundingClientRect()`). `behavior` 는 `auto` 다 — 추정치 위로 부드럽게 미끄러진 뒤 보정하면 두 번 움직이는 것이 그대로 보인다. 실측: 앞·중간·끝 네 항목 모두 툴바 아래 8~9px(목표 8).

② **스크롤에 따라 목차 항목이 강조된다**(`updateTocMark`). 기존 스크롤 rAF 핸들러에 얹었다(리스너를 늘리지 않았다). 강조 배경은 `color-mix(--accent 15%)` — `--mark` 는 패널 바탕(`--bg-soft`)과 너무 붙어 스크롤 중에 어디가 켜졌는지 읽히지 않았다. 밟은 것 둘: **걸린 블록을 `tops[]` 로 역산하면 문서 끝에서 강조가 한 장 뒤처진다**(추정 오차 누적 — 실측에서 0.9 지점만 실패했다). 창 안은 어차피 다 DOM 에 있으니 마운트된 행을 훑어 찾는다. 그리고 **클릭 착지 중에는 스파이를 잠근다**(`landing` 카운터) — 아니면 뛰어넘는 중간 장들을 훑으며 강조가 요동친다(실측: 착지 1.5초 동안 강조는 하나로 고정). 목차가 열려 있고 강조가 패널 밖으로 나갈 때만 패널을 따라 올린다 — 늘 가운데로 끌면 목차를 눈으로 훑는 동안 발밑이 움직인다.

④ **상하 배치(`tb`·`bt`) 제거.** 셀렉트 두 항목만이 아니라 딸린 것을 전부 지웠다: `recomputeEstimates` 의 `stacked` 분기, 상하 전용 손잡이 숨김 두 곳, 읽는 쌍만 남기고 물리던 `updateFocus()`(+ 매 스크롤 호출)와 `.row.dim`/`.row.near` CSS, 상하 그리드·아래칸 여백 규칙. **저장된 옛 값 `"tb"`·`"bt"` 는 `"lr"` 로 옮긴다** — 고르는 손잡이가 없는데 저장값만 살아 있으면 셀렉트가 빈 값이 되고 손잡이 없는 화면에 갇힌다(`"viewport"` → `"chapter"` 이관과 같은 처리). 되살리려면 git 히스토리.

검증은 `the_meaning_of_your_life_compressed.parallax`(945블록·목차 102항목)로 했다 — **전량 번역이 끝난 문서라 열어도 API 호출이 없다**(`pending 0` 을 먼저 확인했고, 관례대로 드라이버에서 `translate.pause(true)` 도 걸었다). 요령 하나: **CDP 드라이버에서 `requestAnimationFrame` 대기를 쓰지 말 것** — 창이 가려지면 rAF 가 throttle 되어 드라이버가 몇 분씩 멈춘다(`setTimeout` 으로 바꿔 해결). 같은 이유로 `gotoBlock` 도 rAF 를 안 기다린다 — `measure()` 가 `getBoundingClientRect` 로 레이아웃을 강제하므로 필요가 없다. `better-sqlite3` 는 Electron ABI 로 빌드돼 있어 맨 `node` 로는 못 연다 — `ELECTRON_RUN_AS_NODE=1 ./node_modules/electron/dist/electron.exe -e …`. 드라이버는 세션 스크래치의 `drive.mjs`/`drive2.mjs`.

2026-08-06 툴바 손질 — 사용자 지시 다섯. ① 앱 메뉴 `Edit` 를 「편집」으로(`role: "editMenu"` 는 라벨이 영어라 항목별 role 에 우리말 라벨을 달았다). ② 툴바 순서를 목차·조판·색상으로 바꾸고 전부 알약형(rounded) 웹 버튼으로 — 네이티브 위젯은 테마를 따라오지 못한다. 테마 셀렉트는 「색상」 토글 버튼이 됐다(둘뿐이라 셀렉트가 과했다). ③ **「따라가기」(viewport) 번역 모드 제거** — `Mode` 는 `"chapter" | "all"` 둘. 「현재 장」은 이제 실제로 구현됐다: 눈이 머무는 장(h1·h2 경계) 전체를 요청하고, 보이는 블록만 우선순위 0. 「전체」에서는 보이는 블록 승격만 한다. 옛 저장값 `"viewport"` 는 읽는 두 곳(main `attach`, 렌더러 init)에서 `"chapter"` 로 옮긴다. main 이 스케줄러를 만들 때 저장된 모드를 심는다 — 전에는 렌더러가 바꿀 때만 보내서 상태 표시줄이 기본값을 보였다. ④ **좌우 분할을 저장하지 않는다** — 드래그로 틀어진 46.9% 가 실행마다 되살아나 시작 화면이 늘 비뚤었다. 시작은 항상 50, 조절은 세션 안에서만 산다. ⑤ 우상단 낭독 메뉴는 코드에 이미 없다 — 옛 빌드를 보고 있던 것이니 재빌드로 해결. CDP 실측: 초기 테마 복원(night)·버튼 radius 999px·토글 동작 확인.

같은 날 더 이어서 — **앱을 .parallax 전용 리더로 좁혔다**(사용자 지시). 파일 메뉴에서 「Anthropic API 키…」(Ctrl+K)·「PDF 열 때 페이지 검증 묻기」·「페이지 검증 리포트」를 뺐고, main 의 PDF·md·txt 가져오기 경로(`planPagecheck`·`showPagecheckReport`·`openPath` 확장자 분기)를 삭제 — 열기 대화상자 필터도 `.parallax` 하나다. 다른 확장자는 「스킬로 변환한 뒤 여세요」 오류로 막는다. 되살리려면 git 히스토리. 키 등록은 키 없이 열었을 때 뜨는 알림의 「키 등록…」 버튼으로는 여전히 가능하다(다이얼로그 코드는 남아 있다). importers 모듈은 `Cancelled`·`cancelImport` 참조 때문에 남겨 뒀다. CDP 로 signals.parallax 열림 확인.

같은 날 이어서 — **번역 UI 를 마저 걷어냈다**(사용자 지시). 툴바 우상단 번역 범위 셀렉트와 우하단 진행 위젯(`.status` — 모드·진행률 막대·블록 수·지출)을 제거. 이제 **모드는 「현재 장」 고정**이고 저장된 옛 모드(`"all"`·`"viewport"`)는 읽지 않는다 — 고르는 손잡이가 없는데 전량 번역이 몰래 돌면 문서를 열 때마다 돈이 새기 때문(전량 번역은 스킬 CLI 몫). 스케줄러의 stats 집계·`"all"` 경로는 남아 있어 되살리기 쉽다. 시작 화면도 설명문·형식 목록을 다 빼고 「문서 열기」 버튼 하나만 남겼다. CDP 실측으로 셋 다 확인.

2026-08-04 세션 — **단락이 왜 갈려 있었는지 찾아 셋을 고쳤다.** 사용자가 「단락 경계가 무너졌다」고 해서 실제로 세어 봤다. `the_mind_is_flat.noads.parallax` 의 문장 중간 끊김은 **52곳**이었다 — 그전에 「0」이라고 보고했던 것은 틀렸다(그 검사는 같은 쪽·같은 유형만 봤다). 처음 센 153곳 중 95곳은 색인이라 오탐이었다(색인 항목은 원래 소문자로 시작하고 마침표가 없다).

갈래는 셋이고 전부 **이어붙이기가 이웃을 잘못 보고 있던 문제**였다.

- **버린 블록이 이음매를 끊었다**(본문 6곳). `drop` 은 지우지 않고 표시만 하는데(ID 불변) 이어붙이기가 바로 앞 블록을 `prev` 로 삼았다. 문장 한가운데에 전자책 뷰어의 `Page 60 of 252 · 3%` 가 끼면 두 반쪽이 영영 이웃이 되지 못한다. 이제 읽지 않는 블록은 이음매를 끊지 않는다(`extract.stitch_blocks` · `pagecheck.stitch_pages` · `repair.find_seams` 셋 다).
- **주석 유형이 항목 안에서 흔들렸다**(46곳). 한 주석이 `footnote → p → quote` 로 번갈아 분류되는데 유형이 다르면 잇지 않는 규칙 탓에 주석부가 통째로 갈렸다. `BODY` 에 `footnote` 를 넣고, 대신 번호로 시작하는 곳(`starts_note`)은 새 항목으로 보아 지킨다. 합칠 때 남는 유형은 `merged_type` 이 정한다 — 캡션과 주석은 자기 자리를 지킨다(본문으로 바꾸면 흐름 한가운데로 들어온다).
- **`is_page_progress` 가 진짜 내용을 삼켰다.** 예전 규칙은 「글자가 없고 숫자가 있으면 참」이라, 분철된 인용의 꼬리(`Journal of Nervous and Mental Dis-` 뒤의 `ease, 131(2): 135-48.`, `46.`, `80.`)를 뷰어 부속물로 지웠다. 이제 진행 표시라는 증거(`%`·page/location·`26 of 201` 꼴)를 요구하고, 판독이 뭉갠 형태(`Page 60T 252 + 3%`, `0f`)까지 잡는다. 맨 숫자 조각은 `is_bare_label` 로 갈라 냈고, **앞 블록이 하이픈으로 끊겼으면 무엇도 버리지 않는다**(`pagecheck.continues_hyphen`). `looks_garbled` 가 URL 주석을 통째로 먹던 것도 막았다.

결과(실측): 문장 중간 끊김 **52 → 10**, 그중 본문은 **6 → 1**. 남은 하나는 `understand-` 뒤 낱말 꼬리가 판독에서 아예 사라진 자리라 잇지 않는 것이 맞다. 기존 파일은 `repair.py --apply --stitch-translated` 로 45쌍을 잇고(1492 → 1447 블록) 대기 44개를 앱에서 채웠다($0.18). 원본은 `.bak`.

**리더가 버린 블록을 그리던 것도 고쳤다.** `DROPPED` 184개가 본문 사이에 흩뿌려져 있었다 — 쪽 대조에 쓸모가 있으리라 보고 원문 칸에 남겨 뒀는데, 실제로는 읽는 흐름만 끊었다. `db.ts` 의 `count`·`range`·`outline`·`meta` 가 전부 거른다(파일에는 그대로 남는다). 진행률 분모도 함께 고쳤다 — 버린 것을 세면 전부 번역해도 100% 에 닿지 않는다. 1492 → 1308 로 보이고, 문서를 훑어도 부속물이 없다(CDP 확인).

2026-08-03~04 세션 — **낭독(TTS)을 넣기 시작했다.** 설치가 깨지던 것부터 고쳤다: `npm start` 가 `'tsc' is not recognized` 로 죽었는데 `node_modules` 자체가 비어 있었다. better-sqlite3 에 Node 24 용 미리 빌드본이 없어 소스 컴파일로 넘어가고, node-gyp 10 이 Visual Studio Build Tools 2026(v18)을 못 알아봐 npm 이 전체를 되돌린 것이다. `.npmrc` 로 네이티브 모듈을 Electron ABI 로 받게 하고(`runtime=electron`), `scripts/rebuild-native.mjs` 를 postinstall 에 걸어 「미리 빌드본 먼저, 안 되면 컴파일, 그래도 안 되면 설치는 성공시킨다」로 만들었다. 깨끗한 재설치로 확인. 그 위에 Supertonic 3(ONNX, `onnxruntime-node`)을 붙였다 — 자세한 것은 아래 절.

2026-07-30 세션 — **07-28 수정분의 화면 재확인을 마쳤다.** `pagecheck_test_20p.pdf` 를 재생성해(원본 앞 20쪽, PyMuPDF — 지난 세션 것은 지워져 있었다) 앱으로 열고 「전 쪽 재판독」 전 구간을 실행($0.15). 확인된 것: ① 쪽별 진행 표시 `페이지 검증 중 · 7 / 20쪽 · 취소` 가 화면 아래 가운데에 뜬다. ② `superseded` 이관 — 시험 문서에 19행, 밀려난 원 레이어(`"Ihe Meaning or Your I ife"` 따위)가 그대로 보관됐다. ③ `page_check` 이관 21행(요약 `page=0` + 20쪽), **파일 → 페이지 검증 리포트** 가 임시 md 로 재조립해 연다(요약·쪽별 표 온전). 검증 방법: `--inspect=9229` 로 메인에 붙어 `dialog.showMessageBox` 를 패치해 자동 응답, 렌더러 CDP 로 관찰 — 스크립트는 세션 스크래치의 `drive-pagecheck.mjs`/`watch-pagecheck.mjs`. 요령 둘: CDP `Page.captureScreenshot` 은 창이 가려지면 응답하지 않는다(메인의 `webContents.capturePage()` 를 쓸 것), 번역 자동 시작은 렌더러에서 `setInterval` 로 `translate.pause(true)` 를 계속 눌러 막았다(지출 $0.00 확인). 같은 날 electron-builder 로 Windows 설치본(`Parallax Setup 0.1.0.exe`)을 처음 빌드했다 — 출력은 `release/`(`.gitignore`), 산출물은 언제든 재생성되므로 보관하지 않는다.

2026-08-06 세션 — **《The Mind Is Flat》 그림 판본의 전량 번역을 마쳤다.** 오전 세션이 그림 복구(figure 45 · asset 45) → 용어집 초안 → 뉴닉체 번역을 걸었다가 89/99 청크에서 강제 종료됐고, 이어받은 세션이 작업 디렉터리(세션 스크래치 `mindflat/work`)의 캐시를 그대로 살려 나머지 80블록만 재실행했다(translate 는 빈 블록만 묶으므로 완료분은 공짜). deslop(약, 39블록) → verify(빠짐 0 · 지적은 URL 각주·색인 구간이라 전부 무해, 각주 번호 누락 16건만 리더에서 확인할 것) → export 로 **`D:\ebook\__output__\the_mind_is_flat.parallax`(875블록 · 번역 816 · 그림 43 · asset 43 · page_check 245행)가 새 정본**이다. 전체 book.json 도 `the_mind_is_flat.book.json` 으로 옆에 백업했다(md 스냅샷보다 무손실). 요령: 임시 폴더에서 CLI 를 돌리면 `.env` 탐색 경로에 저장소가 없다 — `PKT_ENV_FILE=D:\projects\parallax\.env` 로 지정할 것. 중단 시 앱이 물고 있던 0바이트 `-wal`/`-shm` 잔재도 지웠다.

> **`the_mind_is_flat.noads.parallax`(구 번역 완본)와 그 md 스냅샷은 이 PC 에 없다 — 지워진 것이 아니라 애초에 다른 PC 소재다.** 이 PC(사용자 `john.boo`, 26NB9001)의 전 세션 기록·디스크·휴지통 어디에도 그 이름이 등장하지 않고, 낭독 자산 정션이 `C:\Users\johnb\AppData\Roaming\parallax\tts` 에 걸려 있던 것이 증거다 — 8/2~04 노트북 작업은 `johnb` PC 에서 이뤄졌다. 두 PC 의 `D:\ebook\__output__` 는 이름만 같고 내용이 갈라져 있으니 주의. 구판(그림 없음)은 어차피 신판이 대체한다.

2026-08-06 이어서 — **세 번째 책 《SIGNALS》(Angela Troccoli, 마케팅 실용서, 261쪽 순수 스캔본)를 전 공정으로 변환**해 `D:\ebook\__output__\signals.parallax`(1,717블록 · 번역 1,715 · 그림 2 · page_check 262행)를 만들었다. 백업은 `signals.book.json`. 이 실전에서 스킬을 둘 고쳤다:
- **빈 쪽 재구성 버그** — `rebuild_page` 가 삽입 위치·ID 를 밀려나는 기존 블록에서 얻어서, 글자 레이어가 아예 없는 쪽(순수 스캔본은 전부)에 블록을 못 넣었다(261쪽 재구성 0 으로 발각). 빈 쪽 경로 추가: 위치는 쪽 번호 순, ID 는 책 전체 최대 번호를 이어 새로 딴다. 비전·tesseract 경로에도 `page=` 를 같이 넘겼다.
- **비용 로깅** — `_llm.py` 가 모든 응답 usage 를 집계해 실행 끝에 stderr 로 `anthropic 지출(이번 실행): N호출 · in/out tok ≈ $X.XX` 한 줄(단가표 하드코딩, 모르는 모델은 금액 제외 표기). `pagecheck.py` 는 Datalab 응답 `total_cost` 를 모아 콘솔+리포트 md 에 남긴다(캐시 적중 0원). 실측: signals deslop 191호출 ≈ $5.57.
verify 지적 처리: 한글 없음 7건=영문 참고문헌(정상), 용어집 미준수 4건=CMO·AR 등 약어 일관 사용이라 수용, b1095 는 다음 블록 목록을 미리 끌어와 중복 — 원문 경계로 잘라냈다. 각주 번호 누락 25건은 리더에서 확인할 것. 목차는 사후 손질로 부(h1) > 장(h2) > 절(h3) 세 단계로 정리했다(370블록: 번호 소제목 78+표제지 2 는 본문으로, 장 소제목 249 는 h3 로). OCR 이 "Part IFoundational…"처럼 수사와 제목을 붙여 놓으면 h1 승격 규칙이 못 잡는다 — 다음 책에서도 나오면 normalize_headings 에 분리 규칙을 넣을 것. 요령: 백그라운드 작업을 `| tail` 로 받으면 오류 로그가 잘린다 — 전체 로그를 남길 것(실패 9쪽의 원인 파악이 이것 때문에 한 번 헛돌았다).

2026-08-05 두 번째 세션 — **그림이 처음으로 파이프라인을 통과한다(스키마 v2).** Datalab chunks 가 잘라 보내는 그림(base64 JPG + 모델이 쓴 alt 설명 + bbox)을 지금까지 버리고 있었다 — 이제 `harvest_images()` 가 **모든 청크**의 `images` 필드를 줍는다(Picture 유형만 보면 안 된다: 실측에서 표지 그림이 SectionHeader 안에 `<img>` 로 내장되어 왔다). 흐름: pagecheck 가 `book["assets"]` + `figure` 블록(`src`=asset id, NO_TRANSLATE, 한 변 64px 미만은 장식으로 버림) → `export.py`/앱 `Doc.create` 가 `asset` 테이블(id·mime·w·h·alt·data BLOB)로 이관 → 리더가 data URI 로 그린다(`asset:get` IPC, CSP 의 `img-src data:` 는 이미 허용). figure 행은 원문·번역 **양쪽 칸에 같은 그림을 하나씩** 둔다(사용자 지정: 한쪽 대역만 읽는 눈이 그림을 놓치지 않게) — 칸 폭의 60% 상한으로 축소만 하고(`max-width: 60%`, 확대·잘림 없음) 데이터는 한 번만 받아 두 `<img>` 가 나눠 쓴다. `max-height` 는 두면 안 된다 — `<img width>` 속성이나 max 제약 둘이 겹치면 비율이 깨지는 것을 실측으로 확인했고, `width:auto; height:auto; max-width` 하나만 남겼다. 가상 스크롤 높이는 상수 추정이 아니라 w/h 실값으로 계산한다. `schema_version` 1→2(스키마 세 벌 + spec §2.2 모두 갱신), `parallax_import.py` 왕복도 asset 을 실어 **무손실 확인**. 검증: 20쪽 시험 PDF 1~3쪽 실 호출(표지 그림 1417×1274 + 출판사 로고 65×90 수확) → export → 앱을 CDP 로 열어 화면 실측 — 비율 유지(761×685) 확인. 이때 밟은 것 둘: ① `<img width=…>` 속성이 살아 있으면 CSS `max-height` 가 비율을 깨고 옆으로 퍼진다 — `width:auto; height:auto` 로 풀어야 max 제약 둘이 비율을 지키며 줄인다. ② CDP 드라이브 스크립트가 번역 자동 시작을 안 막아 시험 문서 번역이 실제로 돌았다($0.1~0.2 지출) — 다음부터 드라이버에 `translate.pause(true)` 필수(2026-07-30 세션의 요령을 그대로). 렌더러의 번역 요청도 NO_TRANSLATE/DROPPED 블록을 거르게 고쳤다(전부터 새던 구멍). 정본 zip 재포장 완료. 기존 945블록 작업 파일에 그림을 소급하려면 그림 있는 쪽만 datalab 재판독하는 스크립트가 필요하다(미착수).

같은 날 이어서 — **《The Mind Is Flat》(244쪽, `D:\ebook\__output__\the_mind_is_flat.noads.pdf`)을 전 쪽 datalab 재판독으로 변환**해 `the_mind_is_flat.parallax`(875블록 · 그림 43 · figcaption 39 · 번역 0, 같은 폴더)를 만들었다. 이 실전에서 스킬 버그 둘을 잡았다(`a876256`): ① **429 요율 제한** — 8워커 동시 요청에 219/244쪽이 「판독 실패」로 남았다. 실패는 캐시에 안 남으므로 재실행하면 성공분은 공짜다. 지금은 POST·폴링 모두 Retry-After 백오프로 재시도한다. ② **Datalab 파일명은 내용 해시가 아니다** — 레이아웃 기반이라 서로 다른 그림 16장이 같은 이름으로 와 asset 이 덮어써졌다(그림 43개가 asset 11개로 뭉개진 걸로 발각). asset id 는 sha1(내용) 16자리로 우리가 만든다.

2026-08-05 세션 — **페이지 검증에 Datalab API 엔진 추가.** 처음에 로컬 Chandra(`--engine chandra`, 공개 웨이트 + GPU)로 넣었다가 같은 날 사용자의 정정으로 **[Datalab API](https://www.datalab.to)**(Chandra 를 만든 곳의 호스팅 서비스, 공개 웨이트보다 정확한 개선판이 돌아간다 — README 벤치마크 86.7% vs 85.8%)로 교체했다. `pagecheck.py --engine datalab`: 쪽 PNG 를 `POST /api/v1/convert`(multipart, `X-API-Key`)로 보내고 `request_check_url` 을 2초 간격 폴링(5분 한도), **`output_format=chunks`** 를 받아 **항상 재구성**(`from_ocr|rebuilt`, 밀려난 레이어는 `superseded`) — layer 신뢰 모드는 출력이 우리 블록 ID 를 모르는 통짜 판독이라 성립하지 않는다. 처음에 markdown 으로 받았더니 표지 그림 설명문이 h1·본문으로 새어 들어왔다(유형이 없어 못 거른다) — chunks 는 블록마다 유형이 붙어 와서 `chunk_items()` 가 `PageHeader/PageFooter/PageNumber/Picture/Figure` 를 통째로 버리고 `SectionHeader`→h(태그 레벨), `Caption`→figcaption, `Footnote`→footnote, `Table`→table_raw(태그 보존), 나머지→p 로 대응만 한다. 모드는 `--datalab-mode fast|balanced|accurate`(기본 accurate), HTTP 는 anthropic 이 이미 끌어오는 httpx 라 **새 의존성 없음**. 키는 `DATALAB_API_KEY` — 환경변수 → `.env`(cwd → 스킬 폴더 → 그 부모) 순이고, 자리표시자(`your-datalab-api-key-here`)는 없는 것으로 친다. 루트에 `.env`(자리표시자, `.gitignore` 에 추가)와 `.env.example`(커밋됨)을 뒀다. 앱은 다이얼로그에 「전 쪽 재판독 (Datalab API)」 버튼을 **키가 읽힐 때만** 넣고(`readDatalabKey()` — 앱이 직접 읽어 자식 환경변수로 넘긴다, 패키징되면 cwd 가 달라서), Anthropic 키가 없어도 Datalab 키만 있으면 검증이 돈다. 사용자가 실키를 `.env` 에 넣어 **실 호출까지 검증했다** — 20쪽 시험 PDF 로 1~3쪽 재판독(재구성 3, 페이지 병합 5), 표지 그림 설명·러닝 헤더가 유형 단계에서 걸러지고 실측 비용 accurate 쪽당 1¢(응답 `total_cost`). zip 재포장하면서 `{references,scripts,templates}` 가짜 디렉터리가 풀린 사본에 남아 있던 것도 지웠다.

2026-08-01 세션 — **UI 서체를 Adobe Clean 으로 고정.** `--font-ui` 를 `"Adobe Clean"(로컬) → "adobe-clean"(웹폰트) → SUIT…` 순으로 바꾸고, 앱 시작 시 typekit 킷(`use.typekit.net/pps7abe.css` — helpx.adobe.com 이 쓰는 공개 킷, adobe-clean 300/400/700/800/900)을 항상 로드한다. CSP 의 style-src·font-src 에 `use.typekit.net` 추가, preconnect 도. 라틴 전용 서체라 UI 의 한글은 여전히 SUIT 이하가 받는다. CDP 로 실행 확인 — 로컬 미설치 상태에서 adobe-clean 400/700 로드, `.bar` 계산 서체 일치.

2026-07-28 두 번째 세션에서 들어간 것:

- **pagecheck 통합을 처음으로 실제로 태웠고, 성공했다.** 20쪽 발췌 PDF(`pagecheck_test_20p.pdf`, 원본 앞 20쪽)로 전 쪽 재판독 전 구간을 검증 — 전 블록에 `from_ocr|rebuilt`(일부 `stitched`)가 붙었고 임시 폴더도 통째로 지워졌다. 다이얼로그에 쪽 범위 입력이 없으므로 비용을 묶은 시험은 발췌 PDF 를 만들어 여는 방식이 맞다.
- **진행 표시가 침묵하던 원인 둘을 고쳤다.** ① 파이프에 물리면 파이썬 stdout 은 블록 버퍼링이라 flush 없는 `print` 가 끝까지 안 나온다 — 앱이 파이썬을 `-u` 로 띄운다(`runPy`). ② pagecheck 진행이 20쪽마다 한 번이라 20쪽 시험에서는 끝나는 순간 한 번 나오고 `done` 이 곧바로 감췄다 — 쪽마다·완료 순서(`as_completed`)로 바꿨다. 2026-07-30 화면 재확인 완료(아래 참조).
- **앱이 `superseded` 를 버리던 것을 고쳤다.** 재판독이 밀어낸 원 텍스트 레이어를 CLI `export.py` 는 보관하는데 앱 `Doc.create` 경로에는 이관이 없었다. `readExtraction` → `Doc.create` 로 같은 형식(page 정수, payload JSON)으로 옮긴다.
- **pagecheck 판정을 문서에 남기고 앱 메뉴로 되살린다.** 리포트 md 는 임시 폴더와 함께 지워지므로, `pagecheck.py` 가 판정을 `book["page_check"]` 로 내보내고 CLI `export.py` 와 앱 `Doc.create` 가 `page_check` 테이블로 옮긴다. 스키마 세 벌은 그대로 — 요약은 `page=0` 행, 사유·메모는 notes 로 합친다(규약은 `spec.md` §2.2). `parallax_import.py` 는 역방향을 복원해 왕복 무손실(스모크 테스트로 확인). 앱은 **파일 → 페이지 검증 리포트** 에서 임시 md 로 재조립해 기본 편집기로 연다.
- **목차가 본문 조판을 따른다** — 원문 서체·줄간격에 크기는 본문의 0.88배. 항목이 본문 제목 그 자체라 UI 크기 고정(`--ui-size`)의 예외로 뒀다. 같은 크기로 하면 bold 장 제목이 본문보다 커 보여서 한 단계 내렸다(Electron 으로 계산값을 실측해 확인 — 스크립트는 세션 스크래치의 `measure-toc.js`). 덮어 뜨는 패널이라 크기가 바뀌어도 툴바·본문 배치는 요동치지 않는다.

2026-07-28 첫 세션에서 들어간 것:

- **앱이 pagecheck 를 부른다(E5).** PDF 를 열면 추출이 끝난 뒤 「건너뛰기 / 구조만 대조 / 전 쪽 재판독」을 묻는다. 쪽수와 예상 비용을 보여 주려면 추출 뒤여야 한다. 자세한 것은 `USAGE.md`.
  - **묻지 않고 돌리는 일은 없다** — 쪽마다 돈이 나간다. 키가 없으면 조용히 건너뛴다. 「다음부터 묻지 말고 건너뛰기」는 *건너뛰기* 로만 굳는다(설정 `pagecheck: "off"`, 파일 메뉴에서 되돌린다). 매번 돈이 나가는 쪽이 기본값이 될 수 있는 경로는 두지 않았다.
  - 어느 모드를 권할지는 `meta.ocr_layer` 로 정하되 **고르는 것은 감추지 않는다.** 잘못 고르면 되돌릴 수 없고(재판독은 원문의 고어 철자를 현대화한다), 판정은 어차피 추정이다.
  - 중간에 취소하거나 검증이 실패해도 문서는 열린다. `pagecheck.py` 가 다 끝난 뒤에야 `book.json` 을 덮어쓰므로 남는 것은 추출 직후 상태다.
  - 추출·검증 산출물은 임시 폴더 **하나**에 모아 통째로 지운다. pagecheck 는 `book.json` 옆에 쪽 이미지(수백 MB)·캐시·리포트를 만든다.
  - pagecheck 가 남기는 `from_ocr`·`rebuilt`·`stitched`·`dropped` 를 앱 플래그로 옮긴다. 예전에는 `translate` 만 봤다.
- 툴바에 `조판` 패널. 컨트롤 일곱 개가 1400px 에서 가로로 넘치던 것을 접어 넣었다. 바에 남은 것은 목차·조판·번역 범위·테마뿐이고 420px 까지 넘치지 않는다. Esc 는 위에 떠 있는 것부터 하나씩 닫는다(사전 → 조판 → 목차).
- 화면 아래 가운데에 여는 중 표시(`.importing`)와 취소 버튼. 검증은 몇 분씩 가므로 진행이 보이지 않으면 멈춘 것과 구분되지 않는다. 왼쪽은 목차가, 오른쪽은 번역 상태가 이미 쓰고 있어 가운데다.
- **내보내기도 제목을 원문으로 고정.** 파일 이름·HTML `<title>`·본문 제목·목차가 전부 `title`/`src` 를 쓴다(리더와 같은 규칙). 번역 제목은 버리지 않고 부제 한 줄로 남긴다.

2026-07-27 세션에서 들어간 것:

- 목차 항목을 눌러도 목차가 닫히지 않는다(≥900px). 좁은 화면은 겹쳐 뜨므로 그대로 닫는다. 고른 항목에 `aria-current` 표시.
- 조판 컨트롤 정리(아래 「조판」 참조) — 글꼴 크기 10~24pt, 줄간격 5배수, 단락간격 3단계.
- 앱 메뉴 **파일 → Anthropic API 키…**(Ctrl+K). 저장/지우기/상태. 키 값은 renderer로 내려가지 않는다 — 있는지·환경변수인지만 문구로 보여준다.
- 원문 단락 왼쪽 세로줄 제거. PDF 문서는 전 블록에 `FROM_OCR`이 붙어 늘 켜져 있었고, 항상 켜진 표시는 정보가 아니다.
- 툴바를 창 상단 전폭 붙박이로(`position: fixed`). 목차 개폐·글꼴 크기·스크롤과 무관하게 59px 고정. `measureBar()` 및 호출부 제거.
- 툴바의 `열기` 버튼 제거 — 문서 열기는 앱 메뉴(Ctrl+O)로 일원화. 빈 화면의 버튼은 남겼다.
- 목차·책 제목을 **원문 고정**(`db.ts`의 `coalesce(ko, src)` → `src`, `app.js`의 `title_ko || title` → `title`).
- 창 제목을 항상 `Parallax`로. `app.setName` + Windows `setAppUserModelId`.
- 가상 스크롤 높이 추정치를 조판에 연동(아래 참조).
- **PDF 열기가 Windows에서 처음으로 동작한다.** `importers/index.ts`가 `process.env.HOME`(Windows에는 없다)을 보던 것을 `os.homedir()`로, 파이썬을 `"python3"`로 고정해 부르던 것을 `findPython()`으로 바꿨다. Windows의 `python3.exe`는 대개 Microsoft Store로 보내는 0바이트 스텁이라, 후보를 실제로 실행해 보고 고른다. 임시 산출물도 원본 옆이 아니라 임시 폴더에 쓰고 지운다(예전에는 `blocks.txt`와 `*.parallax-extract.json`이 문서 폴더에 남았다).

  스킬이 있어야 동작한다. `findSidecar()`가 보는 곳은 `PARALLAX_SKILL_DIR` → `~/.claude/skills/pdf2parallax` → `<cwd>/pdf2parallax` → `<cwd>/../pdf2parallax` 순이다. 지금은 저장소 안에 `pdf2parallax/`로 풀어 뒀다(정본은 zip이고 이 사본은 `.gitignore`에 있다).

### 낭독 — 제거됨 (2026-08-06)

2026-08-03~04 에 Supertonic 3(ONNX)로 넣어 선택 낭독·이어 읽기·낱말 하이라이트·mp3 내보내기까지 동작했으나, **2026-08-06 사용자 지시로 전부 걷어냈다**(위 앱 상태의 인용 참조 — 지운 목록이 거기 있다). 되살릴 일이 생기면 git 히스토리에서 `spec-tts.md`·`src/main/tts/`·`vendor/supertonic-nodejs/` 를 꺼내면 된다. 그때 다시 필요한 실측 요지만 남긴다: 자산은 `Supertone/supertonic-3` @ `3cadd1ee`(4모델 383MB), 보이스 F1 이 한국어·영어 둘 다 냈고(실시간의 0.22~0.26배, steps=8), `duration_predictor` 출력이 `dims: [1]` 하나뿐이라 토큰별 정렬은 불가능했다. 합성은 utilityProcess 에 격리했었다(모델이 RSS 424MB, ONNX 크래시가 프로세스째 간다).

부산물로 남는 것: 사전이 더블클릭으로 열리는 것은 낭독이 선택을 가져가면서 옮긴 결과인데, 그 자체로 더 나은 조작이라 유지한다. 사용자 settings.json 에 `tts` 키가 남아 있을 수 있으나 아무도 읽지 않는다.

> **저장 대화상자를 가로채는 법**(낭독 검증에서 나온 요령이지만 범용이다). main 의 전역 컨텍스트에는 `require` 도 dynamic import 도 없어서(`process.mainModule` 은 undefined) CDP 에서 곧바로 `dialog` 를 못 바꾼다. `Debugger.setBreakpointByUrl` 로 핸들러에 멈춘 뒤 `Debugger.evaluateOnCallFrame` 로 **`electron_1.dialog`**(tsc 가 만든 이름이다)를 갈아 끼우고 재개하면 된다. 렌더러 CDP 로는 모듈 스코프 변수를 못 읽으니 확인은 DOM 으로 할 것.

### 배치 — 둘 중 하나

조판 패널의 `배치` 로 고른다. `lr` 원문 왼쪽(기본) · `rl` 번역 왼쪽. **상하(`tb`·`bt`)는 2026-08-15 에 걷어냈다**(사용자 지시) — 함께 지운 것은 아래 그 날짜 항목에 있다.

**자리를 바꾸는 데 `order` 를 쓰고 그리드 정의는 건드리지 않는다.** 그래야 `--split`(과 그것을 끄는 손잡이)이 늘 「왼쪽 칸의 폭」이라는 한 가지 뜻으로 남는다 — 열 정의를 뒤집으면 손잡이를 왼쪽으로 끌 때 오른쪽이 넓어진다.

### 조판 — 왜 이렇게 정했나

**축은 세 개고 서로 비례한다.**

| 컨트롤 | 값 | CSS |
|---|---|---|
| 글꼴 크기 | 10~24pt 연속 | `--size` |
| 줄간격 | 1 · 1.2 · 1.5 · 1.8 · 2 (배수) | `--leading` |
| 단락간격 | 없음 · 반 줄 · 한 줄 | `--para-k` |

단락 간격은 **줄 높이(= 크기 × 줄간격)의 배수**다: `padding-top: calc(var(--leading) * var(--para-k) * 1em)`. `em`(크기)만 쓰면 줄간격을 넓혔을 때 단락 사이가 상대적으로 좁아 보여 단락 경계가 흐려진다. 줄 높이 기준이면 두 축 모두에 비례하고, 여백이 줄 높이의 정수배라 좌우 대역의 행이 같은 그리드에 남는다.

**블록 사이 여백은 `.cell` 의 padding 이 아니라 margin 이다.** 짝 강조 상자가 `.cell` 배경이라, padding 이면 단락 간격(줄 높이 한 개 분)까지 상자 안에 들어와 강조가 글보다 한 줄 위에서 시작한다. 여백 값은 `--blockgap`/`--blockgap-b` 로 정하고 붙이는 것은 `.cell` 의 margin 이다. 상자의 안여백은 `--hl-x`(1.5rem, 세로선과 글 사이) · `--hl-y`(0.7rem)이고, 세로 방향은 같은 크기의 **음수 margin 으로 되돌려** 상자를 키워도 줄 자리가 밀리지 않는다(강조는 한 번에 하나뿐이라 이웃과 겹쳐도 보이지 않는다). 세로선 자리는 `margin-left: -.55rem` 그대로다 — 더 왼쪽으로 빼면 본문 여백(`--pad`)을 넘어 창 밖으로 나간다.

**여백을 주면 첫 줄 들여쓰기는 자동으로 뺀다**(`--indent`를 0으로). 단락을 나누는 장치는 하나면 되고 둘을 겹치면 단락 머리가 두 번 표시된다.

**툴바·목차·사전 글자는 본문을 따라가지 않는다.** `--ui-size: 12px` 고정(윈도우 기본 Segoe UI 9pt). 예전에는 본문의 0.8배로 11~19px를 오갔는데, 본문을 키울 때마다 창 전체가 요동쳤다.

**제목은 셋 다 같은 모양이다** — 본문의 1.2배·bold·들여쓰기 없음·왼쪽 정렬. 위계는 크기가 아니라 앞의 여백(장 3.2rem / 절 2.2rem / 섹션 1.4rem)이 만든다. **본문 글자 크기는 요소 종류와 무관하게 하나다** — 각주·도판 설명이 0.84em으로 작던 것을 없앴고 구분은 색과 여백으로만 한다.

저장하는 값은 `sizePt`(pt) · `leadStep`·`paraStep`(단계 번호)다. 단계표를 바꿔도 예전 설정이 엉뚱한 값으로 되살아나지 않는다.

### 가상 스크롤 높이 추정 — 알아둘 것

전체 문서 높이는 `index[]`의 블록별 높이 합이다. 조판이 바뀌면 `invalidateHeights()`가 캐시를 버리는데, **실측은 마운트된 30~40행만** 가능하다(나머지는 DOM에 없다). 예전에는 나머지를 타입별 상수로 되돌려서 줄간격을 2배로 늘려도 총 높이가 그대로였다.

지금은 `recomputeEstimates()`가 조판에서 되짚는다:

```
본문 높이 ∝ size² × leading / 칸폭      (줄 수 ∝ size/폭, 줄 높이 ∝ size×leading)
제목 높이 ∝ size                        (line-height 1.35 고정이라 leading을 안 탄다)
```

기준 상수는 191쪽 문서에서 p 487개·h2 40개·h3 20개를 실측해 역산했다(`p: 174`, `h2: 65`, `h3: 42`). **평균으로 보정했다** — 총 높이는 합이라 중앙값이 아니라 평균이 맞다.

실측 오차: 보정에 쓴 조판에서 +1.3%, 쓰지 않은 조판(13px/2.3/65%)에서 −5.9%. 스크롤바 길이용으로는 충분하고, 더 정확하려면 오프스크린 일괄 실측밖에 없다(조판을 만질 때마다 멈칫한다).

---

## 스킬 상태

**정본은 `pdf2parallax.zip` 이다.** 저장소에 풀려 있는 `pdf2parallax/` 는 `.gitignore` 에 있다 — 스크립트를 고쳤으면 zip 을 다시 말아야 히스토리에 남는다. 2026-07-28에 다시 말면서 `{references,scripts,templates}/` 라는 항목도 지웠다(중괄호 확장이 안 먹은 셸 명령이 남긴 가짜 디렉터리다).

2026-08-12 에 고친 것 — **anthropic 경로의 재시도를 시간으로 잰다**(`_llm.py`). 아래 8/11 항목이 미결로 남긴 자리다. `MAX_RETRY = 3`·5/10/20초는 **35초** 만에 포기해서 몇 분짜리 연결 끊김을 못 넘겼다. `_retry_loop()` 하나로 `call()`·`call_vision()` 을 묶고 **횟수와 시간 둘 다**로 끊는다 — `PKT_MAX_RETRY`(기본 10) · `PKT_RETRY_BUDGET`(기본 900초), 백오프 상한 60초라 실 재시도 폭이 **35초 → 375초**다. `Retry-After` 를 따르고(상한 300초), 영구 오류(400·401·403·404·413·422)는 재시도 없이 즉시 올려 보낸다 — 다시 보내도 같은 답이라 8워커면 헛되이 기다리기만 한다(비전의 400 → `VisionUnavailable` 변환은 그대로다). API 없이 넷을 실측했다: 일시적 오류 재시도 후 성공 · 400 즉시 raise(대기 0) · `Retry-After: 17` 존중 · 소진 시 `RuntimeError` + 백오프 상한. **그래도 셸 재시도 루프는 계속 쓴다** — 안쪽 그물은 blip 용이고 몇 시간짜리 배치에는 바깥 그물이 따로 있어야 한다.

같은 날 — **《The Mind Is Flat》 개조식 판본을 완성했다.** `D:\ebook\__output__\the_mind_is_flat.nominal.parallax`(875블록 · 번역 816 · 그림 43 · superseded 240 · page_check 245행). 뉴닉 판본(`the_mind_is_flat.parallax` = `.newneek.parallax`, 두 파일 sha1 동일)은 손대지 않았다.

- **죽은 배치의 캐시는 세션 스크래치에 그대로 살아 있다.** 8/11 기록은 「18/99 청크에서 죽었다」지만 그 뒤 재시도 래퍼가 한 번 더 돌아 실제로는 **570/816 블록**까지 가 있었다 — 기록만 보고 처음부터 다시 돌렸으면 $3 을 버릴 뻔했다. 작업 디렉터리를 이번 세션으로 복사해 이어 돌렸고(남은 246블록 · 32청크 · $1.58), 이 실행에서도 연결 끊김이 한 번 났으나 새 재시도가 넘겼다. 이어받기 전에 **`build_system(register, glossary, polite)` 결과가 그 디렉터리의 `translate-system.txt` 와 바이트 단위로 같은지 볼 것** — 같아야 기존 번역과 캐시가 현재 명세와 어긋나지 않는다(캐시 키에 시스템 프롬프트가 없다).
- **이어받기의 자동 종결 교정은 이번 실행이 건드린 블록만 본다.** `fix_nominal_endings` 에 넘어가는 것이 `blocks`(= 이번에 빈 것만 고른 목록)라, 먼저 번역된 570블록은 점검을 못 받는다. 재개한 책은 끝나고 `--fix-endings-only` 를 **책 전체에** 한 번 더 돌릴 것. 실측은 816블록 중 누출 4블록·8문장(0.5%)이었고 그중 3블록이 고쳐졌다.
- **괄호 안의 내적 논증은 교정자가 손대지 않는다 — 영원히 「아직 누출」로 남는 자리다.** 남은 `b0085` 는 저자가 괄호로 묶어 옮긴 혼잣말 추론(`…한 부가 필요하다. 온라인으로 읽을 수도 있지만…`)이다. 검출기는 인용부호만 마스킹하고 괄호는 안 하는데, 교정자는 대사로 보고 규칙대로 지킨다. 다시 보내도 그대로이니 **한 블록 남는 것을 정상으로 알 것**. 괄호까지 마스킹하면 진짜 누출을 놓치므로 손대지 않았다.
- **verify 경고 넷은 전부 무해했다**(누락 0 · 한글없음 14 · 숫자 6 · 길이이상 1 · 용어집 1). 한글없음 14는 영문 참고문헌·URL·영어 어휘 문항이고, 길이비율 `b0789` 0.35 는 누락이 아니라 개조식이 더 세게 압축한 것이다(책 평균 0.49 — **정상 범위 0.35~1.6 은 해요체 기준이라 nominal 에서는 하한에 자주 닿는다**). 「숫자 누락」 6건은 각주 번호인데, **원문에 `[^n]` 마커가 0개인데 번역에는 106개가 생긴다** — 모델이 지어내고 대개 `[^3]` 으로 쏠린다. 뉴닉 판본도 115개로 같아서 이번 실행이 만든 회귀가 아니라 파이프라인의 기존 성질이다(미해결). 용어집 1건은 제목 `The Mind Is Flat` 이 개조식 규칙에 걸려 `마음은 평평함` 으로 나온 것 — 확정 역어는 `마음은 평평하다` 다.
- **`export.py --out` 은 파일이 아니라 디렉터리다.** 산출물 이름은 언제나 `book.parallax` 라, `--out …\the_mind_is_flat.nominal.parallax` 로 주면 그 이름의 **디렉터리**가 생기고 안에 `book.parallax` 가 놓인다. 임시 폴더로 뽑아 옮길 것.

같은 날 오후 — **《Beyond Weird》 개조식 판본도 만들었다**(사용자 지시). `D:\ebook\__output__\beyond_weird.nominal.parallax`(1,120블록 · 번역 1,072 · 그림 37 · asset 37 · superseded 5), 백업은 `beyond_weird.nominal.book.json`. 뉴닉 판본은 손대지 않았다. 첫 시도에 완주했고 87호출 ≈ **$4.83**(연결 끊김 한 번은 안쪽 재시도가 넘겼다). 두 번째 개조식 판본이라 절차가 굳었다 — 다음 책도 이대로 한다.

- **소스는 `.book.json` 백업이 아니라 `.parallax` 에서 되돌린다.** `beyond_weird.book.json`(8/9)에는 그 뒤의 사후 손질(광고 쪽 12블록 `dropped`, 가짜 제목 3개 강등, `b0837` 재번역)이 없다. `parallax_import.py` 왕복으로 블록·플래그·그림·용어집 83항목이 온전히 돌아온다. 용어집을 물려받으므로 두 판본의 역어가 어긋나지 않는다.
- **`--force` 대신 `ko`·`ko_raw` 만 비운다.** `--force` 는 매 재시도마다 전량을 다시 보내서, 중간에 죽으면 그때까지 쓴 돈이 날아간다. 번역만 비우면 translate.py 의 「빈 것만 묶는」 정상 경로라 재실행이 완료분을 공짜로 건너뛴다 — 셸 재시도 루프와 짝이 맞는 쪽은 이것이다.
- **전권을 한 번에 돌린 실행은 `--fix-endings-only` 재실행이 필요 없다.** 위 8/12 오전 항목의 「끝나고 책 전체에 한 번 더」는 *이어받기* 실행 한정이다(자동 교정에 넘어가는 것이 이번 실행이 채운 블록뿐이라서). 한 번에 다 돌면 전권이 곧 이번 실행분이고, 다시 돌려도 fixend 캐시가 같은 답만 준다.
- **verify 경고 6건은 전부 무해했다**(누락 0 · 한글없음 1 · 숫자 0 · 길이이상 5 · 용어집 0). 한글없음은 DOI URL 한 줄이고, 길이 비율 0.33~0.35 다섯은 누락이 아니라 개조식 압축이다 — 가장 긴 `b0505`(662→229자)·`b0388`(410→143자)을 원문 꼬리와 대조해 끝나는 지점이 같음을 확인했다(책 평균 0.48).
- **리포트의 「후반 드리프트」 종결어미 분포는 nominal 에서 읽지 마라.** `한다/이다` 로 끝나는 블록만 세는 지표라 개조식에서는 표본이 몇 개뿐이다 — 이 책은 앞 20% 가 1건이었다. 문체가 흐려졌는지는 이 숫자로 판정할 수 없다.
- **종결 누출 4블록(0.37%)에서 교정자가 0블록을 고친 것은 실패가 아니다.** 장 제목(「슈뢰딩거의 고양이, 새끼를 낳다」)·저자의 청유형 감탄·농담의 대화 서술 여섯 문장·짧은 대답으로, 「대사와 인용은 건드리지 않는다」 규칙이 지킨 자리다. 《The Mind Is Flat》 의 괄호 안 논증(`b0085`)과 같은 갈래다 — **몇 블록이 남는 것을 정상으로 알 것.**

2026-08-11 에 넣은 것 — **여섯 번째 register `nominal`(개조식 명사형 종결)과 종결 교정 패스**(사용자 지시). 문장이 한자어 동사성 명사로 끝난다(`두 부분으로 구성.` `설명 제시.` `폐기 필요.`). `함·됨·임`은 없어도 뜻이 통하면 떼고 끝맺는 명사 앞의 조사도 뗀다(`설명을 제시`→`설명 제시`). 고유어 명사형은 마땅한 한자어가 없을 때만, `했음/할 것임/고 있음`은 시제·상을 실을 때만. 세 가지가 이 작업에서 나왔다:

- **종결 규칙은 축 하나에 모아 둬야 한다.** 규칙을 계사문·청유형 처리까지 별도 축으로 쪼개고 `BASE` 의 [절대 규칙] 8번으로까지 승격했더니, 같은 36블록에서 `-다` 누출이 **1건 → 54건**으로 늘었다(시스템 프롬프트 29.4k→33.3k tok). 여러 자리에 흩는 것은 강조가 아니라 희석이다. 압축형으로 되돌리자 다시 잡혔다.
- **종결 교정 패스**(`translate.py: fix_nominal_endings`) — 문체 사양만으로는 새는 자리가 남고 누출률이 실행마다 0.6%~8.8%로 흔들린다. 누출은 **긴 블록의 가운데**에 몰린다(1,200자 12문장 단락은 앞뒤가 명사형인데 중간 여섯 문장이 서술형으로 풀린다). 번역이 끝나면 샌 블록만 골라 종결부만 갈아 끼운다 — 재번역이 아니다. 따로 돌리려면 `--fix-endings-only`, 끄려면 `--no-fix-endings`. 청크를 줄이는 것보다 이쪽이 싸다(전체의 10% 안팎만 다시 나간다).
- **검출기는 인용부호 안을 마스킹한다.** 대사의 `-습니다`는 인물이 실제로 한 말이라 개조식으로 바꾸지 않는 것이 사양인데, 마스킹하지 않으면 그 블록이 영원히 「아직 누출」로 남는다 — 교정자는 규칙대로 손대지 않으므로 다시 보내도 그대로다. `필요`·`중요`가 `요` 로 걸리지 않게 어미 형태(`-어요/-예요/-세요/-지요/-네요/-군요/-까요`)만 본다.

**`nominal` 은 deslop 을 태우지 않는다**(사용자 지시). 교정자의 규칙표가 전부 `-다` 서술문을 전제로 쓰여 있어(`~이 존재한다`→`~이 있다`, 조동사 중첩, 형식주어 …) 명사형 종결을 번역투로 읽고 문장을 통째로 서술형으로 되돌린다 — 문체 자체가 지워진다. `deslop.py` 가 `NO_DESLOP` 을 보고 스스로 빠지고, 굳이 돌리려면 `--strength` 를 명시해야 한다. 정본 zip 재포장 완료(19항목, 바이트 대조 19/19 일치 · UTF-8 손상 0 · 풀어서 import 확인). 옛 zip 은 `%TEMP%\pdf2parallax.zip.bak-20260811`.

같은 날 실전에서 밟은 것 둘 — **전권 번역이 `APIConnectionError` 로 죽는다.** 《The Mind Is Flat》 개조식 재번역이 18/99 청크(179블록·$0.98)에서 `RuntimeError: API failed after 3 attempts: Connection error` 로 끝났다. TLS 검사 장비 문제(`CERTIFICATE_VERIFY_FAILED`)와는 다른 증상이고 `truststore` 는 정상이었다 — 그냥 일시적 연결 끊김이다. `_llm.py` 의 `MAX_RETRY = 3`(5/10/20초 백오프)로는 몇 분짜리 blip 을 못 넘긴다. 손실은 없다(청크마다 저장 + 캐시라 재실행하면 완료분은 공짜) 그러나 **밤새 도는 배치는 셸 재시도 루프로 감쌀 것** — 안 그러면 아침에 22% 에서 멈춰 있다. 근본 해결은 `_llm.py` 의 재시도를 늘리는 것인데(datalab 경로는 2026-08-09 에 이미 `TransportError` 백오프를 넣었다) anthropic 경로는 아직 손대지 않았다. **→ 2026-08-12 에 해결(위 항목).**

**백그라운드 작업 알림의 exit code 를 믿지 마라.** 위 크래시를 `python … ; echo EXIT=$?; tail` 로 엮어 띄웠더니 파이썬이 죽었는데도 셸 전체는 0 으로 끝나 완료 알림에 「exit code 0」이 찍혔다. 로그를 열어 보지 않았으면 성공으로 오판했을 자리다. 종료 코드를 로그에 직접 남기거나 명령을 엮지 말 것.

같은 날 이어서 — **문체 지침을 `ko-style` 스킬로 떼어 냈다**(사용자 지시). `pdf2parallax` 의 register 명세는 그 파이프라인 안에서만 쓸 수 있었는데, 톤앤매너 자체는 번역이든 새로 쓰는 글이든 쓰인다. 네 파일이다 — `SKILL.md`(축 여섯·문체 여섯 표, 쓰는 순서, deslop 강도표), `references/registers.md`(파이프라인 의존 서술을 걷어낸 판), `references/nominal.md`(개조식 전용 — 규칙 여덟, 실측, 흔한 사고), `scripts/check_endings.py`(종결 검사, **표준 라이브러리만** — 스킬을 받은 곳에 anthropic SDK 가 없어도 돈다). 검사기는 마크다운 코드펜스·표·머리글을 건너뛰고 목록 마커는 벗긴다. 실측 확인: 개조식 샘플 190문장에서 오탐 0, 의도적 서술형 2문장 검출, 누출이 있으면 exit 1.

**이 스킬만 소스를 git 에 넣는다.** `pdf2parallax/` 는 `.gitignore` 에 있고 zip 이 정본인데, 그 탓에 「스크립트를 고쳤으면 zip 을 다시 말아야 히스토리에 남는」 마찰이 있고 실제로 zip 이 유일한 구명줄이었던 사고도 있었다(아래 PowerShell 항목). `ko-style` 은 텍스트 네 개뿐이라 소스를 추적하고 `ko-style.zip` 은 배포본으로만 둔다. 두 스킬의 문체 명세가 갈라지지 않게 할 것 — 한쪽을 고치면 다른 쪽도 본다.

2026-08-08~09 에 고친 것 — **네 번째 책 《Beyond Weird》(Philip Ball, 양자역학 교양서, 175쪽 순수 스캔본)를 변환하면서 `pagecheck.py` 에서 셋.** 산출물은 `D:\ebook\__output__\beyond_weird.parallax`(1,120블록 · 번역 1,072 · 그림 37 · 제외 14), 백업은 `beyond_weird.book.json`.

- **순수 스캔본에 Tesseract 레이어를 굽지 마라 — datalab 을 쓸 거면.** 사용자 지적으로 알아챘다. `extract.py` 는 레이어가 없으면 175쪽을 300dpi 로 OCR 해 사본을 만드는데(15분), `--engine datalab` 은 **항상 전 쪽 재구성**이라 그 레이어를 통째로 `superseded` 로 밀어낸다 — 하류로 한 글자도 가지 않는다. 레이어가 닻으로 필요했던 것은 signals 의 빈 쪽 재구성 경로(`rebuild_page`, 2026-08-06)가 들어오기 전 얘기다. 지금은 `extract.py --allow-scanned` 로 **0블록** `book.json` 을 내고 datalab 이 전 블록을 새로 만든다. SKILL.md ② 의 "레이어가 아예 없으면 비전이 읽은 텍스트도 붙일 곳이 없어 조용히 버려진다"는 datalab 경로에는 더 이상 맞지 않는다.
- **datalab 과금 단위는 센트다(100배 과대 보고).** 응답이 `total_cost: 1` 과 `cost_breakdown.final_cost_cents: 1.0` 을 함께 주는데 `_dl_record` 가 total_cost 를 달러로 더해, 5쪽 시험이 「$5.00」로 찍혔다. 실제 5¢. 전 쪽을 태울지 정하는 근거라 틀리면 작업을 접게 만든다. 이제 센트 필드를 우선해 100 으로 나눈다. (실측: 175쪽 accurate 총 $1.72.)
- **연결 끊김이 조용한 빈 쪽을 만든다.** 재시도 루프가 **429 만** 보고 있어 `Server disconnected without sending a response`(httpx `TransportError`)가 곧바로 판독 실패로 떨어졌다 — 175쪽 중 **8쪽이 통째로 빈 쪽**이 됐다. 게다가 리포트에서 그 쪽들의 일치율이 **100%** 로 찍힌다(빈 것 대 빈 것). 가장 망가진 쪽이 가장 멀쩡해 보이는 것이다. POST 를 `TransportError` 에도 백오프 재시도하게 하고, 폴링은 마감까지 계속 두드리고, 실패 쪽 일치율은 `—` 로 찍고 `datalab 판독 실패` 를 통계에 센다. **빈 쪽 검사는 사후에 직접 하라** — 쪽별 블록 수를 세어 0 인 쪽이 있는지 보는 것이 유일하게 확실하다.
- **부분 실행이 판정을 통째로 덮어썼다.** 실패한 8쪽을 `--pages 163-163` 식으로 되살렸더니 `book["page_check"]` 가 마지막 실행분만 남아 **176행 → 2행**으로 줄었다. 리포트 md 는 지워지는 파일이라 이것이 유일한 사본인데 줄어든 것은 아무 데도 안 찍힌다. 이제 이전 판정에 **병합**한다. (이번 책의 `page_check` 는 그 전에 이미 잃었다 — 진단 자료라 읽기에는 영향이 없다.)

이 책에서 사후로 손본 것 둘: 33쪽이 Early Bird Books 광고 페이지(무료 이북 QR)라 12블록을 `dropped` 로, 표지·판권면의 가짜 제목 3개(`BEYOND WEIRD`·`LIBRARY OF CONGRESS…`·`Beyond Weird`)를 본문으로 내렸다(목차 22항목). verify 경고 4건 중 셋은 무해(DOI 원문 유지, 인쇄판 쪽 참조 `( page 151 )` 탈락)였고 하나는 진짜 누락이라 그 블록만 `--range b0837-b0837 --force` 로 다시 번역했다 — 저자의 삽입구가 통째로 빠져 길이 비율 0.30 으로 걸렸다. **길이 비율 이상은 진짜를 잡는다.** 문체 실측: 본문 937단락 중 해요체 893 · `-다` 종결 6(전부 공리 나열) · 문두 입말 접속사 152 · 이모지 0. 지출 합계 ≈ $12(datalab $1.72 + 번역 $4.8 + deslop $5.3 + 나머지).

2026-08-06 에 고친 것 — **목차 계층 규칙 둘** (`extract.py`, 둘 다 `normalize_headings` 안이라 extract·pagecheck 양쪽에 먹는다):
- `CHAPTER_MARK_RE` 에 영단어 수사(one~twenty) 추가 — "PART ONE" 이 h1 로 못 올라가 목차가 평평했다. PART h1 바로 다음 같은 쪽의 제목은 부(部) 부제로 보고 함께 h1.
- `demote_printed_toc()` 신규 — 인쇄된 목차 페이지의 항목들이 제목으로 잡혀 앱 목차에 목차가 두 벌 실리던 것을, Contents 표제가 선 쪽(+제목 3개 이상 몰린 연속 쪽)의 제목을 본문으로 내려 막는다.
`the_mind_is_flat.parallax` 는 같은 내용을 사후 수정으로 반영했다(+본문 대문자 소제목 36개 h3 강등, 원본 `.bak`). 정본 zip 재포장(19항목, 바이트 대조 일치).

정본은 2026-08-04 에 다시 말았다 — 19개 항목(`repair.py` 가 새로 들어갔다). 말기 전에 전 파일을 UTF-8 로 엄격 디코드해 손상 0을 확인했고(BOM·치환문자 없음), 만든 뒤에는 풀어서 바이트 대조 19/19 일치와 `import extract, pagecheck, repair` 까지 확인했다. 옛 zip 은 세션 임시 폴더에 날짜를 붙여 남겼다.

2026-08-02~03 세션에 고친 것 — **단락이 토막 나던 원인을 뿌리에서 잡았다.** `the_mind_is_flat.parallax` 가 문장 한가운데서 끊긴 블록으로 가득했고, 끊긴 블록은 번역도 안 됐다.

**문장이 끝났는지 판정** (`extract.py`)
- `TERMINAL_RE` 가 `[.!?…”"')\]]` 였다 — **닫는 따옴표만으로도 문장이 끝난 것으로 봤다.** `r'[.!?…][\'"’”)\]]*\s*$'` 로 바꿔, 진짜 문장부호 **뒤에 붙은** 따옴표만 인정한다.
- `continues_sentence()` — 뒤 블록이 여는 따옴표·괄호로 시작하면 그것을 벗기고 소문자인지 본다. `("but as we shall see…` 가 대문자로 시작한다고 새 문장 취급되던 경로다.
- `stitch_blocks()` 의 본문 유형에 **`figcaption` 을 넣었다.** 도판 설명이 반토막 난 채(`Figure 3. … Visual acuity beautifully` / `tracks the density…`) 하류로 가고 있었다. 병합할 때 유형은 caption 쪽이 이긴다.

**같은 쪽 안의 이음매** (`pagecheck.py: stitch_pages`)
- 쪽 경계만 보고 있었다. 같은 쪽에서도 앞이 하이픈으로 끝나거나 뒤가 문장을 잇고 있으면 붙인다. 수리 전 157곳 → 0.

**중복 삽입 제거** (`pagecheck.py`)
- 비전이 이미 있는 글을 다시 넣을 때 거르는 규칙이, 한쪽은 문장부호를 벗기고 한쪽은 원문 그대로 비교하고 있었다 — 122건 중 1건만 걸렀다. 낱말 정규화로 바꾸니 93건이 걸렸는데, 이번에는 **기존 글로 시작해서 새 내용으로 이어지는** 블록(1,720자 중 60%가 새 내용)까지 지웠다. 지금은 규칙 하나뿐이다: 덮임률 0.8 이상이면 버린다.

**본문을 도판 조각으로 오인해 버리던 것** (`pagecheck.py: droppable`)
- 전권 첫 실행에서 본문 12블록이 삭제됐다 — 508자 단락과 진짜 캡션 5개 포함. `reads_as_prose` 가 잘린 본문을 못 알아봤다. 기능어 3개 이상이면 본문으로 보고, figcaption 은 25자 넘으면 지키게 했다. 실제 사례 13건으로 대조해 어긋남 0, 재실행에서 본문 오삭제 0.
- 삭제 사유를 구분한다(`furniture` / `figure` / `garbled`) — 러닝 헤드는 3쪽 이상 반복을 요구하지만 도판 조각에는 그 잣대가 맞지 않는다.

**프롬프트를 바꿔도 옛 답이 되살아나던 것** (`pagecheck.py`)
- 캐시 키에 모델은 있는데 **프롬프트가 없었다.** 시스템 프롬프트를 고쳐도 지난 답을 그대로 재생했다. `SYS_FINGERPRINT`(sha1 앞 8자)를 키에 넣었다.

**비전 프롬프트 재작성** (`pagecheck.py: VISION_SYS`)
- 도판 안의 글자(축 이름·눈금·범례·데이터 라벨·축척), 도판 번호(`Figure 4`·`그림 1`·`(b)`), 판독이 뭉갠 글자 덩어리를 **받아쓰지 않는다.** 대신 「**미완성이라는 이유로 블록을 버리지 말 것**」과 「**캡션은 버리지 말 것**」을 못 박았다 — 위의 오삭제가 여기서도 나왔다.

**뉴닉체 레지스터** (`translate.py`, `deslop.py`)
- 해요체·40자 분할·일상어·「우리」·문두 입말 접속사. 지어내기와 이모지, 빈정거림을 다정함으로 바꾸는 것은 금지. deslop 강도는 `약`(문체 자체가 규칙이라 세게 깎으면 도로 평범해진다).

**이어 하기가 처음부터 다시 하던 것** (`translate.py`, `deslop.py`)
- 이미 번역된 블록까지 묶어 보내고 있었다. 빈 것만 묶는다. deslop 도 `ko_raw` 가 없는 것만.
- `--force` 로 다시 번역하면 `ko_raw`(deslop 흔적)가 남아 deslop 이 전량을 건너뛰었다 — 새 번역을 쓸 때 함께 지운다.
- `context_for()` 는 앞 두 블록을 **책 순서로** 집는다. 빈 것만 묶으면 청크 안의 이웃이 실제 이웃이 아니다.

**`repair.py` 신규** — 이미 만들어진 `.parallax` 를 사후 수리한다(중복 삽입 삭제·잘못된 삭제 복원·이음매 병합). `--apply` 는 `.bak` 을 남긴다. 기존 파일에 파이프라인 수정분을 소급할 유일한 길이다.

2026-07-28 두 번째 세션에 고친 것:

**진행 표시** (`pagecheck.py`)
- 20쪽마다 한 번, `pool.map` 순서 소비로 찍던 것을 **쪽마다·완료 순서**(`as_completed`)로. 앱의 진행 표시가 몇 분씩 침묵하던 원인의 절반이다(나머지 절반은 앱의 `-u`).

**판정 보존** (`pagecheck.py` → `export.py` / `parallax_import.py`)
- 판정(쪽별 일치율·단·사유·판독 메모 + 요약 통계)을 `book["page_check"]` 로 내보내고 `page_check` 테이블로 옮긴다. 요약은 `page=0` 행. 리포트 md 는 지워지는 파일이라 이것이 유일한 사본이다.

**번호 목록 항목 강등** (`extract.py: normalize_headings`, `demote_numbered_items`)
- 비전이 굵은 번호로 시작하는 자기평가 문항을 쪽마다 다르게 제목으로 읽는다(실제로 문항 1~4는 본문, 같은 목록의 5~8만 h2 — 쪽이 달랐다). 번호(+마침표)로 시작하면서 60자 이상이면 본문으로 내린다. `1. Introduction` 같은 짧은 번호 제목, `[14]` 마커로 시작하는 진짜 각주는 남는다.
- 같은 규칙을 **footnote 오분류에도, stitch 전에** 적용한다(`demote_numbered_items`). footnote 는 stitch 의 본문 유형이 아니라서, 문항이 footnote 로 붙으면 쪼개진 반쪽이 이어지지 못한 채 하류로 간다 — 챕터 3 문항 구간이 깨진 경로다.

**챕터 3 급 구조 파손 방지** (`extract.py: stitch_blocks`, `pagecheck.py: flag`)
- 분철 하이픈으로 끝난 블록 뒤에 **대문자로 시작하는** 블록은 잇지 않는다. 낱말의 나머지가 아니라, 레이어가 조각을 잃었거나 제목이 섞여 든 것이다(`diffi¬`+`Questions…` → `diffiQuestions` 사고).
- `flag()` 에 구조 신호 둘 추가: 분철 기호(`¬`) 잔존, 낱말 중간 대문자(`[a-z]{3}[A-Z][a-z]{3}` — YouTube·McKinsey 는 안 걸린다). 일치율은 낱말 수준이라 블록이 뒤엉켜도 높게 나와, 이 신호가 없으면 그런 쪽이 비전 수리 대상에서 빠진다.

2026-07-28 첫 세션에 고친 것:

**`print` 가 추출 전체를 죽이던 버그** (`extract.py`, `_llm.py`)
- Windows 콘솔은 cp949 라 `print` 가 코드페이지에 없는 글자에서 예외를 던진다. 진행 문구 하나의 em dash 가 **이미 다 쓴 `book.json` 을 버리게 만들었다** — 파일은 멀쩡한데 exit 1 이라 앱은 추출 실패로 봤다. 조건부로 붙는 글자라 어떤 PDF 에서는 되고 어떤 PDF 에서는 안 됐다.
- 두 곳에서 `sys.stdout.reconfigure(errors="replace")`. 진행 표시는 어떤 경우에도 작업을 실패시킬 수 없어야 한다. 앱은 여기에 더해 `PYTHONIOENCODING=utf-8` 로 띄운다(그러지 않으면 진행 문구의 한글이 깨져 들어온다).

**OCR 레이어 판정** (`extract.py: scan_backed`, `meta.ocr_layer`)
- 기존 신호는 한글 글리프 밀도뿐이라 **한국어 팩으로 OCR 한 경우만** 잡았다. 작업 중인 책이 바로 그 반례다 — 레이어는 판독 산출물인데 밀도가 0이라 「멀쩡함」으로 나왔다.
- 쪽을 통째로 덮는 이미지 위에 글자가 얹혀 있으면(OCR 샌드위치) 그 레이어는 판독 산출물이다. 20쪽을 표본으로 60% 이상이면 참. 이 책은 100%.
- 값을 화면에만 찍고 버리지 않고 `meta.ocr_layer` 로 내보낸다. 앱이 `--trust` 를 권하는 근거가 이것이다.

**키가 없을 때의 실패** (`_llm.py: _client`)
- `ANTHROPIC_API_KEY` 가 없으면 SDK 가 `TypeError` 를 던지는데, 재시도 루프가 이걸 일반 오류로 보고 여섯 번(70초) 물러났다가 역추적을 토했다. 무엇이 없는지는 어디에도 없었다. 먼저 확인하고 한 줄로 죽는다.

2026-07-27에 세 가지를 고쳤다. 상세는 `SKILL.md`와 각 함수 주석.

**러닝 헤더 제거** (`extract.py: strip_running_heads`)
- 줄 번호(`lines[:2]`)가 아니라 **페이지 높이의 위·아래 8% 밴드**로 후보를 고른다.
- 임계값 `max(3, 전체의 25%)` → `max(3, 5%)`. 책은 짝수쪽에만 머리글을 넣거나 장 시작에서 빼므로 20%대가 정상인데, 25% 기준이 191쪽 중 37쪽에 있던 머리글을 통과시켰고 `classify()`가 전부 heading으로 읽어 목차가 도배됐다.
- PDF outline에 있는 제목은 절대 지우지 않는다.

**단락 이어붙이기** (`extract.py: stitch_blocks`)
- 원래 `pagecheck.py`에만 있었다. pagecheck는 쪽당 비전 호출이라 대개 건너뛰므로 끊긴 채로 하류에 넘어갔다. extract로 내려 모델 없이 항상 돈다.
- 페이지 경계는 "앞 블록이 종결부호 없이 끝남"만으로 병합한다(뒤가 소문자일 것까지 요구하면 고유명사 앞 절단을 다 놓친다). 같은 페이지 안은 하이픈이나 소문자 시작을 추가로 요구한다.
- OCR이 분철 하이픈을 `¬`(U+00AC)로 쓰는 것까지 복원한다.

**제목 레벨 정규화** (`extract.py: normalize_headings`, `pagecheck.py` 에서도 호출)
- 폰트 크기 추론은 쪽마다 조판이 달라 흔들리고, `--trust vision` 은 한 페이지씩 따로 읽어 같은 장 제목이 다른 레벨을 받는다. 실제로 `CHAPTER 1`=h3, `CHAPTER 2`=h2, `CHAPTER 3`=h3 이었고 목차에서 장이 절보다 아래에 놓였다.
- `CHAPTER|PART|BOOK|SECTION` + 숫자·로마숫자, 그리고 `CONTENTS`·`INTRODUCTION` 같은 앞뒤 부속물 표제는 **h1**, 나머지 제목은 **h2**. 두 단계면 충분하고, 더 잘게 나누려면 추측해야 하는데 틀린 추측은 평평한 목록보다 나쁘다. `--keep-heading-levels` 로 끌 수 있다.
- 앱의 목차는 h1 만 bold 로 세운다(`reader.css`). 레벨이 두 개뿐이라는 전제 위에 서 있으니 규칙을 바꾸면 CSS 도 함께 봐야 한다.

**`--trust vision`** (`pagecheck.py`)
- 기존 설계는 "글자는 텍스트 레이어가 정답, 구조는 비전이 정답"이라 판독 텍스트를 일치율 계산에만 쓰고 버렸다. **레이어 자체가 OCR 산출물이면 그 전제가 거짓이다.**
- 이 모드는 전 페이지를 이미지에서 다시 받아쓴다. 밀려난 레이어 텍스트는 `superseded`에 보관하고 새 블록에 `from_ocr`가 붙는다.
- **레이어가 멀쩡한 책에는 쓰지 마라.** 비전은 고어 철자를 현대화하고 저자의 의도적 오기를 고친다. 먼저 `--pages 1-20`으로 시험할 것.

---

## 이 PC 환경에서 물린 것

**`requirements.txt`의 `truststore`를 걷어내지 말 것.** 네트워크 구성에 따라 파이썬만 전 API 호출이 `CERTIFICATE_VERIFY_FAILED`로 죽는 환경이 있다 — 브라우저와 Electron은 멀쩡히 통하는데 파이썬만 죽으므로 키 문제로 오진하기 쉽다. `truststore`는 인증서 검증을 OS에 위임한다. 검증을 끄는 것이 아니라 이 PC의 나머지 프로그램과 같은 판단을 하게 하는 것이다. `SSL_CERT_FILE`로 루트를 추가하는 우회는 통하지 않는 경우가 있다.

**`python3`는 Store 스텁.** 이 PC의 `python3.exe`는 0바이트 재파싱 지점(Microsoft Store 별칭)이다. 진짜 파이썬은 `C:\Python314\python.exe`이고 `python`으로 잡힌다. 스킬의 `run.sh`는 `python3`를 부른다.

**`npm run dist` 는 산출물 잠금을 스스로 푼다.** 이 PC 에는 asar 뷰어 Orca 가 깔려 있고 `.asar` 파일 연결을 잡고 있다. 빌드가 새 `release\win-unpacked\resources\app.asar` 을 만드는 순간 **explorer.exe 가 Orca 를 띄우고**(부모가 explorer 인 것을 확인했다) 그 프로세스가 파일을 물어, 다음 빌드가 출력 폴더를 지우지 못하고 `remove … app.asar: The process cannot access the file` 로 죽었다. 오류 문구 어디에도 원인이 없다.

`scripts/clean-release.mjs` 가 `electron-builder` 앞에 끼어 폴더를 비운다. 몇 번 기다려 보고(검사기가 잠깐 잡은 것이면 저절로 풀린다), 그래도 잠겨 있으면 **Restart Manager 에 누가 물고 있는지 직접 물어**(`scripts/who-locks.ps1`) 지목된 프로세스만 닫는다. 이름으로 짐작하지 않으므로 상관없는 앱을 건드리지 않고, 닫은 것은 반드시 찍는다. 연속 두 번 실행으로 확인했다.

Restart Manager 를 쓸 때 물린 것 둘 — **디렉터리를 섞어 넘기면 등록이 조용히 실패해** 목록이 통째로 비어 나온다(파일만 넘길 것). 그리고 `powershell -File script -Paths a b c` 는 첫 하나만 묶고 나머지를 위치 인자로 흘려 죽으므로, 경로 목록은 **파일로** 넘긴다.

**PowerShell 로 UTF-8 한글 파일을 건드리지 말 것.** 2026-08-02 에 `(Get-Content …) | Set-Content -Encoding utf8` 로 스킬의 `references/registers.md` 를 **되돌릴 수 없게 망가뜨렸다.** PowerShell 5.1 의 `Get-Content` 는 UTF-8 을 cp949 로 읽어 그 시점에 글자를 잃는다 — 다시 utf8 로 써도 잃은 것은 돌아오지 않고, 되돌리려는 시도는 BOM(`﻿`)과 `\x80` 만 더 남겼다. 원본은 `pdf2parallax.zip` 에서 꺼내 살렸다. **파일 편집은 Read/Write/Edit 도구나 파이썬으로 한다.** zip 정본이 유일한 구명줄이었다는 점도 기억할 것.

**백그라운드 작업 출력을 `Select-Object -Last N` 으로 거르지 말 것.** 파이프가 전체를 버퍼링해 로그 파일이 0바이트로 남는다 — 전권 deslop 이 1117개 중 758개에서 죽었을 때 무슨 일이 있었는지 볼 수 없었다.

**DRM 컨테이너를 PDF로 착각하지 말 것.** 원래 있던 `the_meaning_of_your_life.pdf`(76MB)는 매직 바이트가 `%PDF-`가 아니라 `SCDSA004`인 전자책 DRM 컨테이너였다. PyMuPDF가 열지 못한다. 2026-07-27에 삭제했고, 쓸 수 있는 파일은 `the_meaning_of_your_life_compressed.pdf`(38.6MB, 진짜 `%PDF-1.5`)다. 새 원본을 받으면 먼저 앞 8바이트를 확인하라.

---

## 접미사 ID — 파서가 삼키던 버그

pagecheck가 삽입·재구성한 블록은 접미사 ID를 갖는다(`b0052a`). 한 페이지에 여러 개가 들어가면 `suffix_id()`가 **직전 ID에 다시 글자를 붙여** `b0052aa`, `b0052aaa`로 자란다.

양쪽 와이어 파서가 이걸 감당하지 못했다:

| 위치 | 기존 | 결과 |
|---|---|---|
| `src/main/translate/prompt.ts` | `[a-p]?` — 한 글자만 | 두 글자 이상 ID의 번역이 응답에서 버려짐 |
| 스킬 `scripts/_llm.py` | 접미사 불허 | CLI 번역에서 pagecheck 산 블록이 전부 누락 |

둘 다 `[a-p]*`로 고쳤다. **실패가 조용하다** — 블록이 그냥 안 채워질 뿐이라 재실행해도 계속 비어 있고, 원인은 `verify.py`가 잡아 줄 때까지 보이지 않는다. 실제로 전량 번역이 929/956에서 멈춘 덕분에 드러났다. 앞으로 번역이 특정 개수에서 멈추면 여기를 먼저 의심하라.

---

## 2026-07-28 오후 — 이름 바뀐 파일을 유실로 오판한 소동

사용자가 작업 파일을 `the_meaning_of_your_life_v2.parallax` → `the_meaning_of_your_life_compressed.parallax` 로 **개명**했는데, 세션이 옛 이름으로 `sqlite3.connect` 하면서 0바이트 빈 파일이 생겼고 이를 유실로 오판해 md 스냅샷에서 재건까지 했다. 원본은 처음부터 무사했다. 교훈 둘: **`sqlite3.connect` 는 없는 파일을 소리 없이 만든다** — 데이터 파일은 열기 전에 존재부터 확인하라(`mode=ro` URI 로 열면 이런 사고가 없다). 그리고 **파일이 없어 보이면 지웠는지보다 이름이 바뀌었는지를 먼저 의심하라.** 잘못 만든 재건본 v2 는 앱이 물고 있어 못 지웠다 — 앱 종료 후 삭제할 것. md 스냅샷 쌍(`.en.md`/`.ko.md`)은 유일한 백업 수단으로 유효하니 남겨 둔다.

챕터 3 「Questions for Reflection and Self-Assessment」 구간이 깨져 있던 것은 진짜였고, **원본 파일에** PDF 67쪽과 대조해 수리했다: 66→67쪽 경계에서 단어 중간(`diffi|cult`)으로 잘린 단락 복원, 문항에 파묻힌 제목을 별도 h2(`b0429a`)로, 쪼개진 문항 8개 병합, `¬` 분철·OCR 쓰레기(`'tri`) 정리, 문항 유형 footnote→p 교정, 제목 번역을 감싼 `**…**` 제거. 최종 948블록.

깨진 원인: 67쪽 블록들의 플래그가 0 — **이 쪽은 재판독을 거치지 않은 생 텍스트 레이어였다.** 일치율은 낱말 수준이라 블록이 뒤엉켜도 높게 나와 수리 그물을 빠져나갔다. 재발 방지로 스킬에 세 가지를 넣었다(아래 스킬 상태 참조).

---

## 정본 아홉 권 — 2026-08-20 현재

`D:\ebook\__output__` 에 **책당 `.parallax` 한 파일뿐이다.** `.book.json` · `.ko.md` ·
stale `-wal`/`-shm` 은 2026-08-19 에 사용자가 지웠다. 개조식 판본도 함께 정리돼
아홉 권이 전부 `-다` 평서형(essay 또는 business)이다.

| 책 | 문체 | 블록 | 번역 | 용어 | h1/h2/h3/h4 |
|---|---|---:|---:|---:|---|
| `beyond_weird` | essay | 1,081 | 1,047 | 90 | 21 |
| `no_self_no_problem` | essay | 559 | 550 | 57 | 10 / 41 |
| `quantum_mechanics_the_theoretical_minimum` | essay | 2,113 | 1,289 | 48 | 14 / 98 / 35 |
| `rewired` | business | 3,667 | 3,473 | 74 | 27 / 58 / 190 / 304 |
| `signals` | business | 1,665 | 1,663 | 84 | 7 / 44 / 224 |
| `the_conquest_of_happiness` | essay | 336 | 335 | 46 | 6 / 17 |
| `the_human_agent_orchestrator` | business | 1,961 | 1,830 | 0 | 14 / 98 / 179 |
| `the_meaning_of_your_life` | essay | 922 | 922 | 0 | 12 / 72 |
| `the_mind_is_flat` | business | 846 | 803 | 66 | 6 / 13 / 35 |

블록 수는 `dropped`(flags&32) 를 뺀 값이다. 아홉 권 모두 `integrity_check` 통과.
문체는 종결로 실측했다(`-다` 종결이 압도적이고 명사형 종결은 33~97 로 전부 2% 미만).
`.parallax` 스키마에 문체 칸이 없어서 파일만 보고는 알 수 없다.

**`the_meaning_of_your_life.parallax` 는 2026-08-19 정리에서 함께 지워졌다가
휴지통에서 복원했다.** 950블록 · 번역 950 · essay 로 온전하다.

**`the_conquest_of_happiness` 는 2026-08-20 에 새로 넣었다.** 전면 이미지 스캔이라
판독을 datalab 으로 돌려야 했다. 아래 「판독은 datalab 고정이다」를 볼 것.
합계 $9.07(datalab $1.91 · 번역 $3.21 · deslop $3.54 · vision 실패분 $0.39 · 용어집 $0.02).

**개조식 세 권은 `.book.json` 에만 있던 유일본이었다**(`quantum_mechanics` ·
`signals` · `the_mind_is_flat`). `.parallax` 짝이 없어 함께 사라졌다. 역시 휴지통에
있다. 되살리려면 `export.py` 로 `.parallax` 를 뽑아야 앱에서 열린다.

계층 깊이가 책마다 다른 것이 **정상**이다. `beyond_weird` 가 평평한 것은 원본
Contents 자체가 번호 없는 에세이 20편이기 때문이고, `rewired` 가 넉 단인 것은
부-장-절-소절을 실제로 쓰기 때문이다.

목차에 아직 남은 잔재 하나가 있다. `rewired` 의 `Library of Congress
Cataloging-in-Publication Data is Available:` 이 h2 다. 판권 쪽이 인쇄 목차 **뒤**
(p20)라 앞표지 규칙에 안 걸린다. 한 건이라 두었다.

## 남아 있는 데이터 파일

**정본 여덟 권** — `__output__` 에 책당 한 파일. business 둘(`rewired`·`the_human_agent_orchestrator`), nominal 다섯(`beyond_weird`·`the_mind_is_flat`·`signals`·`quantum_mechanics_the_theoretical_minimum`·`no_self_no_problem`), 뉴닉 하나(`the_meaning_of_your_life`).

**개조식 판본 셋** — `the_mind_is_flat.nominal.parallax`(2026-08-12 오전, 875블록 · 번역 816) · `beyond_weird.nominal.parallax`(같은 날 오후, 1,120블록 · 번역 1,072) · `signals.nominal.parallax`(2026-08-15 저녁, 1,717블록 · 번역 1,715). 각각 뉴닉 판본과 **같은 원문·같은 용어집**에서 갈라져 나왔고 뉴닉 판본은 그대로 있다. 백업은 `*.nominal.book.json`.

> 2026-08-15 에 `__output__` 이 정리돼 **책당 한 파일**만 남아 있다(뉴닉 중복본 삭제). `the_meaning_of_your_life.parallax` 는 아직 뉴닉이고, 사용자가 이번에는 signals 만 지시했다.

**2026-08-17 에 네 권을 한꺼번에 끝냈다** — 표를 그림으로 오려 내는 새 경로의 첫 실전이다. 넷 다 순수 스캔본이라 전 쪽 판독이 필요했다.

| 책 | 문체 | 블록 | 번역 | 특징 |
|---|---|---|---|---|
| `rewired` | business | 4,258 | 3,855 | 표 39 · 그림 148 |
| `the_human_agent_orchestrator` | business | 1,996 | 1,854 | **표 47** · 그림 84 |
| `quantum_mechanics_the_theoretical_minimum` | nominal | 2,137 | 1,311 | **수식 792** · 인라인 606단락 |
| `no_self_no_problem` | nominal | 580 | 569 | 그림 9 |

**넷 다 `누락 0`.** 지출은 판독 $13.79 + 번역 약 $22 = **약 $36**(1,471쪽).

- **수식이 번역을 온전히 통과한다.** `equation` 792개는 전부 `ko` 가 비어 있고(NO_TRANSLATE 정상), 번역문에 남은 수식 조각 **3,219개가 KaTeX 로 100% 렌더**된다. 진짜 빠진 것은 606단락 중 13곳(2.1%)뿐이고 `$j$`·`$\lambda^*$` 같은 홑기호 반복이다 — 한국어가 「λ의 켤레」로 풀어 쓰면 자연히 하나 준다.
- **수식 검사에서 두 번 오탐을 냈다.** 처음에 **개수**만 세어 19단락이 빠진 것으로 나왔는데, 전문을 보니 `\langle A | \mathbf{M}` → `\langle A|\mathbf{M}` 처럼 **공백만 다른** 것이었다. 번역 모델은 LaTeX 안의 공백을 정규화한다 — **수식을 대조할 때는 공백을 지우고 비교할 것.**
- **비즈니스체는 길이 비율을 세게 낮춘다.** rewired 의 「길이 이상」 9건이 전부 정상이었다(0.31~0.35, 책 평균 0.50). 한자어 우선·대명사 생략이 겹친 결과다 — nominal 에서 하한에 닿는 것과 같은 이유이니 **business 도 길이 경고를 그대로 믿지 말 것.**
- 「한글 없음」은 각주 인용·URL(`McKinsey.com/RewiredAssessment`)·색인 항목이라 무해하다.
- rewired `b2550` 은 360→361쪽 넘김에서 원문이 `such as base vs.` 로 갈린 자리다 — 번역이 그 경계를 따라갔을 뿐 **판독 단계의 이음매 문제**다.

> **밟은 것 넷.**
> ① **Anthropic 사용량 한도에 걸렸다**(`400 … regain access on 2026-09-01`). 재시도로 풀리지 않는 영구 오류인데 바깥 루프가 일시 실패로 보고 30초마다 헛돌았다. 지금은 `books-translate.sh` 가 한도 문구를 만나면 **즉시 멈춘다.** 한도는 콘솔에서 올려야 풀린다.
> ② **`verify.py` 는 경고가 남으면 비-0 을 돌려준다.** 재시도 래퍼로 감싸면 16번 헛돈다 — verify 는 감싸지 말 것(경고 판단은 사람이 한다).
> ③ **판독과 번역을 겹쳐 돌릴 때는 서비스가 갈려야 한다.** Datalab(판독)과 Anthropic(번역)은 무관하지만, 한 권을 「판독→번역」 통째로 도는 스크립트를 병렬로 걸면 짧은 책이 먼저 번역에 닿아 **같은 Anthropic 을 둘이 두드린다.** 판독 전용(`books-read.sh`)과 번역 전용(`books-translate.sh`)으로 갈랐다.
> ④ **`kill` 범위를 잘못 잡아 돌던 번역까지 죽였다.** 캐시 덕에 손실은 없었지만, 프로세스를 세울 때는 PID 를 하나씩 확인할 것.

**다섯 번째 — 《Rewired》(662쪽 순수 스캔본, 표가 많다)** 를 2026-08-16 에 걸었다. 문체는 **비즈니스(`--register business`, `-다` 평서형 — `--polite` 는 쓰지 않는다)**. 표를 그림으로 오려 내는 새 경로를 처음으로 전권에 태우는 실전이다.

```
작업 폴더   …/scratchpad/rwfull/        book.json · pages/ · pagecheck.log · pagecheck-report.md
번역 전 점검 …/scratchpad/rwfull/rewired-pretranslate.parallax
정본        D:\ebook\__output__\rewired.parallax
백업        D:\ebook\__output__\rewired.book.json
```

**번역 전에 `.parallax` 를 먼저 뽑아 앱에서 본다.** `spec.md` 가 「번역 0% 파일도 유효하다」고 정해 둔 것을 **돈 쓰기 전 검문소**로 쓴다 — 662쪽 전권의 표가 정말 그림이 됐는지, 목차 계층이 잡혔는지, 빈 쪽이 없는지를 export(공짜) 한 번으로 확인하고 나서 번역에 $6~10 을 쓴다. 지금까지 확인한 것은 3쪽뿐이었다. 앞선 네 권은 번역까지 끝낸 뒤에야 export 했는데, 그 순서는 판독이 잘못됐을 때 번역비를 통째로 버린다.

**네 번째 책 — `beyond_weird.parallax`**(Philip Ball, 『Beyond Weird』, 2026-08-09). 175쪽 순수 스캔본을 Tesseract 없이 datalab 전 쪽 판독으로만 변환했다(위 스킬 상태 참조). 1,120블록 · 번역 1,072 · 그림 37 · 제외 14(광고 쪽 12 + pagecheck 2) · 문체 newneek. 백업은 `beyond_weird.book.json`. 앱에서 CDP 로 실측 확인 — 목차 22항목, 그림이 좌우 대역에 각각 칸 폭 53%로 비율 유지, 가로 넘침 없음.

**두 번째 책의 정본은 2026-08-06 부로 `the_mind_is_flat.parallax`(그림 판본, 875블록 · 번역 816)다** — 위 08-06 세션 참조. 아래 노랑 판본들은 그 전사(前史)다. `the_mind_is_flat.figures.parallax`(08-06 12:27) 는 중단 시점의 체크포인트라 지워도 된다.

**그 전 판 — `the_mind_is_flat.noads.parallax`**(Nick Chater, 『마음은 평평하다』, **이 PC 에 없음 — johnb PC 소재**). 광고를 걷어낸 PDF 244쪽에서 전 공정을 다시 돌린 결과였다. 추출 1,521블록·이음 2,635·거름 269 → pagecheck 1,492블록(유형 교정 326, 병합 40, **본문 오삭제 0**) → 용어집 77 → 번역 1,117/1,117(뉴닉체) → deslop 전량(강도 `약` 에서 손댄 것 11) → 내보내기. 검증: 문장 중간 끊김 0, 번역 대상에 남은 도판 쓰레기 0. 문체 확인 — 845단락 중 **`-다` 로 끝나는 것 0개**, 해요체 635, 문두 입말 접속사 118. md 스냅샷은 `the_mind_is_flat.noads.{en,ko}.md`.

그 앞선 판(`the_mind_is_flat.parallax`, 광고 포함)은 사후 수리한 것이다 — 1569→1391블록(중복 삽입 97 삭제, 81쌍 병합, 잘못된 삭제 234 복원, 같은 쪽 중간 끊김 157→0), 이어서 미번역 208개를 채웠다(기존 901개는 바이트 단위로 그대로). `repair.py` 가 여기서 나왔다.

첫 작업 파일은 **`the_meaning_of_your_life_compressed.parallax`** 다(945블록, 번역 전량, 쪽 번호·플래그 온전 — 챕터 3 수리 직후는 948이었고 이후 중복 잔해 3블록 삭제로 945). 2026-07-28 오후에 사용자가 v2 에서 개명했다. 같은 날 손본 것: 챕터 7 자기평가 문항 5~8 유형 교정(PDF 151쪽 대조)·중복 잔해 블록 삭제, 번역 앞 `h2 ` 토큰 제거, 챕터 3 문항 구간 수리(위 참조). `*.en.md`/`.ko.md`(13:50)는 백업 — 지우지 말 것. 재건 잔재 `the_meaning_of_your_life_v2.parallax` 는 2026-07-30에 지웠다(내용이 md 스냅샷 재건이라 스냅샷이 남아 있는 한 잃는 것이 없다 — 쪽 번호 0개가 그 증거였다).

시험 산출물 `pagecheck_test_20p.pdf`(원본 앞 20쪽 발췌, 2026-07-30 재생성)와 앱 데이터 폴더의 `pagecheck_test_20p_pdf_*.parallax` 는 pagecheck 재검증용으로 남겨 뒀다.

2026-07-27에 정리하면서 구 추출본(`the_meaning_of_your_life.parallax`, 1,141블록·번역 943개)과 `README.parallax` 시험 산출물, DRM PDF, 스킬 백업 zip을 지웠다.

구 추출본에 번역이 943개 있었지만 블록 ID가 달라 v2로 옮길 수 없었다. v2는 2026-07-27에 앱 스케줄러로 956개 전량을 새로 번역했다(실측 단가 $0.002/블록, 총 $2 안팎). 빈 번역 0개, 접미사 ID 블록 88개도 전부 채워졌다. 한글이 없는 번역 3건은 판권면·인쇄 코드·OCR 조각으로 의도된 결과다.

`.parallax`와 `.pdf`는 `.gitignore`에 있다 — 원문이 전량 들어가고, `.parallax`는 번역이 진행될 때마다 통째로 바뀌는 SQLite라 히스토리에 넣으면 급격히 커진다.

---

## 인쇄된 목차를 정답지로 삼는다 (2026-08-17)

**문제.** 판독기는 쪽을 하나씩 따로 읽으므로 같은 종류의 제목이 실린 쪽에 따라 다른 레벨을 받는다. 그래서 예전 `normalize_headings` 는 아예 두 단(장 / 나머지)으로 눌러 버렸다. 그러면 `quantum_mechanics` 처럼 「Lecture 1 → 1.1 → 1.9.1」로 세 단인 책이 통째로 평평해진다 — 164개 제목이 거의 전부 h2 였다.

**바꾼 것.** 거의 모든 단행본은 앞머리에 Contents 를 싣는다. 거기엔 판독기가 알 수 없는 것 둘이 들어 있다 — **어느 제목이 장인지**, 그리고 **무엇이 애초에 제목인지**. 그것을 거둬서 정답지로 쓴다(`extract.py`).

| 함수 | 하는 일 |
|---|---|
| `toc_pages` | Contents 표제가 선 쪽부터 **항목처럼 생긴 줄로 채워진** 연속 쪽까지 |
| `toc_key` | 수사·쪽번호를 떼고 글자만 남겨 목차 항목과 본문 제목을 맞댄다 |
| `harvest_printed_toc` | `(최상위 열쇠, 하위 열쇠, 목차 쪽)` |
| `_demote_figure_titles` | 그림틀 **안에** 인쇄된 제목을 본문으로 내린다 |
| `_compact_levels` | 스택으로 조상을 좇아 건너뛴 단을 메운다 |

레벨은 이 순서로 정한다: **별행 번호**(`1.1`→h2, `1.9.1`→h3) → **정답지 최상위**(h1) → **정답지 하위**(h2) → 수사/부속물(h1) → **목차에 없음**(h3, 목차보다 잔 것) → h2.

**밟은 함정 다섯.** 전부 실측으로 걸렀다.

1. **목차 자체가 평평하지 않다.** `rewired`·`the_human_agent_orchestrator` 는 `SECTION ONE:`/`PART I:` 아래에 들여쓴 하위 항목을 단다. 들여쓰기 좌표는 이 단계에 없으므로(블록에 bbox 가 없다) 글로만 가른다 — 번호가 붙었거나 부속물이면 최상위.
2. **번호가 없는 목차는 「전부 하위」가 아니라 「평평」이다.** `beyond_weird` 는 장 20개가 죄다 번호 없는 문장형 제목이라 하위로 몰리고 Notes·Index 넷만 최상위가 됐다 — 책에서 장이 사라졌다. 번호 붙은 항목이 **3개 미만이면** 전부 최상위로 본다.
3. **「다음 쪽에 제목 3개 이상」으로 목차 쪽을 이어 붙이면 재실행이 안 된다.** 한 번 정규화한 책은 항목이 이미 본문으로 내려가 있어 둘째 쪽부터 끊긴다(`the_human_agent_orchestrator` 27줄 중 5줄만 거둬졌고, 그 부실한 정답지 탓에 멀쩡한 제목이 그림 제목으로 몰렸다). **유형을 보지 말 것.**
4. **「바로 다음이 그림」만으로 강등하면 안 된다.** 도판을 데리고 있는 진짜 절 제목까지 걷어낸다(`rewired` 의 `SECTION ONE …`). **설명(figcaption)까지** 요구해야 한다.
5. **단 접기는 이웃만 보면 안 된다.** 「앞 제목보다 한 단만」으로 당기면 같은 줄의 형제가 갈린다(`no_self` 는 절 51개 중 첫 하나만 h2 로 올라오고 40개가 h3 에 남아 톱니가 됐다). 스택으로 열린 조상을 좇을 것.

**결과.** 정본 여덟 권 전량에 반영했다(2026-08-17).

| 책 | 전 | 후 |
|---|---|---|
| beyond_weird | h1 2 · h2 23 | **h1 22** · h2 3 |
| no_self_no_problem | 22 / 51 | 24 / 49 |
| quantum_mechanics | 25 / 110 / 27 | 28 / 99 / 35 |
| rewired | 114 / 645 | 115 / 294 / 350 |
| signals | 11 / 58 / 249 | **51** / 267 |
| the_human_agent_orchestrator | 34 / 269 | 30 / 262 / 11 |
| the_meaning_of_your_life | 22 / 78 | 26 / 74 |
| the_mind_is_flat | 7 / 27 / 36 | **35** / 35 |

`quantum_mechanics` 목차가 책의 인쇄 Contents 와 일치한다 — `Lecture 1 · Systems and Experiments` → `1.1`…`1.9` → `1.9.1`…`1.9.5`. 그림틀 안 글자였던 `Complex Numbers`(p33, 그림 1.6)와 `Space of States for a Single Die`(p28)는 본문으로 내려갔다.

`src/shared/headings.ts` 의 `LABEL_EN` 에 `lecture` 를 넣었다 — 없으면 「Lecture 1」과 제목이 목차에서 안 합쳐진다.

> **`.parallax` 를 갈아 끼우기 전에 앱을 닫아라.** 앱이 연 책에는 `-wal` 이 붙는데, 파일만 바꾸면 **묵은 WAL 이 옛 내용을 덮어씌워 보여준다**. 이번에 두 번 속아 「내보내기가 안 먹었다」고 오판했다(갓 내보낸 원본을 직접 열어 보고서야 알았다). 앱을 정상 종료하면 체크포인트가 돌아 WAL 이 0바이트가 된다.

## 번역이 지어내는 마크다운 굵게 (2026-08-17)

원문에 `**` 가 **한 블록도 없는데** 번역에는 여덟 권 합쳐 110블록에 들어 있었다. 리더는 마크다운을 해석하지 않으므로 별표가 글자 그대로 화면에 박힌다. `translate.py` 의 `strip_invented_bold()` 가 타입 토큰을 벗기는 자리에서 함께 걷어낸다 — **원문에 그 표시가 있으면 손대지 않는다**(그때는 지어낸 것이 아니고, 수식 속 별표를 건드릴 위험도 그 경우에만 있다). 정본 여덟 권은 소급 적용했다.

`deslop.py` 는 자리가 아니다 — LLM 기반이고 **개조식 책은 통째로 건너뛴다**(그런데 `quantum_mechanics` 25건, `the_meaning_of_your_life` 6건처럼 개조식에도 나온다).

## 장별 주석을 각주로 묶는다 (2026-08-17)

`rewired` 는 장 끝마다 출처를 다는 조판이라 **「Note」가 37개 제목으로 올라와** 있었고 레벨이 **h1·h2·h3 뒤죽박죽**이라 목차 위계를 망가뜨렸다(h1 47 → 25). 주석 본문도 번호마다 문단이 갈려 본문과 같은 무게로 읽혔다.

`fold_chapter_notes()` 가 셋을 함께 고친다 — 제목을 `footnote` 로 내려 **목차에서 빼고**, 뒤따르는 번호 항목을 같은 블록에 「Note: 내용」 꼴로 묶고(여럿이면 줄로 나눈다), 리더가 `footnote` 를 **본문의 0.7배**로 그린다. 실측(렌더러 안에서 잼): 본문 25.33px / 각주 17.73px = **0.700**, 매달린 들여쓰기 −20.4px.

**뒷부속의 「Notes」 표제는 건드리지 않는다** — 목차에 남아야 하는 절이다. 가르는 기준은 **개수**다: 장마다 다는 책은 열 개 넘게 나오고(rewired 37), 뒷부속 표제는 책에 하나뿐이다(나머지 세 권이 각각 1개).

> **리더의 「모든 유형은 같은 글자 크기」 원칙에 예외를 하나 두었다.** 인용문·도판 설명은 그대로 본문 크기이고 각주만 0.7em 이다. `em` 이라 조판 슬라이더를 그대로 따라가고 `--blockgap` 의 `1em` 도 함께 줄어 여백이 글자에 비례한다.

**밟은 것 하나 — `_compact_levels` 가 숨긴 제목까지 세고 있었다.** 인쇄 목차의 「Contents」 표제는 h1 인 채로 감춰지는데 그것이 스택을 차지해 뒤따르는 h2 가 깊이 2 에 머물렀다. 그래서 **앱 목차가 h1 없이 h2 로 시작했다**. 이제 `dropped` 는 건너뛴다 — 여덟 권 모두 첫 항목이 h1 이다.

## 배경 작업은 껍데기가 죽어도 본체가 산다 (2026-08-18)

비즈니스체 번역을 돌리다 세션이 끊겼다. 하니스 알림은 「중단」이라고 했지만 **`translate.py` 프로세스는 계속 돌고 있었다** — 45초에 1 chunk 씩 진행하는 것을 실측으로 확인했다. 그 상태에서 재실행했다면 같은 `book.json` 을 둘이 쓰며 서로의 결과를 덮었을 것이다.

**재실행 전에 PID 를 확인한다.** 명령줄까지 봐야 무관한 python 과 구분된다.

```powershell
Get-CimInstance Win32_Process -Filter "Name='python.exe'" |
  Where-Object { $_.CommandLine -match 'translate\.py|deslop\.py|export\.py' }
```

**중단에는 순서가 있다. 감시 스크립트(부모)를 먼저, 파이프라인(자식)을 나중에.** 자식만 종료하면 부모의 재시도 loop 가 곧바로 다시 spawn 한다. 실제로 이 순서를 어겨 `translate.py` 두 개가 **6초 간격으로** 떴다(18:37:52, 18:37:58). 하니스의 `TaskStop` 은 자기가 추적하는 셸만 종료하고 **떨어져 나간 bash 자식은 살려 둔다** — `run.sh`·`resume.sh`·`chain.sh` 셋이 각각 살아 있었다.

**그래도 데이터는 무사했다.** `_llm.save_book()` 이 `.tmp` 에 쓰고 `replace()` 하는 원자적 쓰기라, 어느 시점에 종료해도 `book.json` 은 직전 chunk 상태로 온전히 남는다. 두 프로세스가 겹쳐 돌아도 최악이 「같은 chunk 를 두 번 결제」였다. 중단 후 확인: 블록 수 그대로, **중복 ID 0**, 원문 없는 번역 0, `.tmp` 잔재 없음. (`/F` 를 피하라는 이 저장소의 경고는 **Electron 앱의 설정 파일**에 대한 것이지 이 파이프라인이 아니다.)

**재실행 경로는 하나만 남긴다.** 감시 스크립트를 여러 개 두면 어느 것이 살아 있는지 알 수 없다. 옛 것을 삭제하고 `go.sh` 하나로 정리했다.

### 중복 실행 guard 를 만들며 두 번 실패했다

**명령줄 문자열로 셸을 식별하려 하지 마라.** 처음에는 감시용 bash 를 이름으로 찾으려고 패턴에 `go\.sh` 를 넣었는데, **스크립트 자신의 bash 가 self-match 해서 실행 자체가 차단됐다.** 그것을 빼도 여전히 오탐이 났다 — 그 이름을 문자열로 품은 아무 셸이나(guard 를 호출하는 셸 자신을 포함해) 매칭된다.

결론은 **python 명령줄만 본다**는 것이다. `Name='python.exe'` 로 먼저 거르므로 그 이름을 언급하는 셸은 걸리지 않는다. 세 경우로 시험했다 — 아무것도 없을 때 통과, 무관한 python 이 돌 때 통과, 파이프라인 python 이 돌 때 발동.

### `translate.py` 의 재개는 캐시가 아니라 재-chunking 이다

재실행 비용을 「캐시 적중」으로 설명하면 틀린다. `translate.py` 는 재실행 때 **아직 비어 있는 블록만 골라 chunk 를 다시 구성한다**(`if not args.force: blocks = [b for b in blocks if not (b.get("ko") or "").strip()]`). 이미 채워진 블록은 요청에 실리지 않으므로 재결제가 없다.

대신 **chunk 경계가 달라져 기존 캐시는 무효**가 된다. 남겨도 해는 없지만 적중하지도 않는다.

재실행이 실제로 보낼 양은 API 호출 없이 `L.translatable` + `L.chunk_blocks` 로 그대로 재현해 미리 계산할 수 있다. 이번에 그렇게 재서 mfbiz 238블록/29chunk/$1.34, sigbiz 1,716블록/110chunk/$4.38 을 얻었다.

### 중단 시점의 상태

`the_mind_is_flat` 와 `signals` 의 **비즈니스체 판본**을 만드는 중이었다. 개조식 판본과 같은 원문·같은 glossary 에서 갈라져 나오도록 `book.json` 의 `ko`·`ko_raw` 만 비우고 `meta.register` 를 바꾸는 방식이다(개조식 판본 셋을 만들 때 쓴 절차와 같다).

| 작업 폴더 | 책 | 번역 | 남은 것 |
|---|---|---|---|
| `scratchpad/mfbiz` | the_mind_is_flat | 588 / 826 (71%) | translate 잔여 → deslop → verify → export |
| `scratchpad/sigbiz` | signals | 0 / 1,716 | 전부 |

산출물은 기존 개조식 판본을 그대로 두고 `<책>.business.parallax` 로 나란히 둔다(사용자 지시). 이어받기 문서와 스크립트는 `scratchpad/RESUME-비즈니스체-번역.md` 와 `scratchpad/go.sh`.

> **`export.py` 는 md 를 내지 않는다**(앱으로 옮긴 기능이다). `scratchpad/mfbiz/to_md.py` 를 만들어 두었다 — 앱의 `renderMarkdown()` 을 그대로 옮긴 것이라 머리글이 리더 목차와 어긋나지 않는다. 영문으로 돌려 검증했고, 머리글이 68→54로 준 것은 회귀가 아니라 이번 세션의 목차 교정이 반영된 결과다.

## Datalab API 가 실제로 무엇을 주는가 (2026-08-17, 실측)

지금까지 `output_format=chunks` 하나만 써 왔다. **API 에 직접 물어** 무엇이 더 있는지 확인했다(잘못된 값을 보내면 검증 오류로 허용 목록을 돌려주고, 그건 무료다).

```
[epub] 400  Invalid output format. Each format must be one of 'json', 'html', 'markdown', or 'chunks'.
```

**EPUB·XML 은 없다. 네 가지뿐이다.** 그리고 알아낸 것 다섯.

**① 네 형식을 한 번에, 같은 값으로 받는다.** `output_format=json,html` 로 보내면 응답에 `chunks`·`json`·`markdown`·`html` 이 전부 담겨 온다. 비용은 그대로다. 지금 우리는 `chunks` 만 받으며 나머지를 버리고 있다.

**② 응답에 실제 과금이 온다** — `total_cost` · `cost_breakdown: {final_cost_cents: 1.0}`. 우리 코드는 쪽당 1¢ 를 **추정**해 찍는데, 이 값을 그대로 쓰면 어림이 아니라 사실이 된다.

**③ PDF 를 통째로 보낼 수 있고 문서 단위 출력이 온다.** 4쪽 조각으로 시험: `html` 7,243자 · `markdown` 6,602자가 쪽을 넘어 이어지고, `chunks` 가 `{blocks, page_info, metadata}` 로 온다. **블록마다 `page`+`bbox` 가 그대로 있고**(27/27) `page_info` 에 쪽 크기가 온다 — 즉 **우리가 쪽 이미지를 렌더할 필요가 없어진다.** 4쪽에 3¢ 로 쪽당 1¢ 보다 싸다.

**④ html 에 `<sup>1</sup>` 각주 마커가 살아 있다.** 「구조 인식 할 일 ③ 각주 cross-link」가 「본문 쪽 위첨자가 없다」로 막혀 있었는데, **원인이 이것이었다** — 마커는 오고 있었고 우리가 `chunks` 만 받아 html 을 벗기며 버렸다. `Caption` 도 별도 유형으로 온다.

**⑤ `section_hierarchy` 가 chunk 마다 온다**(`{'1': '/page/0/SectionHeader/1', …}`). 판독기가 본 절 구조다. 지금 통째로 버린다 — 인쇄 목차 정답지와 대조해 볼 값이 있다.

> **다만 줄·낱말 좌표는 어디에도 없다.** `json` 트리는 `Page → 블록`이고 블록의 `children` 이 **0** 이다. `<br>` 도 없다. 이것이 아래 검색 레이어 작업의 제약을 규정한다.

**옮겨 붙이기의 걸림돌 둘.** 662쪽을 한 번에 보내면 타임아웃·부분 실패 복구가 어렵다(지금은 쪽 단위 캐시·재시도라 견고하고, 그 견고함이 34쪽을 되살릴 때 값을 했다) — **20~50쪽 묶음**이 현실적이다. 그리고 블록 ID 체계가 바뀌면 **기존 번역 캐시가 무효**가 되니 정본 여덟 권은 손대지 말고 **새 책부터** 적용해야 한다.

## Tesseract 대 vlmparse — 같은 쪽을 두 엔진으로 (2026-08-17)

사용자 지시로 `quantum_mechanics` 앞 10쪽을 두 엔진으로 읽어 비교했다. 앞 10쪽에 수식이 없어(앞부속) 수식 밀집 3쪽(p37~39)을 더했다. 전문은 **`D:\ebook\__output__\ocr-compare\README.md`**.

| | Tesseract | vlmparse |
|---|---|---|
| 10쪽 시간 | 렌더 4.7초 + OCR **18.0초** | **234.5초**(쪽당 20초) |
| 돈 | 0원 | 쪽당 1¢ |
| 재실행 | 다시 다 돈다 | 캐시로 0초·0원 |
| 줄끝 하이픈 잔존 | **9** | 0 |
| 러닝헤드 섞임 | **9** | 0 |
| 블록 유형 | 없음(평문) | `h1`~`h3`·`p`·`equation`·`figure` |
| 별행 수식(3쪽) | **0** | 8 (LaTeX 명령 63) |

**글자 수가 많은 쪽이 좋은 것이 아니다.** Tesseract 가 750자를 더 냈는데 낱말 단위로 갈라 보면 판단이 뒤집힌다 — p2 에서 Tesseract 에만 있는 23낱말이 `aaa`·`eee`×3·`faite`·`nee` 같은 **잡음**이었고 vlmparse 에만 있는 것은 `physics` 하나였다. p6 은 `pur`·`pose`·`sup`·`port` 대 `purpose`·`support` — **조각 대 복원**이다. **품질은 글자 수가 아니라 낱말 집합의 차이로 재라.**

**수식에서 승부가 갈린다.** Tesseract 는 별행 수식을 `4 YAG'E`·`(V9 Z(X9` 로 뭉개고 다섯 차원 열벡터는 **통째로 빠뜨렸다**(「이렇다」 다음에 그것이 없다). 브라켓도 `|A)`·`⟨B|A⟩`→`(B|A)` 로 소괄호가 됐다. vlmparse 는 행렬 환경(`\begin{pmatrix}`)으로 온다.

이 저장소의 정본이 vlmparse 로 만들어진 것이 맞은 판단이었다. **Tesseract 가 맞는 자리는** 수식·표 없는 산문책, 원문을 밖으로 낼 수 없을 때, 예산 0 일 때다.

## 검색 가능한 PDF — 좌표는 Tesseract, 글자는 vlmparse (2026-08-17)

원본 스캔본에 **투명 텍스트 레이어**를 넣어 검색·복사가 되게 만들었다. 시제품과 실측은 `spikes/searchable-pdf/` 와 위 `ocr-compare/README.md`. 산출물은 `ocr-compare/qm_hybrid.pdf`(투명) · `qm_hybrid_debug.pdf`(넣은 글자를 초록으로 보이게 한 대조판).

**1차 시도는 실패했다.** vlmparse 블록 사각형 안에 글을 다시 흘렸더니 낱말이 문단 안에서 표류했다 — **IoU 중앙값 0.00 · 중심 거리 300pt.** 원인은 vlmparse 가 문단을 한 줄로 흘려 주어 **인쇄된 줄바꿈을 알 수 없다**는 것이다. 사용자가 화면에서 「대부분 원본 글자 아래로 되어 있다」고 지적했고, 재 보니 아래로 밀린 상수 편차가 아니라 **±3줄로 흩어진** 것이었다 — 오프셋 하나로는 못 고친다.

**해결은 사용자가 제안한 하이브리드였다.** 좌표는 Tesseract 낱말 상자, 글자는 vlmparse. `difflib.SequenceMatcher` 로 두 낱말 열을 정렬해 배정한다(`mech-`+`anics` 두 상자에 `mechanics` 하나를 넣고 남는 상자는 비운다. Tesseract 에만 있는 잡음은 버린다).

| | 블록 흘림 | 하이브리드 |
|---|---|---|
| IoU 중앙값 | 0.00 | **0.59~0.60** |
| IoU ≥ 0.5 | 0% | **90~95%** |
| 중심 거리 중앙값 | 287~344pt | **8.5~10.9pt** |
| x 편차 | −54~−88pt | **±0.0pt** |

글자 품질은 vlmparse 그대로다(하이픈 조각 0 · 러닝헤드 0 · 그리스 α 10). **렌더는 13/13 쪽 원본과 픽셀 동일**하고 파일 크기도 안 늘었다.

**밟은 함정 셋.**

1. **내장 폰트 `helv` 는 줄표를 `?` 로 깨뜨린다**(`Niels Bohr?Bohr`). WinAnsi 밖이다. `seguisym.ttf` 로 해결(한글은 없다 — 번역 레이어에는 다른 폰트가 필요하다).
2. **글자 크기를 상자 폭에만 맞추면 글리프 상자가 위로 부푼다.** 짧은 낱말이 넓은 상자에서 거대해져 위끝이 13pt 솟았다. 높이로도 눌러야 한다.
3. **`insert_textbox` 는 넘치면 음수를 돌려주고 아무것도 넣지 않는다** — 또 하나의 조용한 누락이다. 반환값을 보고 크기를 줄이며 다시 시도할 것.

> **판정 척도를 잘못 골라 한 번 헤맸다.** 처음엔 「상자 위끝 y 좌표 차이」로 쟀는데, 잉크 상자(Tesseract)와 글리프 상자(PDF 폰트 메트릭)는 애초에 높이가 달라 상수 −13pt 가 나온다. **겹침(IoU)** 이 맞는 척도다.

**남은 결함 둘** — 배정 경계에서 낱말 하나가 떨어진다(p9 이 `Albert` 없이 시작). 블록 사각형 안에 **중심이 든** 낱말만 쓰는 규칙 탓에 경계에 걸친 줄이 버려진다(`dropped` 19건). 둘 다 구조적 한계가 아니라 배정 규칙 문제다.

## 데이터를 고쳐 놓고 코드를 안 고치는 함정 (2026-08-17)

이번 세션의 작업 방식은 「함수를 짜서 임시 스크립트로 정본 여덟 권에 먹인다」였다. 그러다 보니 **정본은 멀쩡한데 파이프라인에는 그 처리가 없는** 상태가 두 번 생겼다 — `merge_label_subtitles` 와 `localize_bare_labels` 는 임시 스크립트로만 돌렸다. 새로 판독하는 책은 그 처리를 못 받는다. 사용자가 「코드 수정 완료된거임?」이라고 물어서야 드러났다.

**정본에 먹인 함수는 반드시 호출부까지 확인할 것.** 확인은 한 줄이다.

```bash
grep -n "함수이름" scripts/extract.py scripts/pagecheck.py scripts/translate.py
```

정의만 나오고 호출이 안 나오면 안 걸린 것이다. 지금 걸려 있는 순서:

```
extract·pagecheck   merge_label_subtitles → canonical_label_headings →
                    normalize_headings → split_lists_in_blocks → drop_printed_toc
normalize_headings  demote_printed_toc · _demote_figure_titles · demote_back_matter ·
                    demote_front_cover · (레벨 판정) · _compact_levels
translate           strip_invented_bold → localize_bare_labels → split_ko_lists
```

**순서가 뜻을 가진다.** `canonical_label_headings` 가 `normalize_headings` 앞이어야 한다(붙은 수사를 떼기 전에는 장으로 안 읽힌다). `split_ko_lists` 는 맨 끝이어야 한다(모델에게는 한 줄로 받고, 개조식 종결 교정도 ko 를 한 줄로 되돌린다).

## 빈 쪽 — 판독이 조용히 빠뜨린 쪽들 (2026-08-17)

**판독은 실패한 쪽을 알려 주지 않는다.** 정본 여덟 권을 훑어 보니 **34쪽이 통째로 비어** 있었다. rewired 는 15쪽(p153·536~543 …), the_mind_is_flat 9쪽, the_human_agent_orchestrator 7쪽, quantum_mechanics 5쪽, the_meaning_of_your_life 2쪽, signals 1쪽. 빈 쪽인가 싶어 원본을 렌더해 보니 **p153 에는 「CHAPTER 8」 제목과 본문 네 문단이 그대로 있었다.**

이 때문에 rewired 는 39장 중 **8·34장이 목차에서 사라져** 있었다. 34장은 제목만이 아니라 **본문 여덟 쪽**이 없었다.

**찾는 법.** 쪽 번호에 구멍이 있는지 보면 된다 — 한 줄이다.

```python
have = {r[0] for r in db.execute("SELECT DISTINCT page FROM block WHERE page IS NOT NULL")}
missing = [p for p in range(1, max(have) + 1) if p not in have]
```

**고치는 법.** `pagecheck.py --pages` 로 그 쪽만 다시 읽고, 새 블록만 번역한 뒤 다시 내보낸다. 쪽당 Datalab 1¢ 정도이고 캐시가 나머지를 공짜로 넘긴다. 이번 지출 **약 $0.75**(재판독 34쪽 $0.34 + 번역·deslop $0.41).

`book.json` 이 없어도 된다 — **`parallax_import.py` 가 `.parallax` → `book.json` 을 무손실로 되돌린다.** the_meaning_of_your_life 를 그렇게 고쳤다.

> **남은 하나.** the_mind_is_flat p239 는 다시 읽어도 빈다 — 원본을 보니 **색인 쪽**이다. 번역 리더에서 쓸 데가 없어 그대로 둔다.

### 여기서 돈을 새게 한 두 가지

**`glossary.json` 을 새 작업 폴더에 복사하지 않으면 용어집을 전권 재생성한다.** 10분을 넘겨 죽었다. 원본 작업 폴더에서 **반드시 같이 복사할 것.**

**`.parallax` → `book.json` 왕복에는 `ko_raw` 가 없다.** 그래서 `deslop.py` 가 **전권을 처음 보는 것으로 여기고 다시 돈다**(the_meaning_of_your_life 에서 945블록을 새로 돌기 시작해 240블록·22건을 고친 뒤 시간 초과로 죽었다). 되돌리려면 정본 `.parallax` 의 `ko` 를 다시 덮으면 된다 — 그렇게 복구했다. **쪽 몇 장을 보태는 경우에는 deslop 을 아예 걸지 말 것.**

## 수사 붙은 제목을 「수사 · 제목」 한 꼴로 (2026-08-17)

원본 조판이 제각각이다 — 붙은 것(`CHAPTER 2THE LAST GENERATION`), 콜론(`Chapter 1: Meet the Interpreter…`), 줄표(`Part I — Foundational…`). **원본 형식을 무시하고** `수사 · 제목` 하나로 맞춘다(`canonical_label_headings`, 구분자는 `LABEL_SEP = " · "` — 리더의 `MERGE_SEP` 과 같다). 목차뿐 아니라 **블록 글 자체**를 바꾸므로 본문에서도 같은 꼴로 보인다. 번역문에도 먹인다(`제2장 마지막 세대` → `제2장 · 마지막 세대`).

**가르는 자리는 인쇄 목차가 정한다.** 글자만 보고는 못 가른다 — 로마자·영문 서수가 제목 첫 글자를 삼킨다. `PART IIDIFFERENT PLAYERS` 를 탐욕 매칭하면 **D 도 로마자라** `PART IIDI` + `FFERENT PLAYERS` 로 끊긴다. 후보를 전부 내놓고 **뒤쪽 열쇠가 목차 항목과 맞는 것**을 고른다. 맞는 것이 없으면 원래 띄어쓰기가 있던 자리로 물러선다 — 근거 없이 자르지 않는다.

**밟은 함정 셋.**

1. **번역만 따로 보면 안 된다.** `signals` 의 `Note` 가 「2장 주석」으로 옮겨져 있어 원문에 장 번호가 없는데도 「2장 · 주석」이 됐다. **원문이 갈렸을 때만** 번역을 손댄다.
2. **맨 낱말 수사는 붙어 있을 때만.** `INTRODUCTIONTHE PERFECT AI …` 는 갈라야 하지만, `FOREWORD BY MARSHALL GOLDSMITH` 를 같은 규칙에 넣으면 「FOREWORD · BY MARSHALL GOLDSMITH」가 된다 — 뒤가 제목인지 딸린 말인지 가릴 방법이 없다. 이미 콜론·공백으로 갈려 있는 것은 손대지 않는다.
3. **순서.** `canonical_label_headings` 가 `normalize_headings` **앞**에 와야 한다. 붙어 있으면 레벨 판정이 그것을 장으로 못 읽는다.

목차 항목이 맨 낱말 수사를 달고 있으면(`INTRODUCTION: The Perfect AI …`) 그 수사를 뗀 열쇠도 정답지에 넣는다 — 본문은 붙어 오므로 뗀 것끼리 맞대야 자리가 잡힌다.

**결과.** 여덟 권 55블록. `the_human_agent_orchestrator` 는 목차 대조 누락이 **5건 → 1건**(의도적으로 숨긴 `CONTENTS` 뿐)으로 떨어졌다 — PART I~IV 넷이 `PART IFROM SUPERVISING…` 꼴로 붙어 있어 통째로 빠져 있었다.

## 인쇄된 목차를 본문에서 뺀다 (2026-08-17)

앱에는 목차가 이미 왼쪽에 있다. 본문에까지 실리면 같은 것을 두 벌 읽게 되고, 대역 리더에서는 그 목차까지 번역돼 돈이 나간다. `rewired` 는 **p2–p17, 372블록 열여섯 쪽**이 통째로 목차와 도판 목록이었다.

**지우지 않고 `dropped` 만 세운다.** 예전에 「지우면 쪽 대조가 깨진다」고 남겨 둔 그 걱정이 여기서 풀린다 — 블록은 `book.json` 에도 `.parallax` 에도 그대로 남고, `DROPPED`(1<<5) 플래그가 **이미** 목차(`db.ts` 의 `VISIBLE`)·본문·번역 스케줄러·Markdown 내보내기 네 곳에서 걸러 준다. 되돌리려면 플래그만 내리면 된다. 새 장치를 만들 필요가 없었다.

**밟은 함정 둘.**

1. **표제지가 딸려 들어간다.** 목차 쪽 이어붙이기 문턱이 3줄이라 `rewired` 의 p18(「REWIRED」·부제·판권 세 줄, 모두 짧다)이 목차로 잡혔다. 강등만 할 때는 무해했지만 **빼는 순간 책 제목을 잃는다.** 문턱을 6줄로 올렸다 — 여덟 권 중 `rewired` 만 바뀌고(p2–18 → p2–17) 정확히 표제지만 빠진다.
2. **마지막 줄에 본문이 눌어붙는다.** 다음 쪽 첫 글이 목차 마지막 항목에 붙어 오는 일이 잦다 — `no_self` 는 「Notes and References」에 제사(*Bhagavad Gita*)가, 양자역학은 「Appendix」에 헌사가 붙어 있었다. **지우면 그 글이 사라진다.** 그 쪽 항목 길이 중앙값의 2.5배를 넘으면 본문이 섞인 것으로 보고 남긴다(여덟 권에서 3블록).

결과: 여덟 권 합쳐 **515블록**을 숨겼다. 본문 첫머리는 이제 추천사·표제·저자로 시작하고, 왼쪽 목차도 `Contents` 항목 없이 책 본체부터 선다. 남은 「contents」 문자열은 전부 본문 산문(「the contents of consciousness」 따위)임을 확인했다.

> 아직 지저분한 것 둘(이번 건과 무관, 예전부터): 양자역학의 표지 쪽 `QUANTUM MECHANICS` 가 h1 으로 셋 서 있고, `rewired` 는 `Library of Congress Cataloging-in-Publication Data is Available:` 이 h2 다.

## 도판 번호표와 붙어 버린 수사 (2026-08-17)

**「EXHIBIT 2.1」은 절 제목이 아니다.** `rewired` 는 이런 도판 번호표 74개가 본문 곳곳(p24–p551)에서 제목으로 올라와 목차를 채우고 있었다. 뒤따르는 유형이 제각각이라(`p,p` 41 · `figure,h3` 3 · `h3,p` 7 …) **위치로는 못 거른다** — 라벨 꼴 자체로 판정한다(`_FIG_LABEL_HEAD_RE`, 30자 이하). 지우지 않고 `figcaption` 으로 내린다: 도판을 가리키는 글이라 본문에는 남아 있어야 한다.

**번호와 제목이 공백 없이 붙으면 장으로 안 읽힌다.** `CHAPTER 2THE LAST GENERATION` 은 숫자 뒤가 글자라 `\b` 가 성립하지 않아 `CHAPTER_MARK_RE` 가 빗나간다. `the_human_agent_orchestrator` 의 **2·3·5·13·15·16·18·19장 여덟 개가 목차에서 통째로 빠져** 있었다. `unglue_label()` 이 원문과 번역 양쪽에서 뗀다 — 번역도 같이 붙는다(`챕터 13구축 이전에`, `제1부전환 로드맵 수립`).

> **한글 쪽 뒤보기를 `[^\s]` 로 두면 안 된다.** 콜론 앞에까지 공백이 들어가 `제1장: 해석자를…` 이 `제1장 : 해석자를…` 이 된다(실측: 무해한 22블록이 망가졌다). `[가-힣A-Za-z]` 로 낱자만 본다.

결과: `rewired` 의 EXHIBIT 제목 74 → **0**, `the_human_agent_orchestrator` 는 **1장부터 19장까지 빠짐없이** 선다. 나머지 여섯 권은 한 블록도 안 움직였다.

## 목록을 줄로 나눈다 (2026-08-17)

판독기는 불릿 목록을 **문단 하나로 접어** 준다 — 「• 첫째 … • 둘째 … • 셋째」가 한 블록에 붙어 오고, 리더는 줄바꿈이 없으니 통짜 문단으로 그린다. 정본 여덟 권에서 불릿 180블록·번호 목록 64블록이 그랬다.

**항목마다 블록으로 쪼개지 않는다.** 대역 리더는 블록끼리 원문·번역을 짝짓는데 번역이 항목 수를 그대로 지킨다는 보장이 없다(개조식은 특히 합치는 쪽으로 기운다). 한 블록에 담긴 채 **줄만 나눈다**.

- `extract.split_inline_lists` — 불릿 앞, 그리고 **번호가 연달아 이어질 때만** 번호 앞에 줄바꿈. 연속성을 안 보면 「… 1929년. 2. 차례로」 같은 본문 숫자를 목록으로 오인한다.
- `translate.split_ko_lists` — 번역 쪽. **맨 마지막에 돌려야 한다.** 모델에게는 「블록 하나는 한 줄」로 받고(그러지 않으면 `from_wire` 가 뒤 줄을 버린다), 개조식 종결 교정도 ko 를 한 줄로 되돌린다. **원문 쪽 줄 수와 맞을 때만** 손댄다 — 어긋난 줄바꿈은 없느니만 못하다.
- 리더 — 줄바꿈이 있으면 `setTextWithMath` 가 `.list` 를 붙이고 CSS 가 `white-space: pre-line` + **매달린 들여쓰기**를 준다. 매달린 들여쓰기가 없으면 넘친 둘째 줄이 글머리 기호 밑으로 파고들어 목록으로 안 보인다.

실측(signals 의 6항목 블록을 렌더러 안에서 잼): 적용 전 `white-space: normal` · 10줄짜리 한 덩어리 → 적용 후 `pre-line` · `text-indent: -29.1px` / `padding-left: 29.1px` · **13줄**. 정본 여덟 권에 소급 적용했다(원문 242블록, 번역 235블록 — 줄 수가 어긋난 7블록은 규칙대로 두었다).

> **`to_wire` 는 줄바꿈을 공백으로 눕힌다.** 그래서 목록이 갈린 `src` 를 보내도 전송 형식은 안 깨진다. 다만 `fix_nominal_endings` 는 wire 를 **직접 f-string 으로** 짓고 있었다 — 거기에도 같은 눕히기를 넣었다.

## 접두어 없는 수사 (2026-08-17)

본문 조판에서 「CHAPTER」를 빼고 서수만 큼직하게 얹는 책이 흔하다. `no_self_no_problem` 은 본문이 「ONE」과 「Meet the Interpreter—An Accidental Discovery」 두 블록인데 인쇄 목차에는 「Chapter One Meet the Interpreter…」 한 줄이다. 접두어를 요구하면 이런 책의 장 제목이 목차에서 **둘로 갈린다** — 게다가 「ONE」이 h2, 제목이 h1 이라 병합 조건(`next.level >= cur.level`)에도 안 걸렸다.

`extract._BARE_NUM_RE` 가 맨 수사를 h1 으로 올리고, `shared/headings.ts` 의 `LABEL_BARE` 가 병합 대상으로 인정한다. **숫자만 본다** — 「INTRODUCTION」 같은 맨 낱말은 뒤따르는 것이 그 부의 제목인지 첫 절인지 데이터로 구분되지 않아 넣지 않았다(기존 판단 그대로).

여덟 권에 돌려 `no_self_no_problem` 만 바뀌었다(수사 8개 → `ONE · Meet the Interpreter—An Accidental Discovery`). 나머지 일곱 권은 한 블록도 안 움직였다.

## 장 제목이 제 소제목을 데려간다 (2026-08-19)

사용자가 `the_human_agent_orchestrator` 에서 짚었다. 「The Question That Stopped
the Room」이 「CHAPTER 2 · THE LAST GENERATION」과 형제로 서 있었다.

범인은 `normalize_headings` 끝의 **부제 흡수** 규칙이다. 수사 표제 다음 제목을
부제로 보고 같은 단으로 끌어올리는데, 조건이 `CHAPTER_MARK_RE` 였다. 이 규칙은
「PART ONE」 / 「The Illusion of Mental Depth」처럼 **수사만 서고 제목이 다음
줄에 있는** 조판을 위한 것이다. 「CHAPTER 2 · THE LAST GENERATION」은 이미 제
제목을 가졌으므로 뒤따르는 것은 부제가 아니라 소제목이다.

조건을 `_is_bare_label()` 로 좁혔다. 수사 뒤에 글이 붙어 있으면 흡수하지 않는다.
열두 판본 회귀 검사에서 **이 한 블록만** 바뀌었다.

### 곁가지에서 더 큰 것이 나왔다

그 자리를 들여다보다 h2 104개 중 **85개가 인쇄 목차에 없다**는 것을 봤다.
그중에 이런 짝이 있었다.

```
h2  CHAPTER 1
h2  THE PROMOTION NOBODY ASKED FOR
```

장 하나가 목차 두 줄을 먹고 첫 줄은 뜻이 없다. 두 블록 병합(`merge_label_subtitles`)이
그동안 `INTRODUCTION` 같은 **낱말 수사**에만 걸려 있었기 때문이다. 번호 수사에도
같은 근거를 적용했다. 뒤따르는 글이 인쇄 목차에 실려 있을 때만 합친다.

| 책 | 병합 |
|---|---:|
| `rewired` | 38 |
| `signals` | 23 |
| `quantum_mechanics` | 10 |
| `the_meaning_of_your_life` | 9 |
| `no_self_no_problem` | 8 |
| `the_human_agent_orchestrator` | 5 |
| `the_mind_is_flat` | 2 |

### 병합이 부른 퇴행 두 건

**합치고 나면 정답지가 그 제목을 못 알아본다.** 「CHAPTER 1 · The Meaning of
Meaning」은 통짜로는 목차에 없다. 인쇄 목차가 수사를 빼고 제목만 싣기 때문이다.
그래서 멀쩡한 장이 「목차에 없는 것」으로 몰려 두 단 아래로 떨어졌다(`no_self` 의
장 열여섯, `the_mind_is_flat` 의 PART ONE/TWO).

고친 것 둘이다.

- 정답지와 맞댈 때 **수사를 뗀 열쇠도 함께** 본다(`keys` 집합).
- 부(部)는 목차가 보증하는 한 언제나 h1 이다. 부제를 합쳐 두면 그 부제가 목차에서
  하위 항목으로 실려 있어 `sub_keys` 갈래에 먼저 걸린다.

> **규칙을 넣었는데 회귀 검사에서 「변화 0」이 나오면 규칙이 안 걸린 것부터
> 의심할 것.** 앞선 세션에서 정규식의 `\b` 가 삽입 과정에 백스페이스 문자로
> 치환돼 패턴이 영원히 매칭되지 않은 적이 있다. `repr(pattern)` 으로 확인한다.

> **여러 줄 문자열을 heredoc 으로 넘길 때 한글·특수문자 앵커를 쓰지 말 것.**
> `strip(" .:·—–-")` 를 앵커로 잡았다가 인코딩 때문에 매칭에 실패했다.
> 앵커는 ASCII 로 잡고 `PYTHONUTF8=1` 을 준다.

여덟 판본을 다시 출력했다. 블록 수·번역 수·용어집·그림·`title_ko` 전부 그대로이고
번역은 손대지 않았다. 목차 두 줄짜리 장이 사라져 h2 가 `rewired` 96→58,
`signals` 64→44, `the_human_agent_orchestrator` 104→98 로 줄었다.

`beyond_weird` 는 h1 스물하나뿐인 평평한 목차인데 인쇄 목차가 거둬지지 않아서다.
이번 규칙으로는 닿지 않는다. **따로 봐야 한다.**

## `.parallax` 는 문체를 기억하지 못한다 (2026-08-19, 실측)

`.book.json` 을 지워도 되는지 판단하려고 왕복시켜 봤다.
`signals.business.parallax` → `parallax_import.py` → 원본 `book.json` 과 대조.

```
최상위 키    차이 없음 (assets · blocks · meta · page_check · superseded)
블록 필드    차이 없음
블록 1,718   id 차이 0 · src/ko/ko_raw/type/page 불일치 0
용어집       68 + 원문유지 16 함께 복원
```

**딱 한 군데가 어긋난다.**

```
meta.register   원본 business  →  복원 essay
```

`.parallax` 스키마에 문체 칸이 없어서 `parallax_import.py:62` 가 `"essay"` 를
박아 넣는다. **틀린 값이 빈 값보다 나쁘다.** `deslop.py:105` 가 그 값을 그대로
믿기 때문이다.

```python
register = book["meta"].get("register") or "essay"
if register in NO_DESLOP and not args.strength:   # NO_DESLOP = {"nominal"}
    ... 건너뛴다
strength = args.strength or STRENGTH.get(register, "중")
```

개조식은 deslop 을 **일부러** 태우지 않는다. 규칙표가 전부 `-다` 서술문을 전제로
쓰여 있어 명사형 종결을 번역투로 읽고 문장을 서술형으로 되돌리기 때문이다.
복원본은 `essay` 라 적혀 있으니 이 방어가 작동하지 않는다. 개조식 한 권이 통째로
서술형으로 되돌아가고, deslop 은 공짜도 아니다(`the_mind_is_flat` 한 권에 $5.54).

**아직 안 고쳤다.** 두 갈래가 있다.

- **파일명에서 읽는다** — 코드 몇 줄이고 스키마를 안 건드린다. 다만 접미사 없는
  `no_self_no_problem.parallax` 가 실제로는 무슨 문체인지 알 길이 없다.
- **`doc` 테이블에 컬럼을 더한다**(권함) — 파일명과 무관하게 정확하다. 스키마가
  세 군데(`pdf2parallax/_parallax.py` · 앱 `src/main/db.ts` · `spec.md`)에 적혀
  있어 전부 고쳐야 하고, 컬럼이 없는 기존 파일도 열려야 한다.

`.parallax` 를 정본으로 삼기로 한 이상 정본이 제 문체를 알아야 한다.


## 판독은 datalab 고정이다 (2026-08-20, 실측)

《The Conquest of Happiness》(191쪽, 전면 이미지 스캔)를 변환하다 vision 엔진이
**191쪽 중 119쪽에서 막혔다.** 응답은 매번 같다.

```
Error code: 400 — invalid_request_error:
  Output blocked by content filtering policy
```

모델 문제가 아니다. `PKT_VISION_MODEL` 을 haiku-4-5 에서 sonnet-4-6 으로 바꿔
같은 쪽을 다시 걸었더니 **똑같은 여섯 쪽이 똑같이 막혔다.** 입력이 전면 이미지
스캔이라 판독 출력이 원문 전재로 판정되는 것으로 보인다. 재시도로는 풀리지 않고,
막힌 쪽은 「vision unavailable — layer text kept」로 남아 OCR 텍스트 레이어가
그대로 통과한다. 즉 검증받지 않은 쪽이 62% 다.

`datalab` 로 같은 책을 다시 걸었다. 결과가 우회책이 아니라 **개선**이었다.

| | h1 | h2 | 상태 |
|---|---|---|---|
| vision 후 | 13 | 35 | 장 제목이 두 줄로 쪼개짐(「WHAT MAKES」/「PEOPLE UNHAPPY?」), 본문 문장 일곱이 h2 로 승격 |
| datalab 후 | 6 | 17 | 2부 + 17장 + 서문 + 표지 + 목차. 인쇄 목차와 정확히 일치 |

비용은 191쪽에 $1.91. vision 이 성공했을 때보다 비싸지만, 사람이 손으로 고칠
자리를 없앤 값이다.

**그래서 기본값을 바꿨다.** `pagecheck.py --engine` 기본이 `datalab` 이고,
`run.sh` 도 `ENGINE:-datalab` 이며 `DATALAB_API_KEY` 를 앞에서 요구한다.
`vision` 과 `tesseract` 는 지우지 않았지만 명시해야 골라지고, docstring 에
「쓰지 말 것」으로 적어 두었다. `vlmparse` 패키지 쪽은 원래부터 백엔드 등록소에
datalab 하나뿐이라 손댈 것이 없었다.

### 환경변수 이름이 갈라져 있었다

`pagecheck.py` 는 `PKT_ENV_FILE` 로 `.env` 를 찾는데, 그 안에서 부르는
`vlmparse.backends.datalab.find_key()` 는 `VLMPARSE_ENV_FILE` 만 봤다. 저장소 밖
작업 폴더에서 돌리면 `.env` 에 열쇠가 있어도 「DATALAB_API_KEY 가 없습니다」로
죽는다. vision 이 기본일 때는 드문 불편이었지만 datalab 이 필수가 된 이상 곧바로
실행 중단이다. `find_key()` 가 **두 이름을 다 보도록** 고쳤다.

고친 자리는 `vendor/vlmparse` 다. `pdf2parallax/scripts/vlmparse` 는 subtree 의
사본이라 `sync-vlmparse.mjs` 가 매번 덮어쓴다. 사본을 고치면 다음 재포장에서
조용히 사라진다(이번에 한 번 겪었다).

### 「블록 없는 쪽」 경고는 유실과 병합을 구분하지 못한다

datalab 이 끝나고 「블록이 없는 쪽 29개」가 떴다. 실제 유실은 **0** 이었다.
쪽 경계 단락을 136건 병합해서 뒤쪽 쪽에 블록이 안 남은 것뿐이다. 확인은
캐시(`work/.cache/pagecheck/`)의 쪽별 판독문을 8낱말 shingle 로 잘라
`book.json` 전문에 있는지 세는 방식으로 했다. 191쪽 전부 적중률 90% 이상,
미달 0. 경고 문구 자리에 이 대조를 넣으면 매번 눈으로 볼 일이 없어진다.



## 형광펜 — 가상 스크롤 위에 칠하기 (2026-08-20)

글을 긁고 `Alt+H`. 칠한 것은 오른쪽 패널에 읽은 순서대로 쌓이고, 항목마다 🗑 이,
패널 우상단에 「선택」이 있다. 원문 칸과 번역 칸은 서로를 모른다.

### 좌표를 DOM 으로 잡으면 안 된다

리더는 가상 스크롤이라 행이 스크롤할 때마다 `makeRow` 로 다시 만들어졌다 지워진다.
`<mark>` 로 감싸 두면 **스크롤 한 번에 사라진다.** 그래서 저장하는 것은
`(block_id, side, start_off, end_off)` 이고, 좌표는 `block.src`·`block.ko` 의
**문자 오프셋**이다. 칠은 늘 이 좌표에서 다시 그린다(`renderWindow` 끝에서
`paintHighlights()`).

칠에는 `CSS Custom Highlight API`(`CSS.highlights` + `::highlight()`)를 쓴다.
텍스트 노드를 쪼개지 않는 것이 핵심이다 — 쪼개면 아래의 오프셋 지도가 그 자리에서
무효가 되고 같은 블록의 다음 형광펜이 어긋난다. Electron 130 에서 확인했다.

### 수식이 낀 자리 — 오프셋 지도

`setTextWithMath` 가 `$…$` 를 KaTeX 로 그리므로 **DOM 글자수 ≠ 원문 글자수**다.
그 함수가 원문을 조각내며 순회하니 그 자리에서 지도를 같이 만든다(`el.__map`).

```
{ node, from, to }            보통 텍스트 — 가운데를 잘라 칠할 수 있다
{ node, from, to, atomic }    수식 — 통째로만. 안을 쪼개면 식이 사라진다
```

실측으로 지도가 원문을 빈틈없이 덮는지 본다(구간이 이어지는가, 합친 길이가
`block.src` 와 같은가). 1,967자 블록에서 통과.

### 번역은 바뀐다

`ko` 는 재번역·deslop 으로 다시 쓰인다. 그래서 조각마다 그 구간의 **사본**(`text`)을
같이 저장하고, 붙일 때 `blockText.slice(start,end)` 와 대조한다. 어긋나면 **지우지
않고** 목록에 「번역이 바뀌었습니다」로 흐리게 남긴다. 사용자가 만든 것을 앱이
말없이 버리지 않는다. `block:updated` 가 오면 다시 따진다.

### 논리가 무너지는 자리 넷

**① radio 로는 복수 선택이 안 된다.** 요청은 「radio button 이 떠서 복수 선택」
이었는데 radio 는 정의상 한 그룹에 하나다. checkbox 로 갔다.

**② 삭제 경로가 둘이면 답이 없다.** 항목별 🗑 과 일괄삭제가 같은 화면에 있으면,
체크해 둔 것이 있는데 다른 항목의 🗑 을 누를 때 무엇이 지워져야 하는지 정할 수
없다. 선택 모드에서 🗑 을 감춰 **모드가 두 길을 배타로** 만든다.

**③ 한 번 그은 것은 한 줄, 한 번에 삭제.** 세 단락을 긁으면 블록마다 조각이
생기지만 사용자에게는 하나다. `group_id` 로 묶는다.

**④ 겹치면 합집합 하나로.** 중첩을 허용하면 화면의 칠 하나에 목록 항목이 둘 붙어
🗑 을 눌러도 절반만 지워진다. 합칠 때 겹친 그룹을 통째로 지우지 않고 **다른 블록의
조각은 새 그룹으로 데려온다** — 세 단락짜리의 가운데만 덧그었다고 위아래가 사라지면
안 된다. 불변식: `(group_id, block_id, side)` 마다 조각 하나.

한 그룹이 두 칸에 걸치지 않도록도 막았다. 마우스로는 CSS 가 이미 막고 있지만
(`body.sel-src .cell.ko { user-select: none }`), 막아 주는 것이 다른 파일의 CSS
한 줄뿐이면 언젠가 뚫린다.

### `schema_version` 을 올리지 않았다

표를 더하는 것은 하위호환이다. 올리면 구버전 앱이 「더 새로운 형식입니다」로 파일
**자체를** 거부한다. 구버전은 `highlight` 표를 모른 채 무시하면 된다. 대신 `end` 가
SQLite 예약어(`CASE…END`)라 컬럼명은 `start_off`·`end_off` 다.

### 걸려 넘어진 것들

- **DDL 이 template literal 이다.** SQL 주석에 백틱을 쓰면 문자열이 그 자리에서
  끊긴다. `` `end` `` 라고 적었다가 파싱이 깨졌다.
- **`.hlpane .bulk { display: flex }` 가 `hidden` 속성을 이겼다.** UA 의
  `[hidden]{display:none}` 보다 클래스 선택자가 세다. 선택 모드가 아닌데도
  일괄삭제 바가 서 있었다. `.bulk[hidden] { display: none }` 을 명시해야 한다.
- **툴바 배지에 고정색을 주면 안 된다.** 패널이 열려 버튼이 accent 로 채워지면
  남색 위의 남색이 된다. `color: inherit` + `opacity`.
- **패널이 열리면 본문 폭이 바뀐다.** 목차와 똑같이 260ms 뒤 `invalidateHeights()`
  와 `placeHandle()` 을 불러야 한다. 안 하면 손잡이가 엉뚱한 자리에 선다.
- **Esc 는 안쪽부터.** `help → dict → 선택 모드 → 패널 → 조판 → 목차`. 선택 모드와
  패널을 함께 닫으면 체크를 물리려다 목록까지 잃는다.

### 시험

Electron 창을 띄워 진짜 문서에 진짜 선택을 만들고 `Alt+H` 를 눌러 확인한다
(스크래치패드의 `smoke.mjs` + `body.js`). 33항목 통과 — 지도의 빈틈, 잡힌 글이
긁은 글과 같은지, **실제로 칠해졌는지**(`CSS.highlights` 의 Range 가 그 행 안에
있는지), 겹침 병합, 두 칸의 독립, 선택 모드에서 radio 가 아닌 checkbox 인지,
🗑 이 감춰지는지, 일괄삭제와 개별삭제. DB 계층은 따로 21항목(`hltest.mjs`).

시험을 짜며 두 번 매달렸다. ① `doc:opened` 를 **기다린 다음에 보내** 서로를
기다렸다. ② `executeJavaScript` 가 던진 예외를 아무도 안 받아 조용히 멈췄다.
시험은 멈추더라도 **멈춘 채로 끝나야** 한다 — 하드 타임아웃을 걸어 뒀다.

`app.js` 는 `type="module"` 이라 내부 상태가 `executeJavaScript` 에서 안 보인다.
진단하느라 잠시 `window.__hl` 을 뒀다가, 시험을 공개 표면(`parallax.blocks.byIds`,
목록 항목 클릭)만으로 다시 쓰고 걷어냈다. 제품 코드에 시험용 훅은 남기지 않는다.

### 아직 안 한 것

Markdown 내보내기(`exporter.ts`)에 형광펜을 싣지 않는다. 별도 판단거리다.

## 열려 있는 것

### 새로 들어온 것 (2026-08-17 사용자 지시)

- ~~**목차 쪽을 본문에서 뺀다.**~~ — 위 「인쇄된 목차를 본문에서 뺀다」에서 끝냈다.
- ~~**불릿/번호 목록 렌더링**~~ — 위 「목록을 줄로 나눈다」에서 끝냈다(아래 ① 이 그것이다).
- ~~**장별 주석을 각주로**~~ — 위 「장별 주석을 각주로 묶는다」에서 끝냈다.

### 새로 들어온 것 (2026-08-19)

- **`.parallax` 에 문체를 담는다.** `doc` 테이블에 `register` 컬럼을 더한다. 위
  「`.parallax` 는 문체를 기억하지 못한다」참조. `.book.json` 을 다 지운 지금은
  `.parallax` 가 유일한 정본이므로 이것이 앞선다.
- **`the_meaning_of_your_life` 와 개조식 세 권을 휴지통에서 되살릴지 결정한다.**
  되살린다면 `.book.json` 셋은 `export.py` 로 `.parallax` 를 뽑아야 앱에서 열린다.
- **`beyond_weird` 의 평평한 목차.** 인쇄 목차가 거둬지지 않아 h1 스물하나뿐이다.
  `harvest_printed_toc` 가 왜 빈손인지부터 봐야 한다.

### 조사만 해 둔 것 — 착수 전 (2026-08-17)

셋 다 근거는 다 모았고 코드로 옮기는 일만 남았다. 순서는 아래가 곧 우선순위다.

**ⓐ 판독을 「PDF 묶음」으로 바꾼다.** 20~50쪽 단위로 PDF 를 통째 보내는 경로를 **기존 쪽 단위 경로 옆에** 추가한다. 얻는 것 넷 — 쪽 이미지를 우리가 렌더할 필요가 없어지고(dpi·긴 변 상한 고민이 사라진다), **`<sup>` 각주 마커**를 얻고(아래 ⓑ 의 선결 조건), `Caption` 이 별도 유형으로 오고, 비용이 조금 싸다. `output_format=chunks,html` 로 바꾸면 html 을 공짜로 함께 받고 `total_cost` 로 실지출을 로그에 남길 수 있다. **정본 여덟 권은 손대지 말 것** — 블록 ID 체계가 바뀌면 번역 캐시가 무효가 된다.

**ⓑ 각주 cross-link**(아래 ③ 과 같은 건). ⓐ 가 `<sup>` 를 가져오면 재료가 갖춰진다. 그때까지는 손댈 수 없다.

**ⓒ 검색 가능한 PDF 를 정식화한다.** `vlmparse` 에 `bbox` 를 산출로 남기고(지금 `chunks_to_blocks` 가 버려서 시제품은 캐시를 직접 뒤졌다) `vlmparse pdf-layer` 서브커맨드로 옮긴다. 시제품·실측은 `spikes/searchable-pdf/`. 남은 결함 둘(배정 경계에서 낱말 하나가 떨어지는 것, 블록 경계에 걸친 줄이 버려지는 것)을 함께 잡는다.

**EPUB 은 Datalab 이 안 준다**(형식은 `json`·`html`·`markdown`·`chunks` 넷뿐). 만들려면 우리가 만들어야 하고, 재료는 Datalab 의 html 보다 `.parallax` 가 낫다(영·한 두 벌 · 교정한 목차 계층 · 정제한 수식 · 내장 asset). 아래 Markdown 내보내기 두 건과 같은 자리를 건드리므로 묶어서 하는 편이 낫다.

### 구조 인식 — 할 일 다섯 (2026-08-16 사용자 지시)

**공통.** 대개 **판독 계층이 먼저**다. 손댈 곳은 `vlmparse`(`backends/datalab.py` 의 `chunks_to_blocks`, `schema.py` 의 블록 유형)이고, 블록 유형이 늘면 `pdf2parallax` 의 `rebuild_page`·리더의 `TAG`/`BASE_EST`·`spec.md` §2.2 가 함께 움직인다. 수식을 넣을 때 밟은 길이 그대로 본보기다.

> **먼저 볼 것 — 판독은 이미 잘 하고 있다.** 파인만 3권에서 수식·그림 많은 20쪽을 뽑아 재 본 결과(2026-08-16, $0.20): 블록 214(**equation 76** · p 117 · figure 6 · figcaption 6 · footnote 5 · h2 4), **수식 398개 전부 KaTeX 렌더 성공**, 앱에서 katex 노드 474 · **오류 0 · 칸 넘침 0**. `\begin{aligned}` 다단 수식도 그려진다. **모자란 것은 판독이 아니라 그 뒤의 처리**다 — 아래 다섯이 그것이다.

| | 할 일 | 상태 |
|---|---|---|
| ④ | 표 지원 | **끝남** — 표를 그림으로 오려 낸다(bbox 크롭 · `asset.wfrac`). 아래 ④ 절의 「리더가 HTML 을 글자로 보여준다」는 그 결정 **이전** 기록이다 |
| ① | 불릿 목록 | **끝남** — 「목록을 줄로 나눈다」(2026-08-17) |
| ② | 별행 수식 번호 | 열려 있음 — 번호가 LaTeX 안에 섞여 가운데 정렬된다 |
| ③ | 각주 cross-link | 열려 있음 — 각주 쪽 마커는 오는데 본문 쪽 위첨자가 없다 |
| ⑤ | 어두운 테마의 그림 | 열려 있음 — JPEG 라 흰 배경이 박혀 있다 |

**그 밖에 열려 있는 것.** Markdown 내보내기 두 건(사용자가 2026-08-17 에 보류를 지시했다) — ⓐ 내보낸 `.md` 파일명을 원본 PDF 이름이 아니라 `.parallax` 파일명 기준으로, ⓑ 수식 기호를 Typora 에서 그대로 렌더되는 꼴로.

**고칠 대상이 아니라고 판단해 닫은 것들.** `beyond_weird` 의 평평한 목차(원본이 그렇다) · `Notes`·`Bibliography`·`Index` 누락(beyond_weird·the_meaning_of_your_life — 원본 PDF 에 그 쪽이 없다) · `the_mind_is_flat` p239(색인 쪽).

**④ 를 먼저 권한다** — 판독 쪽은 손댈 것이 없고 리더만 고치면 되는데, 지금 증상이 가장 나쁘다(HTML 소스가 본문에 통째로 보인다).

**① 불릿 목록을 한 단락으로 묶는다.** 지금은 불릿 하나가 블록 하나로 떨어진다. 대역 리더에서 이것이 특히 나쁘다 — 항목마다 행이 갈려 원문·번역이 **항목 단위로 짝지어지는데**, 번역은 항목 수를 그대로 유지한다는 보장이 없다(개조식은 특히 합치는 쪽으로 기운다). 번역 청킹도 잘게 쪼개져 문맥을 잃는다. 판단할 것: 새 유형 `list` 를 둘지, 아니면 항목들을 줄바꿈으로 이어 붙인 `p` 하나로 접을지. Datalab 의 `block_type` 에 `ListGroup`·`ListItem` 이 **이미 있다** — 지금은 둘 다 `p` 로 접히고 있으니 거기서 시작한다.

**② 별행 수식의 자리와 번호.** 인라인이 아닌 수식은 쪽의 특정 영역(대개 가운데)을 차지하고 **앞이나 뒤에 수식 번호**가 붙는다(`(3.14)` 꼴). 지금은 `equation` 유형으로 `$$…$$` 만 담고 번호는 어디로 가는지 모른다 — 본문에 섞여 들어오거나 사라진다. 번호를 **LaTeX 와 분리해** 따로 싣고(블록 필드 하나가 자연스럽다), 리더는 식은 가운데·번호는 오른쪽 끝에 놓는 관례를 따라야 한다. 본문에서 「식 (3.14)에서 보듯」처럼 참조하는 자리와 이어 주면 ③과 같은 문제가 된다.

**③ 각주를 쪽 단위로 인식하고 본문과 이어 준다.** `footnote` 유형은 있지만 **본문의 어느 자리에 걸린 각주인지**가 없다. 리더가 각주를 그 자리에서 펼쳐 보이려면 그 연결이 필요하다. 관련해 **이미 알려진 고장이 하나 있다** — 원문에 `[^n]` 마커가 0개인데 번역에는 106개가 생긴다(모델이 지어내고 대개 `[^3]` 으로 쏠린다. 뉴닉·개조식 판본 모두 같아서 파이프라인의 기존 성질이다). 진짜 마커를 판독 단계에서 살려 내면 이 지어내기도 함께 없어질 수 있다. Datalab 의 `Footnote` 유형은 각주 **본문**만 주므로, 본문 쪽의 위첨자 번호를 어디서 얻을지가 관건이다.

**④ 표 지원.** 2026-08-16 에 `D:\ebook\rewired\rewired.pdf`(662쪽 순수 스캔본)로 실측했다.

**Datalab 이 주는 형식은 깨끗한 HTML 이다** — 손댈 것이 거의 없다:

```html
<table>
  <tr><th>Data categories</th><th>Level 1</th>…</tr>
  <tr><td>Price data</td><td>✔ Historical base price at regional level (~2 years)</td>…</tr>
</table>
<div><div>✔ Currently available</div>…</div>      ← 범례가 같은 블록에 붙어 온다
```

`<tr>` 8 · `<th>` 4 · `<td>` 28 · **`rowspan`/`colspan` 0** · `thead`/`tbody` 래퍼 없음 · 유니코드 기호(`✔ ✘ Ⓟ`) 보존 · **빈 칸도 `<td></td>` 로 자리를 지킨다.** 내용과 구조가 이미 다 온다 — `table_raw` 가 태그를 보존하기로 한 판단이 맞았다.

**문제는 그 뒤다. 리더가 HTML 을 글자로 보여준다**(실측: 블록이 `<p>` 로 떨어지고 `실제 <table> 엘리먼트 0`, 화면에 `<table><tr><th>Data categories…` 가 그대로 보인다. 높이 800px). `TAG` 에도 CSS 에도 `table_raw` 가 없어 `setTextWithMath` 의 `textContent` 로 들어간다. **`deslop` 만 유일하게 `table_raw` 를 건너뛰고**(`deslop.py:119`) 번역·리더·내보내기는 표를 모른다.

계획 넷:
- **판독** — `</table>` 뒤에 붙어 온 범례 `<div>` 를 별도 `p` 블록으로 뗀다.
- **리더** — 진짜 `<table>` 로 그린다. **살균이 먼저다**: 문서 파일에서 온 HTML 을 `innerHTML` 에 넣는 것이라 허용 태그(`table tr th td caption`)·허용 속성(`colspan rowspan`)만 남긴다. CSP 가 스크립트는 막지만 `<img onerror>` 는 CSP 밖이다. 셀 글자는 `setTextWithMath` 를 통과시켜 셀 안 인라인 수식을 살리고, 넓은 표는 그 칸만 가로로 굴린다(수식과 같은 처리 — 좌우 대역이라 표에 쓸 폭이 절반뿐이다). `BASE_EST.table_raw` 는 상수 80을 **행 수 기반**으로 바꾼다.
- **번역** — HTML 통짜를 모델에 맡기면 태그가 깨진다(각주 `[^n]` 을 지어내는 그 모델이다). 구조는 **우리가 붙들고 셀 텍스트만** 뽑아 번역해 제자리에 끼운다. 셀 30개면 짧은 조각 30개라 싸고, 셀 수를 세면 검증도 된다.
- **내보내기** — GFM 파이프 표로. `colspan` 이 나오면 GFM 이 표현을 못 하니 그때만 HTML 을 흘린다.

> **표본이 하나뿐이다.** 텍스트 레이어가 0이라 표 있는 쪽을 **가로 괘선**으로 찾았는데(스크래치의 `find-tables2.py` — 얇고 고립된 줄만 센다. 첫 판은 어두운 표지를 표로 착각했다), 후보 8쪽 중 표가 나온 것은 **1쪽뿐**이었다. 괘선 없는 표는 이 방법으로 안 걸린다. 설계를 굳히기 전에 **`colspan`/`rowspan`·중첩 머리행·쪽 넘김 표**가 있는지 20~30쪽 더 판독해 볼 것(약 $0.30).

**⑤ 어두운 테마에서 그림이 흰 판이 된다.** 파인만 표본의 그림 6개가 **전부 `image/jpeg`** 라 알파가 없고 흰 배경이 박혀 있다. 밝은 테마에서는 크림색 바탕 위에 살짝 뜨는 정도지만 어두운 테마(`--bg: #15171B`)에서는 눈부시다. 옅은 테두리를 주거나 어두운 테마에서 밝기를 낮추는 처리가 필요하다. (창이 가려져 어두운 테마 스크린샷은 못 찍었다 — MIME 이 근거다.)

- `the_mind_is_flat.nominal.parallax` 와 `beyond_weird.nominal.parallax` 를 앱에서 화면으로 확인하지 않았다 — 목차·그림·문체를 CDP 로 볼 것. 드라이버에 `translate.pause(true)` 를 먼저 넣을 것(안 그러면 여는 순간 「현재 장」 번역이 실제로 돈다). `signals.nominal.parallax` 는 2026-08-15 에 확인했다.
- 기존 작업 파일(`the_meaning_of_your_life_compressed.parallax`)에 그림 소급 — 그림 있는 쪽만 Datalab 재판독해 asset·figure 만 끼워 넣는 스크립트 미착수(본문 블록은 건드리면 안 된다 — 번역 전량이 실려 있다).
- 상하 배치에서 본문 폭(`--maxw`)이 좌우와 같아 줄이 길다 — 조판 컨트롤로 줄일 수 있지만 기본값을 배치마다 달리할지는 미결.
- E5 의 나머지: 용어집 편집기 미착수.
- `spec.md` §10의 E6(`.parallax` 내보내기, 문서 라이브러리) 미착수.
- 블록 단위 재번역 UI 없음(백엔드 `blocks.reset`은 있다).
- 웹폰트를 CDN에서 받는다. 오프라인이면 로컬 대체 서체로 물러난다.
- `src/main/importers/index.ts`(378줄)는 휴면 상태로 남겼다 — 앱을 `.parallax` 전용으로 좁히면서 호출부가 사라졌고, `main/index.ts`는 `Cancelled`와 `cancelImport`만 쓴다. 파이썬 sidecar 경로(`findSkillDir`·`extractPdf`·`pagecheckPdf`·`readExtraction`)는 앱 안에서 다시 PDF를 받을 여지를 두려고 존치. 대가로 스킬의 CLI 인자가 바뀌어도 안 깨져서 조용히 썩는다 — `extract.py`·`pagecheck.py`의 인자를 손대면 이 파일도 같이 볼 것.
- 스킬의 `glossary`·`translate`·`deslop`·`verify`는 남겨 뒀다. "문서추출만 축소"를 렌더링 제거로 해석했고, README가 "밤새 전량 번역은 CLI가 싸다"는 왕복을 전제하기 때문이다. 번역 단계까지 들어낼지는 미결.
