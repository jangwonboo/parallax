# CONTEXT — 지금 어디까지 와 있나

새 세션이 이 저장소를 이어받을 때 먼저 읽는 문서. 설계는 `spec.md`, 실행 방법은 `USAGE.md`.

최종 갱신: 2026-08-05

---

## 이 프로젝트는 두 조각이다

| | 무엇 | 어디 |
|---|---|---|
| **Parallax 앱** | 좌우 대역 리더. Electron + better-sqlite3. 읽기와 스크롤 구동 번역. | `src/` |
| **pdf-ko-translate 스킬** | PDF → 구조 복원 → 배치 번역 → `.parallax` 내보내기. Python. | `pdf-ko-translate.zip` |

둘은 `.parallax`(SQLite) 파일로만 오간다. 스키마는 `spec.md` §2.2와 스킬의 `scripts/_parallax.py`에 **이중으로** 적혀 있다 — 한쪽을 고치면 다른 쪽도 고쳐야 한다.

**렌더링은 앱만 한다.** 스킬에 있던 `render.py`와 `templates/`(이북 HTML·테마 CSS)는 2026-07-27에 제거했다. 같은 조판을 두 벌 유지하면 반드시 어긋나고, 실제로 쓰이는 쪽은 앱이다. 스킬의 마지막 단계는 `export.py`(`.parallax` 생성)다.

---

## 앱 상태

`spec.md` §10 기준 E1~E5(스케줄러·pagecheck 통합) 구현. `tsc --noEmit` 통과.

> **2026-08-03~04 세션분은 아직 커밋되지 않았다.** `git status` 에 열 개 남짓이 수정, `.npmrc`·`scripts/{fetch-tts-assets,rebuild-native}.mjs`·`spec-tts.md`·`src/main/tts/`·`vendor/` 가 미추적으로 떠 있다. 낭독은 선택 낭독·mp3 내보내기까지 동작한다 — 아래 「낭독」 절을 먼저 읽을 것.

2026-08-04 세션 — **단락이 왜 갈려 있었는지 찾아 셋을 고쳤다.** 사용자가 「단락 경계가 무너졌다」고 해서 실제로 세어 봤다. `the_mind_is_flat.noads.parallax` 의 문장 중간 끊김은 **52곳**이었다 — 그전에 「0」이라고 보고했던 것은 틀렸다(그 검사는 같은 쪽·같은 유형만 봤다). 처음 센 153곳 중 95곳은 색인이라 오탐이었다(색인 항목은 원래 소문자로 시작하고 마침표가 없다).

갈래는 셋이고 전부 **이어붙이기가 이웃을 잘못 보고 있던 문제**였다.

- **버린 블록이 이음매를 끊었다**(본문 6곳). `drop` 은 지우지 않고 표시만 하는데(ID 불변) 이어붙이기가 바로 앞 블록을 `prev` 로 삼았다. 문장 한가운데에 전자책 뷰어의 `Page 60 of 252 · 3%` 가 끼면 두 반쪽이 영영 이웃이 되지 못한다. 이제 읽지 않는 블록은 이음매를 끊지 않는다(`extract.stitch_blocks` · `pagecheck.stitch_pages` · `repair.find_seams` 셋 다).
- **주석 유형이 항목 안에서 흔들렸다**(46곳). 한 주석이 `footnote → p → quote` 로 번갈아 분류되는데 유형이 다르면 잇지 않는 규칙 탓에 주석부가 통째로 갈렸다. `BODY` 에 `footnote` 를 넣고, 대신 번호로 시작하는 곳(`starts_note`)은 새 항목으로 보아 지킨다. 합칠 때 남는 유형은 `merged_type` 이 정한다 — 캡션과 주석은 자기 자리를 지킨다(본문으로 바꾸면 흐름 한가운데로 들어온다).
- **`is_page_progress` 가 진짜 내용을 삼켰다.** 예전 규칙은 「글자가 없고 숫자가 있으면 참」이라, 분철된 인용의 꼬리(`Journal of Nervous and Mental Dis-` 뒤의 `ease, 131(2): 135-48.`, `46.`, `80.`)를 뷰어 부속물로 지웠다. 이제 진행 표시라는 증거(`%`·page/location·`26 of 201` 꼴)를 요구하고, 판독이 뭉갠 형태(`Page 60T 252 + 3%`, `0f`)까지 잡는다. 맨 숫자 조각은 `is_bare_label` 로 갈라 냈고, **앞 블록이 하이픈으로 끊겼으면 무엇도 버리지 않는다**(`pagecheck.continues_hyphen`). `looks_garbled` 가 URL 주석을 통째로 먹던 것도 막았다.

결과(실측): 문장 중간 끊김 **52 → 10**, 그중 본문은 **6 → 1**. 남은 하나는 `understand-` 뒤 낱말 꼬리가 판독에서 아예 사라진 자리라 잇지 않는 것이 맞다. 기존 파일은 `repair.py --apply --stitch-translated` 로 45쌍을 잇고(1492 → 1447 블록) 대기 44개를 앱에서 채웠다($0.18). 원본은 `.bak`.

**리더가 버린 블록을 그리던 것도 고쳤다.** `DROPPED` 184개가 본문 사이에 흩뿌려져 있었다 — 쪽 대조에 쓸모가 있으리라 보고 원문 칸에 남겨 뒀는데, 실제로는 읽는 흐름만 끊었다. `db.ts` 의 `count`·`range`·`outline`·`meta` 가 전부 거른다(파일에는 그대로 남는다). 진행률 분모도 함께 고쳤다 — 버린 것을 세면 전부 번역해도 100% 에 닿지 않는다. 1492 → 1308 로 보이고, 문서를 훑어도 부속물이 없다(CDP 확인).

2026-08-03~04 세션 — **낭독(TTS)을 넣기 시작했다.** 설치가 깨지던 것부터 고쳤다: `npm start` 가 `'tsc' is not recognized` 로 죽었는데 `node_modules` 자체가 비어 있었다. better-sqlite3 에 Node 24 용 미리 빌드본이 없어 소스 컴파일로 넘어가고, node-gyp 10 이 Visual Studio Build Tools 2026(v18)을 못 알아봐 npm 이 전체를 되돌린 것이다. `.npmrc` 로 네이티브 모듈을 Electron ABI 로 받게 하고(`runtime=electron`), `scripts/rebuild-native.mjs` 를 postinstall 에 걸어 「미리 빌드본 먼저, 안 되면 컴파일, 그래도 안 되면 설치는 성공시킨다」로 만들었다. 깨끗한 재설치로 확인. 그 위에 Supertonic 3(ONNX, `onnxruntime-node`)을 붙였다 — 자세한 것은 아래 절.

2026-07-30 세션 — **07-28 수정분의 화면 재확인을 마쳤다.** `pagecheck_test_20p.pdf` 를 재생성해(원본 앞 20쪽, PyMuPDF — 지난 세션 것은 지워져 있었다) 앱으로 열고 「전 쪽 재판독」 전 구간을 실행($0.15). 확인된 것: ① 쪽별 진행 표시 `페이지 검증 중 · 7 / 20쪽 · 취소` 가 화면 아래 가운데에 뜬다. ② `superseded` 이관 — 시험 문서에 19행, 밀려난 원 레이어(`"Ihe Meaning or Your I ife"` 따위)가 그대로 보관됐다. ③ `page_check` 이관 21행(요약 `page=0` + 20쪽), **파일 → 페이지 검증 리포트** 가 임시 md 로 재조립해 연다(요약·쪽별 표 온전). 검증 방법: `--inspect=9229` 로 메인에 붙어 `dialog.showMessageBox` 를 패치해 자동 응답, 렌더러 CDP 로 관찰 — 스크립트는 세션 스크래치의 `drive-pagecheck.mjs`/`watch-pagecheck.mjs`. 요령 둘: CDP `Page.captureScreenshot` 은 창이 가려지면 응답하지 않는다(메인의 `webContents.capturePage()` 를 쓸 것), 번역 자동 시작은 렌더러에서 `setInterval` 로 `translate.pause(true)` 를 계속 눌러 막았다(지출 $0.00 확인). 같은 날 electron-builder 로 Windows 설치본(`Parallax Setup 0.1.0.exe`)을 처음 빌드했다 — 출력은 `release/`(`.gitignore`), 산출물은 언제든 재생성되므로 보관하지 않는다.

2026-08-05 두 번째 세션 — **그림이 처음으로 파이프라인을 통과한다(스키마 v2).** Datalab chunks 가 잘라 보내는 그림(base64 JPG + 모델이 쓴 alt 설명 + bbox)을 지금까지 버리고 있었다 — 이제 `harvest_images()` 가 **모든 청크**의 `images` 필드를 줍는다(Picture 유형만 보면 안 된다: 실측에서 표지 그림이 SectionHeader 안에 `<img>` 로 내장되어 왔다). 흐름: pagecheck 가 `book["assets"]` + `figure` 블록(`src`=asset id, NO_TRANSLATE, 한 변 64px 미만은 장식으로 버림) → `export.py`/앱 `Doc.create` 가 `asset` 테이블(id·mime·w·h·alt·data BLOB)로 이관 → 리더가 data URI 로 그린다(`asset:get` IPC, CSP 의 `img-src data:` 는 이미 허용). figure 행은 원문·번역 **양쪽 칸에 같은 그림을 하나씩** 둔다(사용자 지정: 한쪽 대역만 읽는 눈이 그림을 놓치지 않게) — 칸 폭의 60% 상한으로 축소만 하고(`max-width: 60%`, 확대·잘림 없음) 데이터는 한 번만 받아 두 `<img>` 가 나눠 쓴다. `max-height` 는 두면 안 된다 — `<img width>` 속성이나 max 제약 둘이 겹치면 비율이 깨지는 것을 실측으로 확인했고, `width:auto; height:auto; max-width` 하나만 남겼다. 가상 스크롤 높이는 상수 추정이 아니라 w/h 실값으로 계산한다. `schema_version` 1→2(스키마 세 벌 + spec §2.2 모두 갱신), `parallax_import.py` 왕복도 asset 을 실어 **무손실 확인**. 검증: 20쪽 시험 PDF 1~3쪽 실 호출(표지 그림 1417×1274 + 출판사 로고 65×90 수확) → export → 앱을 CDP 로 열어 화면 실측 — 비율 유지(761×685) 확인. 이때 밟은 것 둘: ① `<img width=…>` 속성이 살아 있으면 CSS `max-height` 가 비율을 깨고 옆으로 퍼진다 — `width:auto; height:auto` 로 풀어야 max 제약 둘이 비율을 지키며 줄인다. ② CDP 드라이브 스크립트가 번역 자동 시작을 안 막아 시험 문서 번역이 실제로 돌았다($0.1~0.2 지출) — 다음부터 드라이버에 `translate.pause(true)` 필수(2026-07-30 세션의 요령을 그대로). 렌더러의 번역 요청도 NO_TRANSLATE/DROPPED 블록을 거르게 고쳤다(전부터 새던 구멍). 정본 zip 재포장 완료. 기존 945블록 작업 파일에 그림을 소급하려면 그림 있는 쪽만 datalab 재판독하는 스크립트가 필요하다(미착수).

같은 날 이어서 — **《The Mind Is Flat》(244쪽, `D:\ebook\__output__\the_mind_is_flat.noads.pdf`)을 전 쪽 datalab 재판독으로 변환**해 `the_mind_is_flat.parallax`(875블록 · 그림 43 · figcaption 39 · 번역 0, 같은 폴더)를 만들었다. 이 실전에서 스킬 버그 둘을 잡았다(`a876256`): ① **429 요율 제한** — 8워커 동시 요청에 219/244쪽이 「판독 실패」로 남았다. 실패는 캐시에 안 남으므로 재실행하면 성공분은 공짜다. 지금은 POST·폴링 모두 Retry-After 백오프로 재시도한다. ② **Datalab 파일명은 내용 해시가 아니다** — 레이아웃 기반이라 서로 다른 그림 16장이 같은 이름으로 와 asset 이 덮어써졌다(그림 43개가 asset 11개로 뭉개진 걸로 발각). asset id 는 sha1(내용) 16자리로 우리가 만든다.

2026-08-05 세션 — **페이지 검증에 Datalab API 엔진 추가.** 처음에 로컬 Chandra(`--engine chandra`, 공개 웨이트 + GPU)로 넣었다가 같은 날 사용자의 정정으로 **[Datalab API](https://www.datalab.to)**(Chandra 를 만든 곳의 호스팅 서비스, 공개 웨이트보다 정확한 개선판이 돌아간다 — README 벤치마크 86.7% vs 85.8%)로 교체했다. `pagecheck.py --engine datalab`: 쪽 PNG 를 `POST /api/v1/convert`(multipart, `X-API-Key`)로 보내고 `request_check_url` 을 2초 간격 폴링(5분 한도), **`output_format=chunks`** 를 받아 **항상 재구성**(`from_ocr|rebuilt`, 밀려난 레이어는 `superseded`) — layer 신뢰 모드는 출력이 우리 블록 ID 를 모르는 통짜 판독이라 성립하지 않는다. 처음에 markdown 으로 받았더니 표지 그림 설명문이 h1·본문으로 새어 들어왔다(유형이 없어 못 거른다) — chunks 는 블록마다 유형이 붙어 와서 `chunk_items()` 가 `PageHeader/PageFooter/PageNumber/Picture/Figure` 를 통째로 버리고 `SectionHeader`→h(태그 레벨), `Caption`→figcaption, `Footnote`→footnote, `Table`→table_raw(태그 보존), 나머지→p 로 대응만 한다. 모드는 `--datalab-mode fast|balanced|accurate`(기본 accurate), HTTP 는 anthropic 이 이미 끌어오는 httpx 라 **새 의존성 없음**. 키는 `DATALAB_API_KEY` — 환경변수 → `.env`(cwd → 스킬 폴더 → 그 부모) 순이고, 자리표시자(`your-datalab-api-key-here`)는 없는 것으로 친다. 루트에 `.env`(자리표시자, `.gitignore` 에 추가)와 `.env.example`(커밋됨)을 뒀다. 앱은 다이얼로그에 「전 쪽 재판독 (Datalab API)」 버튼을 **키가 읽힐 때만** 넣고(`readDatalabKey()` — 앱이 직접 읽어 자식 환경변수로 넘긴다, 패키징되면 cwd 가 달라서), Anthropic 키가 없어도 Datalab 키만 있으면 검증이 돈다. 사용자가 실키를 `.env` 에 넣어 **실 호출까지 검증했다** — 20쪽 시험 PDF 로 1~3쪽 재판독(재구성 3, 페이지 병합 5), 표지 그림 설명·러닝 헤더가 유형 단계에서 걸러지고 실측 비용 accurate 쪽당 1¢(응답 `total_cost`). zip 재포장하면서 `{references,scripts,templates}` 가짜 디렉터리가 풀린 사본에 남아 있던 것도 지웠다.

2026-08-01 세션 — **UI 서체를 Adobe Clean 으로 고정.** `--font-ui` 를 `"Adobe Clean"(로컬) → "adobe-clean"(웹폰트) → SUIT…` 순으로 바꾸고, 앱 시작 시 typekit 킷(`use.typekit.net/pps7abe.css` — helpx.adobe.com 이 쓰는 공개 킷, adobe-clean 300/400/700/800/900)을 항상 로드한다. CSP 의 style-src·font-src 에 `use.typekit.net` 추가, preconnect 도. 라틴 전용 서체라 UI 의 한글은 여전히 SUIT 이하가 받는다. CDP 로 실행 확인 — 로컬 미설치 상태에서 adobe-clean 400/700 로드, `.bar` 계산 서체 일치.

2026-07-28 두 번째 세션에서 들어간 것:

- **pagecheck 통합을 처음으로 실제로 태웠고, 성공했다.** 20쪽 발췌 PDF(`pagecheck_test_20p.pdf`, 원본 앞 20쪽)로 전 쪽 재판독 전 구간을 검증 — 전 블록에 `from_ocr|rebuilt`(일부 `stitched`)가 붙었고 임시 폴더도 통째로 지워졌다. 다이얼로그에 쪽 범위 입력이 없으므로 비용을 묶은 시험은 발췌 PDF 를 만들어 여는 방식이 맞다.
- **진행 표시가 침묵하던 원인 둘을 고쳤다.** ① 파이프에 물리면 파이썬 stdout 은 블록 버퍼링이라 flush 없는 `print` 가 끝까지 안 나온다 — 앱이 파이썬을 `-u` 로 띄운다(`runPy`). ② pagecheck 진행이 20쪽마다 한 번이라 20쪽 시험에서는 끝나는 순간 한 번 나오고 `done` 이 곧바로 감췄다 — 쪽마다·완료 순서(`as_completed`)로 바꿨다. 2026-07-30 화면 재확인 완료(아래 참조).
- **앱이 `superseded` 를 버리던 것을 고쳤다.** 재판독이 밀어낸 원 텍스트 레이어를 CLI `export.py` 는 보관하는데 앱 `Doc.create` 경로에는 이관이 없었다. `readExtraction` → `Doc.create` 로 같은 형식(page 정수, payload JSON)으로 옮긴다.
- **pagecheck 판정을 문서에 남기고 앱 메뉴로 되살린다.** 리포트 md 는 임시 폴더와 함께 지워지므로, `pagecheck.py` 가 판정을 `book["page_check"]` 로 내보내고 CLI `export.py` 와 앱 `Doc.create` 가 `page_check` 테이블로 옮긴다. 스키마 세 벌은 그대로 — 요약은 `page=0` 행, 사유·메모는 notes 로 합친다(규약은 `spec.md` §2.2). `parallax_import.py` 는 역방향을 복원해 왕복 무손실(스모크 테스트로 확인). 앱은 **파일 → 페이지 검증 리포트** 에서 임시 md 로 재조립해 기본 편집기로 연다.
- **목차가 본문 조판을 따른다** — 원문 서체·줄간격에 크기는 본문의 0.88배. 항목이 본문 제목 그 자체라 UI 크기 고정(`--ui-size`)의 예외로 뒀다. 같은 크기로 하면 bold 장 제목이 본문보다 커 보여서 한 단계 내렸다(Electron 으로 계산값을 실측해 확인 — 스크립트는 세션 스크래치의 `measure-toc.js`). 덮어 뜨는 패널이라 크기가 바뀌어도 툴바·본문 배치는 요동치지 않는다.

2026-07-28 첫 세션에서 들어간 것:

- **앱이 pagecheck 를 부른다(E5).** PDF 를 열면 추출이 끝난 뒤 「건너뛰기 / 구조만 대조 / 전 쪽 재판독」을 묻는다. 쪽수와 예상 비용을 보여 주려면 추출 뒤여야 한다. 자세한 것은 `USAGE.md`.
  - **묻지 않고 돌리는 일은 없다** — 쪽마다 돈이 나간다. 키가 없으면 조용히 건너뛴다. 「다음부터 묻지 말고 건너뛰기」는 *건너뛰기* 로만 굳는다(설정 `pagecheck: "off"`, 파일 메뉴에서 되돌린다). 매번 돈이 나가는 쪽이 기본값이 될 수 있는 경로는 두지 않았다.
  - 어느 모드를 권할지는 `meta.ocr_layer` 로 정하되 **고르는 것은 감추지 않는다.** 잘못 고르면 되돌릴 수 없고(재판독은 원문의 고어 철자를 현대화한다), 판정은 어차피 추정이다.
  - 중간에 취소하거나 검증이 실패해도 문서는 열린다. `pagecheck.py` 가 다 끝난 뒤에야 `book.json` 을 덮어쓰므로 남는 것은 추출 직후 상태다.
  - 추출·검증 산출물은 임시 폴더 **하나**에 모아 통째로 지운다. pagecheck 는 `book.json` 옆에 쪽 이미지(수백 MB)·캐시·리포트를 만든다.
  - pagecheck 가 남기는 `from_ocr`·`rebuilt`·`stitched`·`dropped` 를 앱 플래그로 옮긴다. 예전에는 `translate` 만 봤다.
- 툴바에 `조판` 패널. 컨트롤 일곱 개가 1400px 에서 가로로 넘치던 것을 접어 넣었다. 바에 남은 것은 목차·조판·번역 범위·테마뿐이고 420px 까지 넘치지 않는다. Esc 는 위에 떠 있는 것부터 하나씩 닫는다(사전 → 조판 → 목차).
- 화면 아래 가운데에 여는 중 표시(`.importing`)와 취소 버튼. 검증은 몇 분씩 가므로 진행이 보이지 않으면 멈춘 것과 구분되지 않는다. 왼쪽은 목차가, 오른쪽은 번역 상태가 이미 쓰고 있어 가운데다.
- **내보내기도 제목을 원문으로 고정.** 파일 이름·HTML `<title>`·본문 제목·목차가 전부 `title`/`src` 를 쓴다(리더와 같은 규칙). 번역 제목은 버리지 않고 부제 한 줄로 남긴다.

2026-07-27 세션에서 들어간 것:

- 목차 항목을 눌러도 목차가 닫히지 않는다(≥900px). 좁은 화면은 겹쳐 뜨므로 그대로 닫는다. 고른 항목에 `aria-current` 표시.
- 조판 컨트롤 정리(아래 「조판」 참조) — 글꼴 크기 10~24pt, 줄간격 5배수, 단락간격 3단계.
- 앱 메뉴 **파일 → Anthropic API 키…**(Ctrl+K). 저장/지우기/상태. 키 값은 renderer로 내려가지 않는다 — 있는지·환경변수인지만 문구로 보여준다.
- 원문 단락 왼쪽 세로줄 제거. PDF 문서는 전 블록에 `FROM_OCR`이 붙어 늘 켜져 있었고, 항상 켜진 표시는 정보가 아니다.
- 툴바를 창 상단 전폭 붙박이로(`position: fixed`). 목차 개폐·글꼴 크기·스크롤과 무관하게 59px 고정. `measureBar()` 및 호출부 제거.
- 툴바의 `열기` 버튼 제거 — 문서 열기는 앱 메뉴(Ctrl+O)로 일원화. 빈 화면의 버튼은 남겼다.
- 목차·책 제목을 **원문 고정**(`db.ts`의 `coalesce(ko, src)` → `src`, `app.js`의 `title_ko || title` → `title`).
- 창 제목을 항상 `Parallax`로. `app.setName` + Windows `setAppUserModelId`.
- 가상 스크롤 높이 추정치를 조판에 연동(아래 참조).
- **PDF 열기가 Windows에서 처음으로 동작한다.** `importers/index.ts`가 `process.env.HOME`(Windows에는 없다)을 보던 것을 `os.homedir()`로, 파이썬을 `"python3"`로 고정해 부르던 것을 `findPython()`으로 바꿨다. Windows의 `python3.exe`는 대개 Microsoft Store로 보내는 0바이트 스텁이라, 후보를 실제로 실행해 보고 고른다. 임시 산출물도 원본 옆이 아니라 임시 폴더에 쓰고 지운다(예전에는 `blocks.txt`와 `*.parallax-extract.json`이 문서 폴더에 남았다).

  스킬이 있어야 동작한다. `findSidecar()`가 보는 곳은 `PARALLAX_SKILL_DIR` → `~/.claude/skills/pdf-ko-translate` → `<cwd>/pdf-ko-translate` → `<cwd>/../pdf-ko-translate` 순이다. 지금은 저장소 안에 `pdf-ko-translate/`로 풀어 뒀다(정본은 zip이고 이 사본은 `.gitignore`에 있다).

### 낭독 — 어디까지 왔나

설계는 `spec-tts.md` — §13 은 코드와 대조한 기록, **§14·§15 는 조작 방식을 두 번 바꾼 기록**이다. 단계는 T0~T4.

| | 무엇 | 상태 |
|---|---|---|
| T0 | Supertonic 자산 내려받기 (4모델 383MB) | 됨 |
| T1 | 한국어 낭독 품질 판정 | 됨 — 보이스 **F1** 채택 |
| T2 | utilityProcess 합성·재생 경로 | 됨 |
| T3 | 선행 합성 · 연속 재생 · 따라 스크롤 | 됨(§14.1 · §15.2) |
| T4 | 재생 컨트롤·목소리·속도 | 됨(툴바) · **유휴 종료만 남음** |
| — | mp3 내보내기 · 낱말 하이라이트 | 됨(§14.2 · §15.3) — 사양에 없던 것 |

**자산은 히스토리에 넣지 않는다.** 고정 리비전(`Supertone/supertonic-3` @ `3cadd1ee`)에서 `scripts/fetch-tts-assets.mjs` 로 언제든 다시 받는다 — `.part` 로 받아 크기를 확인하고 이름을 바꾸며, 해시는 `tts-assets/VERSION` 에 남는다. 파이썬이 아니라 Node 로 짠 이유는 이 PC 의 파이썬이 `CERTIFICATE_VERIFY_FAILED` 로 죽기 때문이다(아래 「이 PC 환경」).

**`.npmrc` 의 `runtime=electron` 과 `onnxruntime-node` 는 부딪히지 않는다.** onnxruntime-node 는 N-API v6 로 배포되어 ABI 를 타지 않으므로 `electron-rebuild` 가 필요 없다. 대신 asar 안에서는 네이티브 `.node` 를 못 열어 `package.json` 에 `asarUnpack` 을 걸었다.

**합성은 utilityProcess 에서 돈다.** 모델을 물면 RSS 가 424MB 늘고, ONNX 가 죽으면 프로세스째 간다 — 메인에 얹으면 읽던 책이 함께 사라진다. 취소가 프로세스 경계를 넘어 먹는 것까지 확인했다.

**실측**(이 PC): 모델 로드 1.0초, fork+로드 1.38초. `steps=8` 에서 실시간의 0.22~0.26배(`steps=5` 는 0.15~0.16배). 한국어와 영어가 **같은 보이스로 둘 다** 난다(ko F1 0.22배, en F1 0.26배) — g2p·espeak 없이 유니코드 코드포인트로 색인하기 때문이다. 0.22배는 선행 합성 임계(0.5)의 절반 이하라, `spec-tts.md` §5.6 이 「캐시하지 않는다」로 정한 근거가 실측으로 확인됐다.

**조작은 툴바에 있다 — 고르면 준비되고, 누르면 시작한다.** 툴바 토글(늘 소음) → 선택 팝업(고를 때마다 본문을 가림)을 거쳐 지금 자리에 왔다. 낭독은 읽는 내내 켜져 있는 것이라 뜨고 지는 창보다 늘 같은 자리가 맞다.

```
[▶] [■]  읽는 중 42%   [F1▾] [1.0×▾] [MP3]
```

- **고르면 준비, 누르면 시작.** 고르는 순간 소리가 나면 글을 짚어 보려던 선택마다 놀란다 — 고르는 것은 「무엇을」이고 누르는 것이 「지금」이다. 고를 때 조각 계획과 첫 조각 합성까지 끝내 두므로 누르면 **0.2초**에 소리가 난다.
- **세 낱말 이하는 범위가 아니라 자리다** — 한 낱말만 짚으면 그 블록부터 **문서 끝까지** 이어 읽는다. 읽을 수 없는 블록과 번역이 아직 없는 블록은 건너뛴다(원문으로 바꿔 읽으면 언어가 문장 중간에 튄다). 손으로 스크롤하면 따라가기를 3초 억제한다.
- **속도는 재생 배속**(`playbackRate`)이라 즉시 먹고 읽는 중에도 바뀐다. 목소리는 그럴 수 없어 바꾸면 다시 합성한다(읽던 자리는 지킨다).
- **mp3 는 제목 경계로 쪼갠다** — 합성 청크가 아니다. `{책}_{01}_{장 제목}.mp3`, 제목이 둘 미만이면 `{책}_{시작 세 낱말}.mp3` 한 개. 번호는 총 개수만큼 자릿수를 채운다. 인코더는 `@breezystack/lamejs` 를 번들한다 — 이 PC 엔 ffmpeg 이 있지만 설치본엔 없다.

**낱말 하이라이트 — 어디까지 정확한가.** `duration_predictor.onnx` 의 출력은 `duration` 하나뿐이고 **`dims: [1]`**(발화 전체 길이 하나)이다. 실측으로 확인했다 — 3자든 48자든 값 하나. **토큰별 길이가 없으므로 진짜 정렬은 이 엔진으로 못 만든다.** 그래서 조각(문장) 경계는 WAV 길이로 **정확히** 맞추고, 그 안에서만 글자 수 비례로 나눈다(마침표 +4, 쉼표 +2). 문장마다 다시 맞물리므로 어긋남이 한 문장(3~8초) 안에 갇힌다. 표시는 **두 겹**이다 — 읽을 영역을 옅게 깔고(`say-range` 14%) 지금 읽는 낱말을 그 위에 조금 진하게 얹는다(`say-word` 38%). 영역은 고른 범위(또는 이어 읽기라면 지금 읽는 블록)다. **색은 짝 강조와 갈라 뒀다** — `--say`(청록) vs `--accent`(슬레이트블루). 둘은 동시에 켜져 있을 수 있어서 같은 색이면 무엇이 무엇인지 알 수 없다. CSS Custom Highlight API 라 DOM 을 건드리지 않고, 가상 스크롤이 행을 다시 만들면 `paintRange()` 가 자리로부터 되살린다.

**사전은 더블클릭으로 옮겼다.** 예전에는 고르기만 하면 떴는데, 낭독이 「낱말 하나를 짚으면 여기부터」를 쓰면서 자리를 정할 때마다 사전이 함께 떠 본문을 가렸다. 더블클릭은 낱말을 고르는 동작 그 자체라 손이 더 가지도 않는다. 여는 길을 옮겼으니 **닫는 길을 따로 뒀다** — 밖을 한 번 누르면 닫힌다(예전에는 다음 클릭이 곧 다음 mouseup 이라 저절로 닫혔다). 더블클릭의 둘째 누름까지 닫아 버리지 않도록 `e.detail === 1` 일 때만 본다.

CDP 로 실행 확인(세션 스크래치의 `drive-say*.mjs`·`drive-export*.mjs`·`tail-test.mjs`·`drive-layout.mjs`): 툴바 `themeSel,tocBtn,typeBtn,sayPlay,sayStop,sayVoice,saySpeed,sayExport,modeSel` · 긴 선택은 「준비됨」에서 2.5초 뒤에도 무음 · 재생 0.2초 · 하이라이트가 `2018.` → `asserted.` → `book` 으로 소리를 따라 흐름 · 짧은 선택은 「준비됨 · 여기부터」 후 `b0019 → b0020` 으로 블록을 넘어가며 42초 연속 재생 · 제목 4개 구간 → mp3 4개(44.1kHz 모노 64kbps, ffprobe 확인) · 단락 하나 → 1개(367자 = 27.2초, 8.1초).

> **저장 대화상자를 가로채는 법.** main 의 전역 컨텍스트에는 `require` 도 dynamic import 도 없어서(`process.mainModule` 은 undefined) CDP 에서 곧바로 `dialog` 를 못 바꾼다. `Debugger.setBreakpointByUrl` 로 핸들러에 멈춘 뒤 `Debugger.evaluateOnCallFrame` 로 **`electron_1.dialog`**(tsc 가 만든 이름이다)를 갈아 끼우고 재개하면 된다. 렌더러 CDP 로는 모듈 스코프 변수(`source`·`cur`)를 못 읽으니 확인은 DOM 으로 할 것.

**남은 것**: `src/shared/types.ts` 의 `isExportable`/`isSpeakable` 을 **아무도 쓰지 않는다**(렌더러가 같은 판정을 제 손으로 한다). `exporter.ts` 는 아직 `b.flags & T.DROPPED` 를 직접 본다. 설정의 `tts.on`·`tts.side` 는 뜻을 잃었다.


### 배치 — 넷 중 하나

조판 패널의 `배치` 로 고른다. `lr` 원문 왼쪽(기본) · `rl` 번역 왼쪽 · `tb` 원문 위 · `bt` 번역 위.

**자리를 바꾸는 데 `order` 를 쓰고 그리드 정의는 건드리지 않는다.** 그래야 `--split`(과 그것을 끄는 손잡이)이 늘 「왼쪽 칸의 폭」이라는 한 가지 뜻으로 남는다 — 열 정의를 뒤집으면 손잡이를 왼쪽으로 끌 때 오른쪽이 넓어진다. 상하에서는 손잡이를 감춘다(나눌 세로선이 없다).

**상하에서는 읽는 쌍만 남기고 나머지를 물린다**(`.row.dim` .26 / `.row.near` .58). 좌우로 놓으면 한 쌍이 한 줄에 들어와 눈이 쉽게 짝을 찾지만, 위아래로 쌓으면 원문 끝과 다음 쌍의 원문이 세로로 이어져 보여 어느 것이 짝인지 흐려진다. 기준은 **화면 한가운데** — 커서를 따르면 스크롤만 하는 동안 초점이 굳고, 맨 위를 기준으로 하면 넘어가는 순간이 너무 이르다. `display` 가 아니라 투명도라 읽기 도구는 여전히 전부 읽는다.

가상 스크롤 높이 추정은 상하에서 여백이 두 번 들어가는 것만 더한다 — 칸이 넓어지면 줄 수가 줄어 대략 상쇄되고, 마운트된 행은 어차피 실측이 덮어쓴다.

**자산은 `userData/tts` 에서 찾는다.** 이 PC 에서는 저장소의 `tts-assets/` 로 디렉터리 정션을 걸어 뒀다(`C:\Users\johnb\AppData\Roaming\parallax\tts`) — 383MB 를 두 벌 두지 않으려는 것이고, 앱의 `tts:install` 을 쓰면 정식으로 다시 받는다.

**Voice Builder(직접 만든 보이스) 접수는 2026-08-31 에 닫힌다.** 쓸 생각이면 그 전에.

### 조판 — 왜 이렇게 정했나

**축은 세 개고 서로 비례한다.**

| 컨트롤 | 값 | CSS |
|---|---|---|
| 글꼴 크기 | 10~24pt 연속 | `--size` |
| 줄간격 | 1 · 1.2 · 1.5 · 1.8 · 2 (배수) | `--leading` |
| 단락간격 | 없음 · 반 줄 · 한 줄 | `--para-k` |

단락 간격은 **줄 높이(= 크기 × 줄간격)의 배수**다: `padding-top: calc(var(--leading) * var(--para-k) * 1em)`. `em`(크기)만 쓰면 줄간격을 넓혔을 때 단락 사이가 상대적으로 좁아 보여 단락 경계가 흐려진다. 줄 높이 기준이면 두 축 모두에 비례하고, 여백이 줄 높이의 정수배라 좌우 대역의 행이 같은 그리드에 남는다.

**블록 사이 여백은 `.cell` 의 padding 이 아니라 margin 이다.** 짝 강조 상자가 `.cell` 배경이라, padding 이면 단락 간격(줄 높이 한 개 분)까지 상자 안에 들어와 강조가 글보다 한 줄 위에서 시작한다. 여백 값은 `--blockgap`/`--blockgap-b` 로 정하고 붙이는 것은 `.cell` 의 margin 이다. 상자의 안여백은 `--hl-x`(1.5rem, 세로선과 글 사이) · `--hl-y`(0.7rem)이고, 세로 방향은 같은 크기의 **음수 margin 으로 되돌려** 상자를 키워도 줄 자리가 밀리지 않는다(강조는 한 번에 하나뿐이라 이웃과 겹쳐도 보이지 않는다). 세로선 자리는 `margin-left: -.55rem` 그대로다 — 더 왼쪽으로 빼면 본문 여백(`--pad`)을 넘어 창 밖으로 나간다.

**여백을 주면 첫 줄 들여쓰기는 자동으로 뺀다**(`--indent`를 0으로). 단락을 나누는 장치는 하나면 되고 둘을 겹치면 단락 머리가 두 번 표시된다.

**툴바·목차·사전 글자는 본문을 따라가지 않는다.** `--ui-size: 12px` 고정(윈도우 기본 Segoe UI 9pt). 예전에는 본문의 0.8배로 11~19px를 오갔는데, 본문을 키울 때마다 창 전체가 요동쳤다.

**제목은 셋 다 같은 모양이다** — 본문의 1.2배·bold·들여쓰기 없음·왼쪽 정렬. 위계는 크기가 아니라 앞의 여백(장 3.2rem / 절 2.2rem / 섹션 1.4rem)이 만든다. **본문 글자 크기는 요소 종류와 무관하게 하나다** — 각주·도판 설명이 0.84em으로 작던 것을 없앴고 구분은 색과 여백으로만 한다.

저장하는 값은 `sizePt`(pt) · `leadStep`·`paraStep`(단계 번호)다. 단계표를 바꿔도 예전 설정이 엉뚱한 값으로 되살아나지 않는다.

### 가상 스크롤 높이 추정 — 알아둘 것

전체 문서 높이는 `index[]`의 블록별 높이 합이다. 조판이 바뀌면 `invalidateHeights()`가 캐시를 버리는데, **실측은 마운트된 30~40행만** 가능하다(나머지는 DOM에 없다). 예전에는 나머지를 타입별 상수로 되돌려서 줄간격을 2배로 늘려도 총 높이가 그대로였다.

지금은 `recomputeEstimates()`가 조판에서 되짚는다:

```
본문 높이 ∝ size² × leading / 칸폭      (줄 수 ∝ size/폭, 줄 높이 ∝ size×leading)
제목 높이 ∝ size                        (line-height 1.35 고정이라 leading을 안 탄다)
```

기준 상수는 191쪽 문서에서 p 487개·h2 40개·h3 20개를 실측해 역산했다(`p: 174`, `h2: 65`, `h3: 42`). **평균으로 보정했다** — 총 높이는 합이라 중앙값이 아니라 평균이 맞다.

실측 오차: 보정에 쓴 조판에서 +1.3%, 쓰지 않은 조판(13px/2.3/65%)에서 −5.9%. 스크롤바 길이용으로는 충분하고, 더 정확하려면 오프스크린 일괄 실측밖에 없다(조판을 만질 때마다 멈칫한다).

---

## 스킬 상태

**정본은 `pdf-ko-translate.zip` 이다.** 저장소에 풀려 있는 `pdf-ko-translate/` 는 `.gitignore` 에 있다 — 스크립트를 고쳤으면 zip 을 다시 말아야 히스토리에 남는다. 2026-07-28에 다시 말면서 `{references,scripts,templates}/` 라는 항목도 지웠다(중괄호 확장이 안 먹은 셸 명령이 남긴 가짜 디렉터리다).

정본은 2026-08-04 에 다시 말았다 — 19개 항목(`repair.py` 가 새로 들어갔다). 말기 전에 전 파일을 UTF-8 로 엄격 디코드해 손상 0을 확인했고(BOM·치환문자 없음), 만든 뒤에는 풀어서 바이트 대조 19/19 일치와 `import extract, pagecheck, repair` 까지 확인했다. 옛 zip 은 세션 임시 폴더에 날짜를 붙여 남겼다.

2026-08-02~03 세션에 고친 것 — **단락이 토막 나던 원인을 뿌리에서 잡았다.** `the_mind_is_flat.parallax` 가 문장 한가운데서 끊긴 블록으로 가득했고, 끊긴 블록은 번역도 안 됐다.

**문장이 끝났는지 판정** (`extract.py`)
- `TERMINAL_RE` 가 `[.!?…”"')\]]` 였다 — **닫는 따옴표만으로도 문장이 끝난 것으로 봤다.** `r'[.!?…][\'"’”)\]]*\s*$'` 로 바꿔, 진짜 문장부호 **뒤에 붙은** 따옴표만 인정한다.
- `continues_sentence()` — 뒤 블록이 여는 따옴표·괄호로 시작하면 그것을 벗기고 소문자인지 본다. `("but as we shall see…` 가 대문자로 시작한다고 새 문장 취급되던 경로다.
- `stitch_blocks()` 의 본문 유형에 **`figcaption` 을 넣었다.** 도판 설명이 반토막 난 채(`Figure 3. … Visual acuity beautifully` / `tracks the density…`) 하류로 가고 있었다. 병합할 때 유형은 caption 쪽이 이긴다.

**같은 쪽 안의 이음매** (`pagecheck.py: stitch_pages`)
- 쪽 경계만 보고 있었다. 같은 쪽에서도 앞이 하이픈으로 끝나거나 뒤가 문장을 잇고 있으면 붙인다. 수리 전 157곳 → 0.

**중복 삽입 제거** (`pagecheck.py`)
- 비전이 이미 있는 글을 다시 넣을 때 거르는 규칙이, 한쪽은 문장부호를 벗기고 한쪽은 원문 그대로 비교하고 있었다 — 122건 중 1건만 걸렀다. 낱말 정규화로 바꾸니 93건이 걸렸는데, 이번에는 **기존 글로 시작해서 새 내용으로 이어지는** 블록(1,720자 중 60%가 새 내용)까지 지웠다. 지금은 규칙 하나뿐이다: 덮임률 0.8 이상이면 버린다.

**본문을 도판 조각으로 오인해 버리던 것** (`pagecheck.py: droppable`)
- 전권 첫 실행에서 본문 12블록이 삭제됐다 — 508자 단락과 진짜 캡션 5개 포함. `reads_as_prose` 가 잘린 본문을 못 알아봤다. 기능어 3개 이상이면 본문으로 보고, figcaption 은 25자 넘으면 지키게 했다. 실제 사례 13건으로 대조해 어긋남 0, 재실행에서 본문 오삭제 0.
- 삭제 사유를 구분한다(`furniture` / `figure` / `garbled`) — 러닝 헤드는 3쪽 이상 반복을 요구하지만 도판 조각에는 그 잣대가 맞지 않는다.

**프롬프트를 바꿔도 옛 답이 되살아나던 것** (`pagecheck.py`)
- 캐시 키에 모델은 있는데 **프롬프트가 없었다.** 시스템 프롬프트를 고쳐도 지난 답을 그대로 재생했다. `SYS_FINGERPRINT`(sha1 앞 8자)를 키에 넣었다.

**비전 프롬프트 재작성** (`pagecheck.py: VISION_SYS`)
- 도판 안의 글자(축 이름·눈금·범례·데이터 라벨·축척), 도판 번호(`Figure 4`·`그림 1`·`(b)`), 판독이 뭉갠 글자 덩어리를 **받아쓰지 않는다.** 대신 「**미완성이라는 이유로 블록을 버리지 말 것**」과 「**캡션은 버리지 말 것**」을 못 박았다 — 위의 오삭제가 여기서도 나왔다.

**뉴닉체 레지스터** (`translate.py`, `deslop.py`)
- 해요체·40자 분할·일상어·「우리」·문두 입말 접속사. 지어내기와 이모지, 빈정거림을 다정함으로 바꾸는 것은 금지. deslop 강도는 `약`(문체 자체가 규칙이라 세게 깎으면 도로 평범해진다).

**이어 하기가 처음부터 다시 하던 것** (`translate.py`, `deslop.py`)
- 이미 번역된 블록까지 묶어 보내고 있었다. 빈 것만 묶는다. deslop 도 `ko_raw` 가 없는 것만.
- `--force` 로 다시 번역하면 `ko_raw`(deslop 흔적)가 남아 deslop 이 전량을 건너뛰었다 — 새 번역을 쓸 때 함께 지운다.
- `context_for()` 는 앞 두 블록을 **책 순서로** 집는다. 빈 것만 묶으면 청크 안의 이웃이 실제 이웃이 아니다.

**`repair.py` 신규** — 이미 만들어진 `.parallax` 를 사후 수리한다(중복 삽입 삭제·잘못된 삭제 복원·이음매 병합). `--apply` 는 `.bak` 을 남긴다. 기존 파일에 파이프라인 수정분을 소급할 유일한 길이다.

2026-07-28 두 번째 세션에 고친 것:

**진행 표시** (`pagecheck.py`)
- 20쪽마다 한 번, `pool.map` 순서 소비로 찍던 것을 **쪽마다·완료 순서**(`as_completed`)로. 앱의 진행 표시가 몇 분씩 침묵하던 원인의 절반이다(나머지 절반은 앱의 `-u`).

**판정 보존** (`pagecheck.py` → `export.py` / `parallax_import.py`)
- 판정(쪽별 일치율·단·사유·판독 메모 + 요약 통계)을 `book["page_check"]` 로 내보내고 `page_check` 테이블로 옮긴다. 요약은 `page=0` 행. 리포트 md 는 지워지는 파일이라 이것이 유일한 사본이다.

**번호 목록 항목 강등** (`extract.py: normalize_headings`, `demote_numbered_items`)
- 비전이 굵은 번호로 시작하는 자기평가 문항을 쪽마다 다르게 제목으로 읽는다(실제로 문항 1~4는 본문, 같은 목록의 5~8만 h2 — 쪽이 달랐다). 번호(+마침표)로 시작하면서 60자 이상이면 본문으로 내린다. `1. Introduction` 같은 짧은 번호 제목, `[14]` 마커로 시작하는 진짜 각주는 남는다.
- 같은 규칙을 **footnote 오분류에도, stitch 전에** 적용한다(`demote_numbered_items`). footnote 는 stitch 의 본문 유형이 아니라서, 문항이 footnote 로 붙으면 쪼개진 반쪽이 이어지지 못한 채 하류로 간다 — 챕터 3 문항 구간이 깨진 경로다.

**챕터 3 급 구조 파손 방지** (`extract.py: stitch_blocks`, `pagecheck.py: flag`)
- 분철 하이픈으로 끝난 블록 뒤에 **대문자로 시작하는** 블록은 잇지 않는다. 낱말의 나머지가 아니라, 레이어가 조각을 잃었거나 제목이 섞여 든 것이다(`diffi¬`+`Questions…` → `diffiQuestions` 사고).
- `flag()` 에 구조 신호 둘 추가: 분철 기호(`¬`) 잔존, 낱말 중간 대문자(`[a-z]{3}[A-Z][a-z]{3}` — YouTube·McKinsey 는 안 걸린다). 일치율은 낱말 수준이라 블록이 뒤엉켜도 높게 나와, 이 신호가 없으면 그런 쪽이 비전 수리 대상에서 빠진다.

2026-07-28 첫 세션에 고친 것:

**`print` 가 추출 전체를 죽이던 버그** (`extract.py`, `_llm.py`)
- Windows 콘솔은 cp949 라 `print` 가 코드페이지에 없는 글자에서 예외를 던진다. 진행 문구 하나의 em dash 가 **이미 다 쓴 `book.json` 을 버리게 만들었다** — 파일은 멀쩡한데 exit 1 이라 앱은 추출 실패로 봤다. 조건부로 붙는 글자라 어떤 PDF 에서는 되고 어떤 PDF 에서는 안 됐다.
- 두 곳에서 `sys.stdout.reconfigure(errors="replace")`. 진행 표시는 어떤 경우에도 작업을 실패시킬 수 없어야 한다. 앱은 여기에 더해 `PYTHONIOENCODING=utf-8` 로 띄운다(그러지 않으면 진행 문구의 한글이 깨져 들어온다).

**OCR 레이어 판정** (`extract.py: scan_backed`, `meta.ocr_layer`)
- 기존 신호는 한글 글리프 밀도뿐이라 **한국어 팩으로 OCR 한 경우만** 잡았다. 작업 중인 책이 바로 그 반례다 — 레이어는 판독 산출물인데 밀도가 0이라 「멀쩡함」으로 나왔다.
- 쪽을 통째로 덮는 이미지 위에 글자가 얹혀 있으면(OCR 샌드위치) 그 레이어는 판독 산출물이다. 20쪽을 표본으로 60% 이상이면 참. 이 책은 100%.
- 값을 화면에만 찍고 버리지 않고 `meta.ocr_layer` 로 내보낸다. 앱이 `--trust` 를 권하는 근거가 이것이다.

**키가 없을 때의 실패** (`_llm.py: _client`)
- `ANTHROPIC_API_KEY` 가 없으면 SDK 가 `TypeError` 를 던지는데, 재시도 루프가 이걸 일반 오류로 보고 여섯 번(70초) 물러났다가 역추적을 토했다. 무엇이 없는지는 어디에도 없었다. 먼저 확인하고 한 줄로 죽는다.

2026-07-27에 세 가지를 고쳤다. 상세는 `SKILL.md`와 각 함수 주석.

**러닝 헤더 제거** (`extract.py: strip_running_heads`)
- 줄 번호(`lines[:2]`)가 아니라 **페이지 높이의 위·아래 8% 밴드**로 후보를 고른다.
- 임계값 `max(3, 전체의 25%)` → `max(3, 5%)`. 책은 짝수쪽에만 머리글을 넣거나 장 시작에서 빼므로 20%대가 정상인데, 25% 기준이 191쪽 중 37쪽에 있던 머리글을 통과시켰고 `classify()`가 전부 heading으로 읽어 목차가 도배됐다.
- PDF outline에 있는 제목은 절대 지우지 않는다.

**단락 이어붙이기** (`extract.py: stitch_blocks`)
- 원래 `pagecheck.py`에만 있었다. pagecheck는 쪽당 비전 호출이라 대개 건너뛰므로 끊긴 채로 하류에 넘어갔다. extract로 내려 모델 없이 항상 돈다.
- 페이지 경계는 "앞 블록이 종결부호 없이 끝남"만으로 병합한다(뒤가 소문자일 것까지 요구하면 고유명사 앞 절단을 다 놓친다). 같은 페이지 안은 하이픈이나 소문자 시작을 추가로 요구한다.
- OCR이 분철 하이픈을 `¬`(U+00AC)로 쓰는 것까지 복원한다.

**제목 레벨 정규화** (`extract.py: normalize_headings`, `pagecheck.py` 에서도 호출)
- 폰트 크기 추론은 쪽마다 조판이 달라 흔들리고, `--trust vision` 은 한 페이지씩 따로 읽어 같은 장 제목이 다른 레벨을 받는다. 실제로 `CHAPTER 1`=h3, `CHAPTER 2`=h2, `CHAPTER 3`=h3 이었고 목차에서 장이 절보다 아래에 놓였다.
- `CHAPTER|PART|BOOK|SECTION` + 숫자·로마숫자, 그리고 `CONTENTS`·`INTRODUCTION` 같은 앞뒤 부속물 표제는 **h1**, 나머지 제목은 **h2**. 두 단계면 충분하고, 더 잘게 나누려면 추측해야 하는데 틀린 추측은 평평한 목록보다 나쁘다. `--keep-heading-levels` 로 끌 수 있다.
- 앱의 목차는 h1 만 bold 로 세운다(`reader.css`). 레벨이 두 개뿐이라는 전제 위에 서 있으니 규칙을 바꾸면 CSS 도 함께 봐야 한다.

**`--trust vision`** (`pagecheck.py`)
- 기존 설계는 "글자는 텍스트 레이어가 정답, 구조는 비전이 정답"이라 판독 텍스트를 일치율 계산에만 쓰고 버렸다. **레이어 자체가 OCR 산출물이면 그 전제가 거짓이다.**
- 이 모드는 전 페이지를 이미지에서 다시 받아쓴다. 밀려난 레이어 텍스트는 `superseded`에 보관하고 새 블록에 `from_ocr`가 붙는다.
- **레이어가 멀쩡한 책에는 쓰지 마라.** 비전은 고어 철자를 현대화하고 저자의 의도적 오기를 고친다. 먼저 `--pages 1-20`으로 시험할 것.

---

## 이 PC 환경에서 물린 것

**`requirements.txt`의 `truststore`를 걷어내지 말 것.** 네트워크 구성에 따라 파이썬만 전 API 호출이 `CERTIFICATE_VERIFY_FAILED`로 죽는 환경이 있다 — 브라우저와 Electron은 멀쩡히 통하는데 파이썬만 죽으므로 키 문제로 오진하기 쉽다. `truststore`는 인증서 검증을 OS에 위임한다. 검증을 끄는 것이 아니라 이 PC의 나머지 프로그램과 같은 판단을 하게 하는 것이다. `SSL_CERT_FILE`로 루트를 추가하는 우회는 통하지 않는 경우가 있다.

**`python3`는 Store 스텁.** 이 PC의 `python3.exe`는 0바이트 재파싱 지점(Microsoft Store 별칭)이다. 진짜 파이썬은 `C:\Python314\python.exe`이고 `python`으로 잡힌다. 스킬의 `run.sh`는 `python3`를 부른다.

**`npm run dist` 는 산출물 잠금을 스스로 푼다.** 이 PC 에는 asar 뷰어 Orca 가 깔려 있고 `.asar` 파일 연결을 잡고 있다. 빌드가 새 `release\win-unpacked\resources\app.asar` 을 만드는 순간 **explorer.exe 가 Orca 를 띄우고**(부모가 explorer 인 것을 확인했다) 그 프로세스가 파일을 물어, 다음 빌드가 출력 폴더를 지우지 못하고 `remove … app.asar: The process cannot access the file` 로 죽었다. 오류 문구 어디에도 원인이 없다.

`scripts/clean-release.mjs` 가 `electron-builder` 앞에 끼어 폴더를 비운다. 몇 번 기다려 보고(검사기가 잠깐 잡은 것이면 저절로 풀린다), 그래도 잠겨 있으면 **Restart Manager 에 누가 물고 있는지 직접 물어**(`scripts/who-locks.ps1`) 지목된 프로세스만 닫는다. 이름으로 짐작하지 않으므로 상관없는 앱을 건드리지 않고, 닫은 것은 반드시 찍는다. 연속 두 번 실행으로 확인했다.

Restart Manager 를 쓸 때 물린 것 둘 — **디렉터리를 섞어 넘기면 등록이 조용히 실패해** 목록이 통째로 비어 나온다(파일만 넘길 것). 그리고 `powershell -File script -Paths a b c` 는 첫 하나만 묶고 나머지를 위치 인자로 흘려 죽으므로, 경로 목록은 **파일로** 넘긴다.

**PowerShell 로 UTF-8 한글 파일을 건드리지 말 것.** 2026-08-02 에 `(Get-Content …) | Set-Content -Encoding utf8` 로 스킬의 `references/registers.md` 를 **되돌릴 수 없게 망가뜨렸다.** PowerShell 5.1 의 `Get-Content` 는 UTF-8 을 cp949 로 읽어 그 시점에 글자를 잃는다 — 다시 utf8 로 써도 잃은 것은 돌아오지 않고, 되돌리려는 시도는 BOM(`﻿`)과 `\x80` 만 더 남겼다. 원본은 `pdf-ko-translate.zip` 에서 꺼내 살렸다. **파일 편집은 Read/Write/Edit 도구나 파이썬으로 한다.** zip 정본이 유일한 구명줄이었다는 점도 기억할 것.

**백그라운드 작업 출력을 `Select-Object -Last N` 으로 거르지 말 것.** 파이프가 전체를 버퍼링해 로그 파일이 0바이트로 남는다 — 전권 deslop 이 1117개 중 758개에서 죽었을 때 무슨 일이 있었는지 볼 수 없었다.

**DRM 컨테이너를 PDF로 착각하지 말 것.** 원래 있던 `the_meaning_of_your_life.pdf`(76MB)는 매직 바이트가 `%PDF-`가 아니라 `SCDSA004`인 전자책 DRM 컨테이너였다. PyMuPDF가 열지 못한다. 2026-07-27에 삭제했고, 쓸 수 있는 파일은 `the_meaning_of_your_life_compressed.pdf`(38.6MB, 진짜 `%PDF-1.5`)다. 새 원본을 받으면 먼저 앞 8바이트를 확인하라.

---

## 접미사 ID — 파서가 삼키던 버그

pagecheck가 삽입·재구성한 블록은 접미사 ID를 갖는다(`b0052a`). 한 페이지에 여러 개가 들어가면 `suffix_id()`가 **직전 ID에 다시 글자를 붙여** `b0052aa`, `b0052aaa`로 자란다.

양쪽 와이어 파서가 이걸 감당하지 못했다:

| 위치 | 기존 | 결과 |
|---|---|---|
| `src/main/translate/prompt.ts` | `[a-p]?` — 한 글자만 | 두 글자 이상 ID의 번역이 응답에서 버려짐 |
| 스킬 `scripts/_llm.py` | 접미사 불허 | CLI 번역에서 pagecheck 산 블록이 전부 누락 |

둘 다 `[a-p]*`로 고쳤다. **실패가 조용하다** — 블록이 그냥 안 채워질 뿐이라 재실행해도 계속 비어 있고, 원인은 `verify.py`가 잡아 줄 때까지 보이지 않는다. 실제로 전량 번역이 929/956에서 멈춘 덕분에 드러났다. 앞으로 번역이 특정 개수에서 멈추면 여기를 먼저 의심하라.

---

## 2026-07-28 오후 — 이름 바뀐 파일을 유실로 오판한 소동

사용자가 작업 파일을 `the_meaning_of_your_life_v2.parallax` → `the_meaning_of_your_life_compressed.parallax` 로 **개명**했는데, 세션이 옛 이름으로 `sqlite3.connect` 하면서 0바이트 빈 파일이 생겼고 이를 유실로 오판해 md 스냅샷에서 재건까지 했다. 원본은 처음부터 무사했다. 교훈 둘: **`sqlite3.connect` 는 없는 파일을 소리 없이 만든다** — 데이터 파일은 열기 전에 존재부터 확인하라(`mode=ro` URI 로 열면 이런 사고가 없다). 그리고 **파일이 없어 보이면 지웠는지보다 이름이 바뀌었는지를 먼저 의심하라.** 잘못 만든 재건본 v2 는 앱이 물고 있어 못 지웠다 — 앱 종료 후 삭제할 것. md 스냅샷 쌍(`.en.md`/`.ko.md`)은 유일한 백업 수단으로 유효하니 남겨 둔다.

챕터 3 「Questions for Reflection and Self-Assessment」 구간이 깨져 있던 것은 진짜였고, **원본 파일에** PDF 67쪽과 대조해 수리했다: 66→67쪽 경계에서 단어 중간(`diffi|cult`)으로 잘린 단락 복원, 문항에 파묻힌 제목을 별도 h2(`b0429a`)로, 쪼개진 문항 8개 병합, `¬` 분철·OCR 쓰레기(`'tri`) 정리, 문항 유형 footnote→p 교정, 제목 번역을 감싼 `**…**` 제거. 최종 948블록.

깨진 원인: 67쪽 블록들의 플래그가 0 — **이 쪽은 재판독을 거치지 않은 생 텍스트 레이어였다.** 일치율은 낱말 수준이라 블록이 뒤엉켜도 높게 나와 수리 그물을 빠져나갔다. 재발 방지로 스킬에 세 가지를 넣었다(아래 스킬 상태 참조).

---

## 남아 있는 데이터 파일

**두 번째 책이 생겼다 — `the_mind_is_flat.noads.parallax`**(Nick Chater, 『마음은 평평하다』). 광고를 걷어낸 PDF 244쪽에서 전 공정을 다시 돌린 결과이고 이것이 정본이다. 추출 1,521블록·이음 2,635·거름 269 → pagecheck 1,492블록(유형 교정 326, 병합 40, **본문 오삭제 0**) → 용어집 77 → 번역 1,117/1,117(뉴닉체) → deslop 전량(강도 `약` 에서 손댄 것 11) → 내보내기. 검증: 문장 중간 끊김 0, 번역 대상에 남은 도판 쓰레기 0. 문체 확인 — 845단락 중 **`-다` 로 끝나는 것 0개**, 해요체 635, 문두 입말 접속사 118. md 스냅샷은 `the_mind_is_flat.noads.{en,ko}.md`.

그 앞선 판(`the_mind_is_flat.parallax`, 광고 포함)은 사후 수리한 것이다 — 1569→1391블록(중복 삽입 97 삭제, 81쌍 병합, 잘못된 삭제 234 복원, 같은 쪽 중간 끊김 157→0), 이어서 미번역 208개를 채웠다(기존 901개는 바이트 단위로 그대로). `repair.py` 가 여기서 나왔다.

첫 작업 파일은 **`the_meaning_of_your_life_compressed.parallax`** 다(945블록, 번역 전량, 쪽 번호·플래그 온전 — 챕터 3 수리 직후는 948이었고 이후 중복 잔해 3블록 삭제로 945). 2026-07-28 오후에 사용자가 v2 에서 개명했다. 같은 날 손본 것: 챕터 7 자기평가 문항 5~8 유형 교정(PDF 151쪽 대조)·중복 잔해 블록 삭제, 번역 앞 `h2 ` 토큰 제거, 챕터 3 문항 구간 수리(위 참조). `*.en.md`/`.ko.md`(13:50)는 백업 — 지우지 말 것. 재건 잔재 `the_meaning_of_your_life_v2.parallax` 는 2026-07-30에 지웠다(내용이 md 스냅샷 재건이라 스냅샷이 남아 있는 한 잃는 것이 없다 — 쪽 번호 0개가 그 증거였다).

시험 산출물 `pagecheck_test_20p.pdf`(원본 앞 20쪽 발췌, 2026-07-30 재생성)와 앱 데이터 폴더의 `pagecheck_test_20p_pdf_*.parallax` 는 pagecheck 재검증용으로 남겨 뒀다.

2026-07-27에 정리하면서 구 추출본(`the_meaning_of_your_life.parallax`, 1,141블록·번역 943개)과 `README.parallax` 시험 산출물, DRM PDF, 스킬 백업 zip을 지웠다.

구 추출본에 번역이 943개 있었지만 블록 ID가 달라 v2로 옮길 수 없었다. v2는 2026-07-27에 앱 스케줄러로 956개 전량을 새로 번역했다(실측 단가 $0.002/블록, 총 $2 안팎). 빈 번역 0개, 접미사 ID 블록 88개도 전부 채워졌다. 한글이 없는 번역 3건은 판권면·인쇄 코드·OCR 조각으로 의도된 결과다.

`.parallax`와 `.pdf`는 `.gitignore`에 있다 — 원문이 전량 들어가고, `.parallax`는 번역이 진행될 때마다 통째로 바뀌는 SQLite라 히스토리에 넣으면 급격히 커진다.

---

## 열려 있는 것

- 기존 작업 파일(`the_meaning_of_your_life_compressed.parallax`)에 그림 소급 — 그림 있는 쪽만 Datalab 재판독해 asset·figure 만 끼워 넣는 스크립트 미착수(본문 블록은 건드리면 안 된다 — 번역 전량이 실려 있다).
- `tts.zip` 을 히스토리에 넣을지 미결 (`vendor/` 는 커밋됨 — 08-05 병합 시 확인).
- 낭독의 유휴 종료(T4 나머지). Voice Builder 접수 마감 2026-08-31.
- 상하 배치에서 본문 폭(`--maxw`)이 좌우와 같아 줄이 길다 — 조판 컨트롤로 줄일 수 있지만 기본값을 배치마다 달리할지는 미결.
- E5 의 나머지: 용어집 편집기 미착수.
- `spec.md` §10의 E6(`.parallax` 내보내기, 문서 라이브러리) 미착수.
- 블록 단위 재번역 UI 없음(백엔드 `blocks.reset`은 있다).
- 웹폰트를 CDN에서 받는다. 오프라인이면 로컬 대체 서체로 물러난다.
- 스킬의 `glossary`·`translate`·`deslop`·`verify`는 남겨 뒀다. "문서추출만 축소"를 렌더링 제거로 해석했고, README가 "밤새 전량 번역은 CLI가 싸다"는 왕복을 전제하기 때문이다. 번역 단계까지 들어낼지는 미결.
