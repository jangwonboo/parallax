# Parallax TTS — 사양

Parallax 리더에 낭독 기능을 붙인다. 엔진은 **Supertonic 3**, 실행은 **Node.js 인프로세스**(파이썬 없음).

이 문서는 `parallax/spec.md` 의 자매 문서다. 앱 본체 사양은 그쪽 §1~§12 를 따르고, 여기서는 TTS 만 다룬다. 단계 번호는 본 사양의 `E1~E6` 과 겹치지 않도록 **`T1~T5`** 를 쓴다.

최초 작성: 2026-08-03

---

## 1. 무엇을 만드나

좌우 대역 리더에서 **한국어 번역문(또는 영문 원문)을 소리 내어 읽는다.** 스크롤 구동 번역과 같은 방식으로, 재생 헤드가 지나가는 자리를 앞질러 합성한다.

**범위 안**

- 블록 → 청크 분할 → 순차 합성 → 끊김 없는 연속 재생
- 재생 중인 블록 표시와 따라가기 스크롤
- 음성·속도·품질·낭독 대상(한/영) 설정
- 재생 헤드가 번역 스케줄러의 우선순위를 끌어올림

**범위 밖 (v1)**

- 오디오 파일 내보내기 (`.wav`/`.m4b`)
- 커스텀 보이스(Voice Builder 산출물) — §3.4 참조
- expression 태그(`<laugh>` 등) — 엔진은 지원하나 책 낭독에 쓸 데가 없다
- 단어 단위 하이라이트 — 엔진이 정렬 정보를 내놓지 않는다 (§5.5)

---

## 2. 결정 — Node 인프로세스

### 2.1 무엇을 골랐나

`supertonic serve`(파이썬 HTTP 사이드카)를 쓰지 않는다. **`onnxruntime-node` 로 ONNX 를 Electron 안에서 직접 돌린다.**

### 2.2 얻는 것

| | 파이썬 사이드카 | **Node 인프로세스** |
|---|---|---|
| 런타임 의존 | 파이썬 3 + venv + pip | **없음** (앱에 이미 있는 Node) |
| 배포 | 사용자가 파이썬을 따로 깐다 | **설치본 하나** |
| 이 PC 의 `truststore` 함정 | 물린다 (§9.1) | **비껴간다** |
| 기동 시간 | 프로세스 + uvicorn + 모델 | 모델만 |
| 포트·수명주기 | 포트 선택·readiness 폴링·손자 프로세스 정리 | 없음 |
| 실패 지점 | 파이썬 탐색, 패키지 설치, 포트, HTTP | 모델 로딩뿐 |

PDF 열기가 이미 `pdf-ko-translate` 스킬(파이썬) 설치를 요구한다. **읽기 기능까지 같은 종류의 의존을 얹으면 앱이 파이썬 없이는 아무것도 못 하는 물건이 된다.** 낭독은 PDF 변환과 달리 상시 기능이라 이 차이가 크다.

### 2.3 잃는 것

- **엔진 교체가 어려워진다.** 사이드카였다면 OpenAI 호환 엔드포인트라 URL 과 키만 바꾸면 클라우드 TTS 로 갈아탈 수 있었다. 인프로세스는 §7 의 `Synth` 인터페이스를 경계로 삼아 이를 되찾는다 — 구현체를 갈아 끼울 자리를 처음부터 남긴다.
- **엔진 코드를 우리가 떠안는다.** §2.4 참조.
- **GPU 를 못 쓴다.** 현재 `loadTextToSpeech(dir, useGpu=true)` 는 `GPU mode is not supported yet` 로 던진다. 사이드카라도 마찬가지라 실질적 손해는 아니다.

### 2.4 코드를 베껴 온다 — 선택지가 없다

**supertonic 의 Node 구현은 npm 패키지가 아니다.** 저장소 `nodejs/` 는 `"name": "tts-onnx-nodejs"` 의 예제 프로젝트이고 npm 에 발행돼 있지 않다. 파이썬만 `pip install supertonic` 로 패키지가 있다.

따라서 `nodejs/helper.js`(559줄, MIT)를 **저장소 안으로 옮겨 TS 로 포팅한다.** 이건 우회가 아니라 유일한 경로다. 어차피 다음을 고쳐야 하므로 손해도 적다:

- `Array.from(...)` 로 오디오를 일반 JS 배열에 담는 것 → `Float32Array` 유지 (§5.4)
- `console.log` 제거
- 취소 지점 추가 (§7.3)
- 배치 경로(`batch()`) 삭제 — 우리는 언제나 단일 화자·단일 문장이다

**상류 저장소는 아카이브 예정이다.** 베껴 온 시점의 커밋 해시를 파일 머리에 남긴다. 갱신될 상류가 없으므로 이는 부채가 아니라 사실상의 동결이다.

---

## 3. 자산

### 3.1 파일과 크기 (실측, `huggingface.co/Supertone/supertonic-3`)

```
onnx/vector_estimator.onnx      256.5 MB   ← 스텝마다 도는 뜨거운 고리
onnx/vocoder.onnx               101.4 MB
onnx/text_encoder.onnx           36.4 MB
onnx/duration_predictor.onnx      3.7 MB
onnx/unicode_indexer.json         278 KB   ← 토크나이저
onnx/tts.json                     8.3 KB   ← 설정(sample_rate 등)
                                ─────────
                                 ~398 MB

voice_styles/{M1..M5,F1..F5}.json  각 ~292 KB, 합계 2.9 MB
```

**99M 파라미터인데 398MB 인 이유는 fp32 이기 때문이다**(99M × 4B ≈ 396MB). 공개 자산에 int8·fp16 판은 없다. 네 세션을 모두 올리면 RSS 가 400MB 이상 늘어난다 — Electron 앱으로서 가볍지 않은 값이고, §4 의 프로세스 분리를 정당화하는 근거이기도 하다.

### 3.2 어디에 두나

```
app.getPath("userData")/tts/
  onnx/          ← 6개 파일
  voice_styles/  ← 10개 파일
  VERSION        ← 받아온 HF 리비전 해시
```

**설치본에 동봉하지 않는다.** 이유 셋:

1. 설치본이 400MB 커진다. 낭독을 안 쓰는 사용자에게 부과할 값이 아니다.
2. 모델 가중치는 **OpenRAIL-M** 이다(앱과 예제 코드는 MIT). 동봉해 배포하면 사용제한 조항을 함께 실어야 한다. 받아 쓰면 그 관계가 사용자와 Supertone 사이에 남는다.
3. `.parallax`·`.pdf` 를 `.gitignore` 에 둔 것과 같은 판단이다 — 언제든 다시 받을 수 있는 것은 보관하지 않는다.

### 3.3 받는 방법

**Node 가 받는다**(§9.1 — 파이썬이 받으면 이 PC 에서 인증서로 죽는다). HF 는 인증 없이 직접 받을 수 있다:

```
https://huggingface.co/Supertone/supertonic-3/resolve/<rev>/onnx/vocoder.onnx
```

- `<rev>` 는 `main` 이 아니라 **고정 리비전 해시**를 쓴다. 상류가 아카이브되면 `main` 이 움직일 일은 없지만, 움직였을 때 조용히 다른 모델을 받는 것보다 낫다.
- 파일마다 `Content-Length` 로 진행률을 낸다. 400MB 는 진행 표시 없이 기다리게 할 분량이 아니다 — `import:progress` 와 같은 방식으로 `tts:progress` 를 낸다.
- 부분 다운로드는 `.part` 로 받고 완료 시 이름을 바꾼다. 중간에 끊긴 파일을 정상으로 오인하면 ONNX 로딩이 알 수 없는 이유로 실패한다.
- 검증은 크기 비교로 충분하다. HF API(`/api/models/.../tree/main/onnx`)가 바이트 수를 준다.

### 3.4 Voice Builder — 기한

**2026-08-31 에 닫힌다.** 커스텀 보이스(자기 목소리·특정 화자)를 쓸 생각이 있으면 그 전에 JSON 을 뽑아 둬야 한다. 이후에는 프리셋 10종만 남는다.

산출물은 `voice_styles/*.json` 과 같은 형식(`style_ttl`·`style_dp` 텐서)이라, **받아만 두면 앱 코드는 그대로 읽는다.** v1 범위 밖이지만 자산 확보는 지금 해야 하는 일이다.

---

## 4. 프로세스 구조 — 여기가 핵심

### 4.1 합성은 별도 프로세스에서 돈다

**`utilityProcess.fork()` 로 TTS 전용 자식을 띄운다.** main 에서 직접 돌리면 안 된다.

ONNX Runtime 의 `session.run()` 자체는 네이티브 스레드풀로 빠지지만, **그 앞뒤의 JS 는 전부 호출한 스레드에서 돈다**:

- `sampleNoisyLatent()` — Box-Muller 로 `latentDim × latentLen` 개의 정규난수를 순수 JS 로 생성
- `arrayToTensor()` — `array.flat(Infinity)` 후 `Float32Array.from()`
- 보코더 출력 `Array.from(result.wav_tts.data)` — 44.1kHz 라 **1초당 44,100개** 
- 16-bit PCM 변환 — 샘플마다 `writeInt16LE`

10초짜리 문장이면 이 마지막 둘만으로 441,000회 반복이다. main 에서 돌리면 **재생 중에 UI 가 멈춘다.** 툴바도, 스크롤도, 번역 결과 반영도.

`worker_threads` 가 아니라 `utilityProcess` 인 이유:

- **크래시 격리** — ONNX 세션이 죽어도 앱이 아니라 낭독만 죽는다. 다시 fork 하면 된다.
- **메모리 분리** — 400MB 가 main 의 힙 밖에 있다. 낭독을 안 쓰면 그 프로세스가 없다.
- **기존 정신 모델과 같다** — 사이드카는 남고 파이썬만 빠진 것이다.

대가는 오디오가 프로세스 경계를 넘는 복사다. 문장당 ~500KB~1MB 이고 초당 한 번 미만이라 무시할 수 있다.

### 4.2 프로세스 지도

```
renderer                main                       tts utilityProcess
────────                ────                       ──────────────────
                        SpeechQueue
tts.play(id) ──IPC──▶   ├ 청크 분할
                        ├ 선행 합성 요청 ──port──▶  Synth
                        │                          ├ 4× ort.InferenceSession
                        │                          ├ chunkText / UnicodeProcessor
                        │                          └ Float32Array → WAV Buffer
                        ◀────────────port────────── { chunkId, wav: Buffer }
   ◀──IPC(Uint8Array)───┤
Blob → <audio>          │
  ended ──IPC──────────▶└ 다음 청크 + "tts:pos" 방송
```

**렌더러는 네트워크에도 파일시스템에도 나가지 않는다.** 사전 조회(`dict:lookup`)를 main 이 대신하는 기존 원칙 그대로다.

### 4.3 수명주기

| 시점 | 동작 |
|---|---|
| 앱 시작 | 아무것도 안 한다. 자산 존재 여부만 확인해 `tts:status` 에 싣는다 |
| 첫 재생 요청 | fork → 모델 4개 로드(수 초) → `ready` 방송. 그 사이 렌더러는 「음성 준비 중」 |
| 재생 중 | 상주 |
| 유휴 5분 | **종료한다.** 400MB 를 안 쓰면서 들고 있을 이유가 없다. 다음 재생에서 다시 로드한다 |
| 문서 닫기 / 앱 종료 | `kill()`. `before-quit` 에도 건다 |

유휴 종료의 대가는 재개할 때 모델 로딩이 다시 드는 것이다. **로딩 시간을 T1 에서 실측한 뒤 5분을 조정한다** — 로딩이 10초를 넘으면 유휴 종료가 오히려 거슬린다.

---

## 5. 합성

### 5.1 파이프라인

`helper.js` 의 `_infer()` 가 하는 일:

```
텍스트 ──UnicodeProcessor──▶ 유니코드 인덱스 + 마스크
                                  │
   ┌──────────────────────────────┤
   ▼                              ▼
duration_predictor           text_encoder
   │ (duration)                   │ (text_emb)
   ▼                              │
noisy latent 샘플링 ◀─────────────┤
   │                              │
   ▼                              ▼
vector_estimator ×  totalStep 회 반복   ← 뜨거운 고리 (256MB 모델)
   │
   ▼
vocoder ──▶ wav (float32, 44.1kHz, mono)
```

**g2p 도, espeak 도, 사전도 없다.** 텍스트를 유니코드 코드포인트로 인덱싱해 곧장 먹인다. 언어별 외부 의존이 하나도 없다는 뜻이고, 이것이 이 모델을 붙일 만하게 만드는 성질이다.

`speed` 는 `duration` 을 나누는 방식으로 들어간다(0.7~2.0). `totalStep` 은 `vector_estimator` 반복 횟수다(5~12) — **지연과 품질의 유일한 손잡이이며, 비용이 선형이다.**

### 5.2 청크 — 상류의 분할을 그대로 쓴다

직접 문장 분할기를 쓰지 않는다. `helper.js` 의 `chunkText(text, maxLen)` 를 그대로 가져온다:

- 문단(`\n\s*\n+`) → 문장(`.!?` 뒤 공백, `Mr.`·`e.g.`·단일 대문자 약어 제외) 순으로 자르고, `maxLen` 이내로 다시 묶는다
- **`maxLen` 은 한국어·일본어 120, 나머지 300**

**중요 — 이렇게 하면 장문 경로와 출력이 같다.** 상류의 `call()` 은 내부적으로 `chunkText` 로 자른 뒤 청크마다 `_infer` 하고 0.3초 무음으로 이어 붙인다. 우리는 같은 분할을 쓰되 이어 붙이지 않고 **청크마다 따로 재생**할 뿐이다. 즉 스트리밍이 음질을 깎지 않는다 — 어차피 상류도 문장을 따로 합성한다.

청크 사이 0.3초 무음은 **재생기가 만든다**(§6.2). 오디오에 굽지 않는다 — 마지막 청크 뒤에 무음이 붙는 것을 피하고, 사용자가 간격을 조절할 여지를 남긴다.

**첫 청크만 짧게.** 재생 시작 지연은 첫 청크 길이에 비례한다. 세션의 첫 청크는 `maxLen` 을 60(한)/150(영)으로 낮춰 소리를 먼저 내고, 이후는 정상값으로 돌아간다.

### 5.3 청크 ID

```
b0052#0   b0052#1   b0052a#0
└블록 ID┘ └순번┘
```

`#` 를 쓰는 이유는 **접미사 ID(`b0052a`, `b0052aa`)와 충돌하지 않기 때문이다.** 본 사양의 와이어 파서가 `[a-p]*` 로 물려 있으므로 영문자 구분자는 위험하다. 청크 ID 는 저장되지 않고 세션 안에서만 산다.

### 5.4 상류에서 반드시 고칠 것

| `helper.js` | 문제 | 고침 |
|---|---|---|
| `Array.from(vocoderResult.wav_tts.data)` | 10초 오디오 = 441,000 요소의 일반 JS 배열(~3.5MB 힙 + GC) | `Float32Array` 그대로 쓴다 |
| `wavCat = [...wavCat, ...silence, ...wav]` | 청크마다 전체 재복사 (O(n²)) | 이어 붙이지 않는다 (§5.2) |
| `writeWavFile()` 이 파일로 쓴다 | 우리는 메모리 버퍼가 필요하다 | 헤더 생성부만 떼어 `wavBuffer(f32, rate): Buffer` |
| `console.log` | utilityProcess stdout 오염 | 제거 |
| `Math.random()` 노이즈 | 같은 문장도 매번 미세하게 다르다 | 그대로 둔다 — 대신 **캐시를 신뢰하지 않는다**(§5.6) |
| `batch()`, `--use-gpu` | 우리 경로에 없다 | 삭제 |

`package.json` 의 `fft.js`·`js-yaml` 은 **`helper.js` 가 실제로 쓰지 않는다**(README 가 언급하는 `Preprocessor`/STFT 는 현재 파일에 없다). 가져오지 않는다. 실질 의존은 **`onnxruntime-node` 하나**다.

### 5.5 단어 단위 하이라이트은 못 한다

`duration_predictor` 는 **발화 전체 길이 하나**를 낸다. 음소·단어별 정렬(alignment)은 출력에 없다. 블록·청크 단위 표시가 v1 에서 가능한 최대 해상도다.

청크가 문장 하나 정도라 실사용에서는 충분하다. 단어 단위가 필요해지면 강제 정렬기를 따로 붙여야 하는데 그건 별개 프로젝트다.

### 5.6 캐시하지 않는다

**합성 결과를 `.parallax` 에도, 디스크에도 저장하지 않는다.**

- `.parallax` 는 번역이 진행될 때마다 통째로 바뀌는 SQLite 다. WAV 는 텍스트보다 두 자릿수 크다 — 넣으면 파일이 못 쓰게 커진다.
- 노이즈가 매번 달라(§5.4) 같은 입력이 같은 출력을 주지 않는다. 캐시의 전제가 성립하지 않는다.
- 재합성이 충분히 싸면 캐시는 순손해다. **T1 에서 실측해 판정한다** — 문장당 합성이 재생 길이의 절반 이하면 캐시는 필요 없다.

메모리에 **앞뒤 3청크만** 들고 나머지는 버린다.

---

## 6. 재생

### 6.1 큐 — 번역 스케줄러와 성격이 다르다

`SpeechQueue` 를 `translate/scheduler.ts` 에서 복사하지 말 것. 닮은 것은 겉모양뿐이다.

| | `Scheduler`(번역) | `SpeechQueue`(음성) |
|---|---|---|
| 순서 | 우선순위 큐, 순서 무관 | **엄격한 재생 순서** |
| 동시성 | 4 | **1** (CPU 포화) |
| 선행 | 뷰포트 기준 | 재생 헤드 +2청크 |
| 취소 | 없음 | **필수, 즉시** |
| 결과 | DB 영구 저장 | 휘발 |
| 비용 | $ 추적 | CPU |
| 실패 | 조용히 빈칸 | **재생이 멈춘다 — 보여야 한다** |

동시성이 1인 이유: 세션이 하나뿐이고 CPU 를 포화시킨다. 둘을 동시에 돌리면 둘 다 느려져 첫 소리가 늦어진다.

### 6.2 렌더러 재생기

**`<audio>` 두 개를 번갈아 쓴다.** 하나가 울리는 동안 다음 청크 Blob 을 다른 하나에 물려 둔다. `ended` 에서 즉시 `play()` 하면 문장 사이가 매끄럽다.

- 청크 간 간격 0.3초는 `ended` 후 `setTimeout` 으로 준다. 블록 경계에서는 0.5초(단락 호흡).
- `<audio>` 를 매번 새로 만들지 않는다 — Blob URL 은 `revokeObjectURL` 로 반드시 회수한다. 안 하면 세션이 길어질수록 메모리가 샌다.

### 6.3 하이라이트 — 가상 스크롤이 함정이다

`renderWindow()` 가 뷰포트 밖 행을 `el.remove()` 한다. **재생 중인 행에 클래스를 붙여 두면 스크롤 한 번에 사라진다.**

```js
let playingId = null;      // 상태는 DOM 이 아니라 여기 산다

// makeRow(b) 안에서
if (b.id === playingId) row.classList.add("playing");
```

마운트될 때마다 복원된다. `tts:pos` 를 받으면 `playingId` 를 갱신하고 이전·현재 행이 마운트돼 있으면 클래스만 옮긴다.

### 6.4 따라가기 스크롤

기본 켬. 재생 블록을 화면 중앙 근처에 유지한다.

**사용자가 직접 스크롤하면 3초간 억제한다.** 없으면 앞 문단을 되짚어 보려 할 때마다 재생 위치로 끌려가 읽을 수가 없다. 억제 중에는 툴바에 「재생 위치로」 버튼을 띄워 되돌아갈 길을 준다.

`anchor()`/`restore()` 가 이미 있으므로 스크롤 위치 계산은 그것을 쓴다.

---

## 7. 인터페이스

### 7.1 `preload/index.ts` — 렌더러 표면

```ts
tts: {
  status:  () => ipcRenderer.invoke("tts:status"),
  install: () => ipcRenderer.invoke("tts:install"),      // 자산 내려받기
  play:    (blockId: string) => ipcRenderer.invoke("tts:play", blockId),
  stop:    () => ipcRenderer.invoke("tts:stop"),
  pause:   (v: boolean) => ipcRenderer.invoke("tts:pause", v),
  ended:   (chunkId: string) => ipcRenderer.invoke("tts:ended", chunkId),
  voices:  () => ipcRenderer.invoke("tts:voices"),
},
```

`on()` 의 `allowed` 배열에 **반드시 추가**한다. 지금 구조는 목록에 없는 채널을 `unknown channel` 로 던진다:

```ts
const allowed = [
  "doc:opened", "block:updated", "import:progress", "stats", "keys:prompt",
  "tts:state",     // { phase: "idle"|"loading"|"ready"|"playing"|"paused"|"error", message? }
  "tts:progress",  // { got: number, total: number }  — 자산 내려받기
  "tts:chunk",     // { chunkId, blockId, wav: Uint8Array }
  "tts:pos",       // { blockId, chunkId }
];
```

### 7.2 `TtsStatus`

```ts
export interface TtsStatus {
  installed: boolean;      // 자산이 다 있나
  bytesNeeded: number;     // 없으면 받아야 할 크기 (~401MB)
  ready: boolean;          // 모델이 올라와 있나
  voice: string;           // "F1"
  voices: string[];        // ["M1"..."M5","F1"..."F5"] + 커스텀
}
```

### 7.3 utilityProcess 경계 — 교체 가능한 자리

```ts
// src/main/tts/synth.ts — 이 인터페이스가 엔진 교체점이다 (§2.3)
export interface Synth {
  load(): Promise<void>;
  chunk(text: string, lang: Lang, first: boolean): string[];
  speak(text: string, lang: Lang, opt: SpeakOpt): Promise<Float32Array>;
  cancel(): void;
  dispose(): void;
}
```

**`cancel()` 은 진행 중인 합성을 실제로 끊어야 한다.** `vector_estimator` 반복 사이에 취소 플래그를 확인한다 — 스텝 하나가 최소 단위다. 세션 자체를 죽이지 않는 한 그보다 잘게 끊을 수 없고, 그럴 필요도 없다.

### 7.4 CSP — 지금 상태로는 막힌다

`renderer/index.html` 에 `media-src` 가 없어 `default-src 'self'` 로 떨어진다. **`blob:` 오디오는 재생되지 않는다.**

```diff
- img-src 'self' data:; script-src 'self'; connect-src 'none'
+ img-src 'self' data:; media-src 'self' blob:; script-src 'self'; connect-src 'none'
```

`connect-src 'none'` 은 그대로 둔다 — 오디오는 IPC 로 들어오지 fetch 로 들어오지 않는다.

---

## 8. 설정과 연동

### 8.1 `settings.json`

```jsonc
"tts": {
  "side": "ko",          // "ko" | "src" — 어느 열을 읽나
  "voice": "F1",
  "speed": 1.05,         // 0.7 ~ 2.0
  "steps": 8,            // 5 ~ 12
  "gapMs": 300,          // 청크 사이
  "blockGapMs": 500,     // 블록 사이
  "follow": true,        // 따라가기 스크롤
  "idleExitSec": 300     // utilityProcess 유휴 종료
}
```

기존 `settings:get`/`settings:set` 인프라를 그대로 쓴다. 새 저장소는 만들지 않는다.

### 8.2 번역 스케줄러와의 연동 — 이 설계의 이득

**재생 헤드는 뷰포트와 나란한 두 번째 우선순위 구동원이다.**

```js
// 재생 헤드 앞 N블록을 우선순위 0으로 당긴다
api.translate.request(idsAhead, 0);
```

한국어를 낭독하려면 그 블록이 번역돼 있어야 한다. **미번역 블록에 닿으면 멈추고 기다린다** — 「번역 대기 중」을 표시하고, 채워지면 이어서 읽는다.

영문을 대신 읽는 대안은 버린다. 언어가 문장 중간에 튀면 듣기 나쁘고, 사용자가 무엇을 듣고 있는지 헷갈린다.

`side: "src"` 일 때는 이 연동이 통째로 없다 — 원문은 언제나 있다.

### 8.3 읽지 않는 블록

`NO_TRANSLATE`·`DROPPED` 플래그가 붙은 블록은 건너뛴다. `table_raw`·`figcaption` 도 마찬가지다 — 표 원본과 도판 설명을 소리 내어 읽으면 흐름이 끊긴다.

제목(`h1`~`h3`)은 읽되 **뒤에 blockGap 을 한 번 더 준다.**

---

## 9. 이 PC 에서 물릴 것

### 9.1 `truststore` 문제를 비껴간다

CONTEXT.md 에 기록된 함정: 이 PC 는 **파이썬만** 전 API 호출이 `CERTIFICATE_VERIFY_FAILED` 로 죽는다. 브라우저와 Electron 은 멀쩡하다.

400MB 자산 내려받기를 파이썬이 했다면 정면으로 물렸을 문제다. **Node 가 받으므로 해당 없다.** §2.2 표의 항목 중 이 PC 에서 가장 실질적인 이득이다.

`onnxruntime-node` 는 설치 시 네이티브 바이너리를 내려받지만(`adm-zip`·`global-agent` 의존이 그 증거), 이 역시 Node 경로라 안전하다.

### 9.2 `asarUnpack` 이 필요하다

`onnxruntime-node` 는 네이티브 `.node` 바이너리를 담고 있다. **asar 안에서는 로드되지 않는다.**

```jsonc
"build": {
  "files": ["dist/**", "package.json"],
  "asarUnpack": ["**/node_modules/onnxruntime-node/**"]
}
```

### 9.3 `electron-rebuild` 는 필요 없다

`onnxruntime-node` 는 `napi_versions: [6]` — **N-API 라 ABI 가 안정적이다.** `better-sqlite3` 와 달리 Electron 용으로 다시 빌드할 필요가 없다. `postinstall` 의 electron-rebuild 대상에 넣지 말 것.

### 9.4 실측해야 하는 값

T1 에서 반드시 잰다. 아래 판단이 전부 여기 걸려 있다.

| 값 | 왜 필요한가 |
|---|---|
| 모델 4개 로딩 시간 | §4.3 유휴 종료 5분이 타당한가 |
| 100자 한국어 문장 합성 시간 (steps=8) | 실시간의 몇 배인가 → 선행 1청크로 충분한가 |
| 같은 값 steps=5 | 첫 청크를 낮출 만한가 |
| RSS 증가량 | 400MB 예상이 맞는가 |
| 재생 중 UI 프레임 | utilityProcess 분리가 실제로 듣는가 |

「1,200자/초」는 상류가 공개한 수치이고 우리 환경 값이 아니다. **T1 이전의 어떤 설계 결정도 이 수치에 기대지 않는다.**

---

## 10. 단계

| | 내용 | 완료 판정 |
|---|---|---|
| **T0** | 자산 확보 — HF 리비전 고정 복제, (필요시) Voice Builder 산출물 확보 | 로컬에 401MB, 백업 있음. **8월 안에** |
| **T1** | `nodejs/helper.js` 를 스크래치에서 그대로 돌려 본다. 앱 코드 없음 | **한국어 문장 하나가 wav 로 나온다.** §9.4 를 전부 실측 |
| **T2** | `helper.js` → `src/main/tts/synth.ts` 포팅(§5.4), utilityProcess 골격, 자산 다운로더, `tts:status`/`install` | 앱 안에서 블록 하나 재생. CSP 수정 포함 |
| **T3** | `SpeechQueue`, 청크 분할·선행 합성, 연속 재생, 하이라이트, 따라가기 스크롤 | **챕터 하나를 끊김 없이 완주** |
| **T4** | 툴바 재생 컨트롤, 음성·속도·품질 설정 UI, 번역 우선순위 연동, 유휴 종료 | 미번역 구간에서 자연스럽게 멈췄다 이어진다 |
| **T5** | (선택) 오디오 내보내기, 커스텀 보이스 선택 | — |

**T1 에서 판정하고 넘어간다.** 한국어 번역문의 낭독 품질이 쓸 만하지 않으면 T2 이후가 전부 무의미하다. 앱에 손대기 전에 답이 나오고, 반나절이면 된다.

T1 은 `git clone https://github.com/supertone-inc/supertonic` → `cd nodejs && npm i` → `node example_onnx.js --lang ko --text "..." --voice-style ../assets/voice_styles/F1.json` 이 전부다. **앱 코드를 한 줄도 쓰지 않는다.**

---

## 11. 결정 기록과 대가

### 11.1 파이썬을 쓰지 않는다

**얻는 것**: 설치본 하나로 끝난다. 이 PC 의 인증서 함정을 비껴간다. 포트·readiness·손자 프로세스 정리가 통째로 사라진다.

**대가**: 엔진 코드 559줄을 떠안는다. 상류가 아카이브되므로 갱신 부담은 없지만, 버그가 나오면 우리가 고친다. §7.3 의 `Synth` 인터페이스가 나중에 클라우드 TTS 로 갈아탈 자리를 남긴다.

### 11.2 자산을 동봉하지 않는다

**얻는 것**: 설치본이 400MB 가볍다. OpenRAIL-M 재배포 문제가 사용자와 Supertone 사이에 남는다.

**대가**: 첫 낭독 전에 400MB 다운로드가 있다. 상류가 사라지면 새 설치가 불가능해진다 — **§3.4 의 로컬 고정이 이 위험의 유일한 대비다.**

### 11.3 캐시하지 않는다

**얻는 것**: `.parallax` 가 커지지 않는다. 캐시 무효화 규칙이 없다.

**대가**: 같은 문단을 두 번 들으면 두 번 합성한다. §9.4 의 실측이 「합성이 재생보다 느리다」로 나오면 **이 결정을 뒤집어야 한다** — 그때는 세션 한정 디스크 캐시(temp, 앱 종료 시 삭제)를 넣는다.

### 11.4 낭독 대상은 한 열뿐

한국어와 영문을 번갈아 읽는 모드는 두지 않는다.

**대가**: 대역 학습용으로 쓰려면 아쉽다. 다만 두 열을 번갈아 읽으면 청크 수가 두 배가 되고, 두 언어를 오가는 프리롤이 매 문장 붙는다. 필요해지면 `side: "both"` 를 추가하되 그때 지연을 다시 잰다.

### 11.5 미번역 블록에서 멈춘다

**대가**: 번역이 느린 구간에서 낭독이 끊긴다. `mode: "all"` 로 미리 번역해 두면 해결되고, §8.2 의 우선순위 연동이 실사용에서 대부분을 흡수한다.

---

## 12. 열려 있는 것

- **오디오 내보내기** — 챕터를 하나의 wav/m4b 로. 상류 `call()` 의 이어붙이기 경로가 이미 하는 일이라 어렵지 않다. 다만 §5.4 의 O(n²) 이어붙이기는 고쳐 써야 한다.
- **커스텀 보이스 선택 UI** — 자산만 있으면 코드는 파일 경로 하나 차이다.
- **int8 양자화** — 공개 자산에 없다. 직접 양자화하면 398MB → ~100MB 가 되지만 품질 검증을 우리가 해야 한다. 메모리·다운로드가 문제로 드러날 때만.
- **웹 경로 재사용** — 상류 `web/` 은 WebGPU 로 돈다. 렌더러에서 직접 돌리면 utilityProcess 가 없어지고 GPU 도 쓰지만, `connect-src 'none'`·`script-src 'self'` 원칙과 400MB 자산 로딩을 렌더러가 떠안는다. **지금 구조와 맞지 않아 채택하지 않는다.**

---

## 13. 현재 코드 대조 (2026-08-04)

이 사양은 `65343ec` 를 읽고 썼다. 저장소로 옮기면서 지목된 자리를 전부 다시 확인했다.

### 13.1 확인된 것 — 사양대로다

| 사양 | 실제 | |
|---|---|---|
| CSP 에 `media-src` 없어 blob 오디오가 막힌다 (§7.4) | `renderer/index.html:6` — `default-src 'self'` 로 떨어진다 | ✅ |
| `allowed` 에 없는 채널은 throw (§7.1) | `preload/index.ts:42` — 채널 5개, `unknown channel` | ✅ |
| 가상 스크롤이 뷰포트 밖 행을 지운다 (§6.3) | `renderer/app.js:217` — `el.remove(); mounted.delete(id)` | ✅ |
| `chunkText` ko/ja 120, 나머지 300 (§5.2) | `helper.js:278` | ✅ |
| `Array.from(wav_tts.data)`, O(n²) 이어붙이기 (§5.4) | `helper.js:270, 292` | ✅ |
| 실질 의존은 `onnxruntime-node` 하나 (§5.4) | import 는 `fs`·`path`·`url`·`onnxruntime-node` 넷뿐. `fft.js`·`js-yaml` 미사용 | ✅ |

### 13.2 사양이 모르는 것 — 셋

**① `.npmrc` 가 `runtime=electron` 을 못 박고 있다 — T2 의 첫 관문**

```
runtime=electron
target=33.4.11
disturl=https://electronjs.org/headers
```

`better-sqlite3` 를 Electron ABI 로 받게 하려고 넣은 것인데(Node 24 용 prebuilt 가 없어 소스 컴파일로 새고, 이 PC 의 node-gyp 10 은 VS 2026 을 인식하지 못한다), `npm i onnxruntime-node` 에도 그대로 걸린다.

`onnxruntime-node` 는 N-API 라 ABI 무관 바이너리를 받으므로 §9.3 의 "electron-rebuild 불필요"는 유효할 것이다. 다만 그 install 스크립트가 `npm_config_runtime` 을 어떻게 읽는지는 **실제로 깔아 봐야 안다.** T2 에서 가장 먼저 확인할 것.

T1 은 저장소 밖에서 클론해 돌리므로 이 설정에 걸리지 않는다 — 그래서 T1 이 이 문제와 독립적으로 판정을 낼 수 있다.

**② `chunkText` 는 export 되지 않는다**

`helper.js:522` 의 내부 함수다. export 표면은 `loadVoiceStyle`·`loadTextToSpeech`·`writeWavFile`·`timer`·`sanitizeFilename` 다섯뿐이다. §5.2 가 "그대로 가져온다"고 한 것은 **함께 베낀다**는 뜻이지 import 가 아니다.

**③ 따라가기 스크롤에 `restore()` 를 쓰면 안 된다**

`app.js:349` 의 `restore()` 는 `mounted.get(id)` 가 없으면 그냥 돌아간다 — 재생 블록이 화면 밖이면 아무 일도 일어나지 않는다. §6.4 가 "`anchor()`/`restore()` 를 쓴다"고 한 것은 이 경우에 맞지 않는다. 목차 이동이 쓰는 패턴을 쓸 것:

```js
// app.js:680 — 이미 있는 것
scrollTo({ top: doc.offsetTop + tops[i] - 80, behavior: "smooth" });
```

`anchor()`/`restore()` 는 조판이 바뀔 때 읽던 자리를 지키는 용도로 남는다.

### 13.3 §8.3 의 전제가 그사이 튼튼해졌다

낭독에서 건너뛸 블록(`NO_TRANSLATE`·`DROPPED`·`figcaption`)의 판정이 믿을 만해졌다. `pdf-ko-translate` 스킬에 도판 안 글자·판독 잔해·그림 번호표를 걸러 내는 규칙이 들어갔다 — 그전이면 `ZINZINZINZ` 를 소리 내어 읽었을 것이다.

같은 시기에 `main/exporter.ts` 의 `renderMarkdown` 도 `DROPPED` 를 건너뛰게 됐다. **판정이 두 곳으로 갈렸다는 뜻이므로, T4 에서 공유 술어로 뽑는다:**

```ts
// shared/types.ts
export const isSpeakable  = (b: Block) => …   // NO_TRANSLATE·DROPPED·figcaption·table_raw 제외
export const isExportable = (b: Block) => …   // DROPPED 만 제외
```

### 13.4 T0 는 끝났다

`scripts/fetch-tts-assets.mjs` 로 받았다. `tts-assets/` 에 382.7MB(=401MB 십진), `VERSION` 에 리비전 해시. `.gitignore` 에 등록.

Voice Builder(커스텀 보이스)는 **2026-08-31 마감**이라 아직 열려 있다.

---

## 14. 조작 방식을 바꿨다 — 고르고 오른쪽 클릭 (2026-08-04)

§6·§8 은 「낭독을 켜고, 단락을 눌러 읽고, 재생 위치를 따라 스크롤한다」를 전제로
썼다. 실제로 만들어 보니 그 전제가 툴바에 값을 치렀다: 켜고 끄는 토글과 읽을 열을
고르는 선택기가 늘 자리를 차지하는데, **둘 다 낭독을 안 쓰는 동안에는 순수한 소음**
이다. 게다가 켜 둔 상태에서는 글을 짚으려는 클릭마다 소리가 난다.

**지금 방식은 이렇다.** 읽고 싶은 데를 고르고 **오른쪽 클릭**(또는 `Ctrl+T`)을 한다.
고른 자리 옆에 팝업이 떠서 재생/일시정지 · 정지 · 진행률 · 목소리 · 속도 ·
「MP3 로 내보내기」를 준다. 툴바에는 낭독이 없다.

바뀐 전제 넷:

- **단위가 블록이 아니라 선택이다.** 읽고 싶은 만큼이 늘 블록 경계와 같지 않다 —
  문단 한가운데의 두 문장, 블록 셋에 걸친 한 대목. 다만 글자만 넘기지는 않는다:
  범위를 복제해 **블록별 조각**(`{type, text}`)으로 쪼갠다. 내보내기가 제목 경계를
  알아야 하기 때문이다.
- **§11.4(낭독 대상은 한 열)의 선택기가 필요 없다.** 어느 열에서 골랐는지가 곧
  어느 언어다. 드래그 선택이 이미 시작한 열에만 걸리므로(`body.sel-src|sel-ko`)
  섞일 일도 없다. 오른쪽에서 골랐어도 한글이 없으면(=미번역이라 원문이 보이는
  중이면) 영어로 읽는다.
- **§8.2/§11.5(미번역 블록에서 멈춘다)가 해당 없다.** 보이는 글만 읽으므로 없는
  번역을 기다릴 일이 없다.
- **§6.3 의 재생 블록 강조(`.row.playing`)와 `playingId` 는 걷어냈다.** 재생 단위가
  선택이라 강조할 블록이 없고, 선택 자체가 이미 눈에 보인다.

### 14.1 재생은 조각으로 — §6.1 의 선행 합성을 여기서 한다

`Synth.speak()` 는 스스로 청크를 나누지 않는다. 처음에는 선택 전체를 한 번에
넘겼는데, 그러면 긴 대목에서 소리가 나기까지 한참 기다린다(367자에 30초).

지금은 `tts:plan` 이 조각 목록을 만들고 `tts:chunk(i)` 가 하나씩 내준다. 렌더러가
큐를 쥐고, **조각 하나를 트는 동안 다음 조각을 만든다.** 실측 0.24배속이라 합성이
재생보다 네 배 빠르므로 한 조각만 앞서면 끊기지 않는다 — 더 앞서 봐야 취소할 때
버리는 것만 는다. 첫 소리까지 **2.1초**(367자 기준, 모델 로딩 포함).

이로써 §11.3 이 정한 「캐시하지 않는다」가 재생 경로에서도 성립한다. 그리고 앞서 두었던
4,000자 상한이 사라졌다 — 상한은 「한 번에 다 만들고 튼다」의 부산물이었다.

### 14.2 mp3 내보내기 — 쪼개는 기준은 청크가 아니라 제목이다

**합성 청크로 쪼개면 안 된다.** 그건 엔진 사정(문장 몇 개)이라 20~30초짜리 파일이
수십 개 나온다. 사람이 듣는 단위가 아니다. **제목(h1~h3) 경계로 끊으면** 트랙 하나가
장 하나가 되어 오디오북처럼 쓰인다. 선택 안에 제목이 둘 미만이면 나눌 이유가 없으므로
한 파일이다.

이름:

```
여럿 : {책 제목}_{01}_{장 제목}.mp3
하나 : {책 제목}_{시작 세 단어}.mp3
```

- 번호는 **총 개수만큼 자릿수를 채운다.** `_1_`, `_10_` 이 섞이면 플레이어도 파일
  관리자도 문자열 정렬이라 `1, 10, 11, 2` 로 늘어선다.
- 파일명 금지문자(`\ / : * ? " < > |`)를 털고 길이를 자른다(책 40 · 장 40 · 세 단어 24자).
  세 단어만으로는 길이가 안 잡힌다 — `Notwithstanding the aforementioned` 같은 것이 나온다.
- 여럿이면 **폴더만** 고르게 하고 이름은 위 규칙이 정한다. 하나면 이름까지 고르게 한다.
  파일 이름을 하나 받아 나머지를 그 옆에 흩뿌리면 무엇이 어디 생기는지 알 수 없다.

**인코더는 `@breezystack/lamejs`(순수 JS)를 번들한다.** 이 PC 엔 ffmpeg 이 있지만
설치본에는 없다 — `python3` 가 Store 스텁이라 물렸던 것과 같은 함정이고, §11.1 이
정한 「외부 실행 의존을 얹지 않는다」와도 어긋난다. 실측 27배속이라 30분 오디오
인코딩이 1분 남짓이다. ESM 전용이라 `new Function("s","return import(s)")` 로 부른다
(tsc 가 CommonJS 로 내리면서 `await import()` 를 `require()` 로 바꿔 버린다).

**합성과 인코딩은 둘 다 워커에서 끝낸다.** 30분이면 중간 PCM 이 150MB 라 프로세스
경계를 왕복시킬 물건이 아니다. 진행률은 워커가 조각마다 `step` 으로 올린다.

64kbps 모노 — 32분에 약 15MB. 낭독에 그 이상은 값이 없다.

**중간에 멈춰도 이미 쓴 파일은 남긴다.** 지우면 40분을 다시 기다려야 한다.

### 14.3 속도는 재생 배속이다

합성기의 `speed` 가 아니라 `playbackRate` 로 준다. 즉시 먹고, 읽는 중에도 바뀌고,
음높이도 그대로다. 합성 파라미터로 주면 슬라이더 한 칸마다 전체를 다시 만들어야 한다.
목소리는 그럴 수 없어서 바꾸면 다시 합성한다.

---

## 15. 조작을 다시 옮겼다 — 툴바 + 고르면 준비 (2026-08-04)

§14 의 팝업은 고를 때마다 창이 떠서 본문을 가렸다. 낭독은 **읽는 동안 계속 켜져
있는 것**이라 뜨고 지는 창보다 늘 같은 자리가 맞다. 컨트롤은 툴바로 갔다:

```
[▶] [■]  읽는 중 42%   [F1▾] [1.0×▾] [MP3]
```

### 15.1 고르면 준비, 누르면 시작

고르는 순간 소리가 나면 글을 짚어 보려던 선택마다 놀라게 된다. **고르는 것은
「무엇을」이고, 누르는 것이 「지금」이다.** 고르면 조각 계획과 첫 조각 합성까지
끝내 두므로(§14.1 의 선행 합성을 준비 단계로 앞당긴 것이다) 누르는 순간 소리가 난다
— 실측 0.2초.

### 15.2 세 낱말 이하는 범위가 아니라 자리다

한 문장만 듣고 싶으면 그 문장을 고른다. **여기서부터 듣고 싶으면 한 낱말만 짚는다** —
그러면 그 블록부터 문서 끝까지 이어 읽는다. 경계를 세 낱말로 둔 것은, 그보다 짧게
고른 것을 「이만큼만 읽어라」로 읽는 것이 거의 언제나 오해이기 때문이다.

이어 읽기는 블록마다 계획하고 조각을 큐에 이어 붙인다. 읽을 수 없는 블록
(`NO_TRANSLATE`·`DROPPED`·`table_raw`·`figcaption`)과 번역이 아직 없는 블록은
건너뛴다 — 원문으로 바꿔 읽으면 언어가 문장 중간에 튄다.

§6.2 의 따라 스크롤이 여기서 되살아났다. 손으로 스크롤하면 3초 억제한다.

**사전을 더블클릭으로 옮겼다.** 낱말 하나를 고르면 사전 팝업도 함께 떠서, 자리를
정할 때마다 본문을 가렸다. 더블클릭은 낱말을 고르는 동작 그 자체라 손이 더 가지
않는다. 여는 길을 옮겼으니 **닫는 길을 따로 뒀다** — 밖을 한 번 누르면 닫힌다
(예전에는 다음 클릭이 곧 다음 `mouseup` 이라 저절로 닫혔다). 더블클릭의 둘째
누름까지 닫아 버리지 않도록 `e.detail === 1` 일 때만 본다.

### 15.3 낱말 짚기 — 어디까지 정확한가

**Supertonic 은 토큰별 길이를 주지 않는다.** `duration_predictor.onnx` 의 출력은
`duration` 하나뿐이고 `dims: [1]` 이다 — 발화 전체 길이 하나. 실측으로 확인했다
(3자든 48자든 값 하나). 그러므로 **진짜 정렬은 이 엔진으로 만들 수 없다.**

할 수 있는 것은 둘이다:

| | 정확도 | 값 |
|---|---|---|
| 조각(문장) 경계 | **정확** | 조각마다 WAV 길이를 알고 있다 |
| 조각 안의 낱말 | **추정** | 글자 수 비례(마침표 +4, 쉼표 +2 가중) |

지금은 둘을 겹쳐 쓴다. 문장 경계에서 매번 다시 맞물리므로 **어긋남이 한 문장
안에 갇힌다** — 문장이 3~8초라 최악이 그 절반 남짓이다. 실사용에서는 낱말이
소리를 따라 흐르는 것으로 보이고, 가끔 반 낱말쯤 앞서거나 늦는다.

진짜 정렬을 원하면 별도의 강제 정렬기(ASR/CTC)를 얹어야 하는데, 모델이 하나 더
붙고 합성 뒤에 한 번 더 도는 값이다. **낭독을 듣는 데 그만한 값은 없다고 본다.**

표시는 **두 겹**이다 — 읽을 영역을 옅게 깔고(`::highlight(say-range)`, 14%) 그 위에
지금 읽는 낱말을 조금 진하게 얹는다(`::highlight(say-word)`, 38%). 한 겹만 쓰면 둘 중
하나를 잃는다: 낱말만 칠하면 어디까지 읽을 것인지 안 보이고, 영역만 칠하면 어디를
읽고 있는지 안 보인다. 반투명이라 겹친 자리는 저절로 더 진해진다.

영역이 무엇인지는 방식에 따라 다르다 — **고른 범위를 읽을 때는 그 범위**, **여기부터
끝까지 읽을 때는 지금 읽는 블록**이다. 후자에 책 전체를 칠하면 표시가 아니라 배경이
된다. 브라우저 선택은 다음 클릭에 사라지지만 「무엇을 읽을 것인가」는 재생이 끝날
때까지 보여야 하므로, 선택과 별개로 우리가 들고 있는다.

**색은 짝 강조와 갈라 뒀다**(`--say` 청록 / `--accent` 슬레이트블루). 둘은 동시에 켜져
있을 수 있어서 — 마우스를 얹은 단락 쌍과 지금 읽는 자리는 다른 곳이다 — 같은 색이면
무엇이 무엇인지 알 수 없다. 경고색(`--warn`, 호박)과도 겹치지 않는다.

CSS Custom Highlight API 를 쓰는 이유는 그대로다. DOM 을 건드리지 않으므로
가상 스크롤이 행을 지웠다 다시 만들어도 텍스트 노드가 어긋나지 않고, 선택·사전 등
다른 기능과도 부딪히지 않는다. 배경만 칠하고 글자색은 두었다 — 색까지 바꾸면 읽던
줄의 대비가 낱말마다 튄다.
