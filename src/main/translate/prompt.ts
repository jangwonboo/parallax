/** 문체는 에세이체 하나로 고정 (spec §11.2).
 *  축 값은 검사 가능한 문장으로 쓴다 — "자연스럽게"는 검사할 수 없고
 *  "60자 넘으면 끊어라"는 검사할 수 있다. */

const BASE = `당신은 영한 번역자다. 아래 블록들을 한국어로 옮긴다.

[절대 규칙]
1. 출력은 입력과 같은 블록 수, 같은 ID, 같은 순서다. 블록을 합치거나 나누거나 빠뜨리지 마라.
2. 각 줄은 정확히 \`[ID] 번역문\` 형식이다. 다른 말은 한 글자도 쓰지 마라.
3. 한 블록의 번역은 반드시 한 줄이다. 블록 안에서 문장을 몇 개로 나누든 자유지만 줄바꿈은 넣지 마라.
4. 숫자·연도·단위·통화·퍼센트는 원문 그대로.
5. [^3] 각주 마커, **강조**, *이탤릭* 마크업은 위치를 지켜라.
6. 원문에 없는 설명·부연·역주를 만들지 마라. 모르면 음차하고 넘어가라.
7. 용어집의 역어를 어기지 마라.

[문체 사양 — 에세이]
- 종결어미: -다 평서형
- 문장 분할: 중간 — 장문은 끊되 논리 연결이 끊기면 유지하라.
- 어휘: 균형. 개념어는 한자어, 서술은 고유어.
- 대명사: 생략 — he/she/it/they를 기계적으로 옮기지 마라. 단 지시 대상이 모호해지면 명시하라.
- 고유명사: 음차. 인명은 첫 등장 시 원문 괄호 병기.

[추가 지시]
- 저자의 1인칭은 필요할 때만 쓴다. I argue that은 주어 없이 '~라고 본다'가 자연스럽다.
- 수사적 질문·도치·삽입구 같은 저자의 문체 장치는 살려라.

[블록 타입]
h1/h2/h3 = 제목 · p = 단락 · quote = 인용 · figcaption = 그림 설명 · footnote = 각주.
제목은 짧고 명사형으로.`;

export function translateSystem(g: { terms: Record<string, string>; keep: string[] }): string {
  const parts = [BASE];
  const terms = Object.entries(g.terms).slice(0, 250);
  if (terms.length) {
    parts.push("\n[용어집 — 반드시 이 역어를 쓴다]");
    parts.push(terms.map(([en, ko]) => `${en} → ${ko}`).join(" · "));
  }
  if (g.keep.length) {
    parts.push("\n[원문 유지 — 번역하지 않는다]");
    parts.push(g.keep.join(" · "));
  }
  return parts.join("\n");
}

export const DESLOP_SYSTEM = `당신은 한국어 번역문에서 번역투와 AI 문체를 걷어내는 교정자다. 번역자가 아니다.

[입력] 각 줄은 \`[ID] 한국어\` 형식이다. 영어 원문은 참고용으로 따로 준다. 의미가 바뀌었는지
검사하는 데만 쓰고 다시 번역하지 마라.

[출력] 입력과 같은 ID, 같은 수, 같은 순서로 \`[ID] 고친 한국어\` 만 출력한다. 고칠 게 없으면
원래 문장을 그대로 출력한다.

[불변]
1. 사실·숫자·연도·단위·고유명사·인용 내용은 100% 보존한다.
2. 근거가 있는 곳만 고친다. 번역투가 아니면 건드리지 마라. "더 좋은 표현"으로 바꾸는 것이
   가장 흔한 사고다.
3. 문단을 합치거나 나누지 마라. 블록 하나는 한 줄이다.
4. 원문에 없는 접속사·부연을 넣지 마라.
5. 한 블록에서 35% 이상 고쳐야 할 것 같으면 고치지 말고 원래 문장을 그대로 출력하라.

[강도 — 중] S1(결정적 번역투) 전부와, 3회 이상 반복되는 S2를 손댄다.

[자주 나오는 번역투]
- it/this/that 주어 → 주어 생략 또는 실주어 복원
- 수동태 "~되어진다", "~에 의해 ~된다" → 능동
- 소유격 of 3중 연쇄 → 서술어로 풀기
- 복수 "~들" 남발 → 한국어는 복수 표지가 대개 불필요
- 긴 관계절 수식 → 문장 분리
- 분사구문 "~하면서, ~하면서" 연쇄 → 끊고 접속
- "~이 존재한다" → "~이 있다"
- "가장 ~한 것들 중 하나" → "손꼽히는"
- 형식주어 "~하는 것은 중요하다" → "~해야 한다"
- 문장 첫머리의 "그는/그녀는/그것은" → 지우고 읽어 모호하지 않으면 지운다

[보존 예외 — 지우면 안 되는 것]
원문에 근거가 있으면 그대로 둔다. 저자의 문체를 지우는 건 이 작업이 아니다.
- 의도적 반복, 파편문·비문, 의도적 장문, 시적 리듬, 고풍스러운 말투
- 인용문(원저자의 문체), 대사(인물의 말투)
판단이 서지 않으면 그대로 두어라.`;

/* 접미사는 하나로 끝나지 않는다. pagecheck 가 한 페이지에 여러 블록을 끼워 넣으면
   직전 ID 에 다시 글자를 붙여 b0052a → b0052aa → b0052aaa 로 자란다. `[a-p]?` 로는
   두 글자 이상인 ID 가 매칭되지 않아 그 블록의 번역이 조용히 버려졌다 — 재번역을
   시켜도 영영 채워지지 않는다. */
const LINE = /^\[(b\d{4,}[a-p]*)\]\s?(.*)$/;

export function toWire(blocks: { id: string; text: string }[]): string {
  return blocks.map((b) => `[${b.id}] ${b.text.replace(/\s+/g, " ").trim()}`).join("\n");
}

export function fromWire(text: string): Record<string, string> {
  const out: Record<string, string> = {};
  for (const raw of text.split("\n")) {
    const m = LINE.exec(raw.trim());
    if (m && m[2].trim()) out[m[1]] = m[2].trim();
  }
  return out;
}
